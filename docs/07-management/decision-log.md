# Registro de Decisões

| ID | Data | Decisão | Status | Justificativa |
|---|---|---|---|---|
| D001 | 22/07/2026 | Focar projetos de software | Aprovada | Reduz amplitude e permite linguagem específica |
| D002 | 22/07/2026 | Público inicial: profissionais de tecnologia | Aprovada | Define persona concreta |
| D003 | 22/07/2026 | Aplicação individual no MVP | Aprovada | Reduz complexidade |
| D004 | 22/07/2026 | Execução antes de ensino | Aprovada | Produto deve ser ferramenta |
| D005 | 22/07/2026 | Orientação incorporada à UI | Aprovada | Diferencial central |
| D006 | 22/07/2026 | Orientar sem aprisionar | Aprovada | Respeita tailoring |
| D007 | 22/07/2026 | IA fora do MVP | Aprovada | Reduz risco técnico e custo |
| D008 | 22/07/2026 | Preparar lógica para IA futura | Aprovada | Evita retrabalho estrutural |
| D009 | 22/07/2026 | Ciclos de duas semanas | Aprovada | Compatível com 10h semanais |
| D010 | 22/07/2026 | GitHub como fonte oficial | Aprovada | Centraliza histórico |
| D011 | 22/07/2026 | Claude Code como ambiente | Aprovada | Apoia implementação |
| D012 | 22/07/2026 | Repositório privado até Release 0 | Aprovada | Organizar antes da exposição |
| D013 | 22/07/2026 | MoSCoW | Aprovada | Priorização simples |
| D014 | 22/07/2026 | Persistência simples com JSON | Aprovada | Evita perda e complexidade |
| D015 | 22/07/2026 | Testes automatizados desde Walking Skeleton | Aprovada | Protege regras críticas |
| D016 | 22/07/2026 | Fluxo híbrido de entrada | Aprovada | Orientação inicial com revisão livre |
| D017 | 22/07/2026 | Uma ação principal e alternativas acessíveis | Aprovada | Reduz carga cognitiva |
| D018 | 22/07/2026 | Pular gera explicação e pendência | Aprovada | Flexibilidade controlada |
| D019 | 22/07/2026 | Comparar stacks com vantagem ao SvelteKit | Aprovada | Evita decisão prematura |
| D020 | 22/07/2026 | Arquitetura e contratos antes da implementação assistida | Aprovada | Evita caixa-preta |
| D021 | 25/07/2026 | Testes com usuários externos serão realizados quando Matheus decidir que o Hydra atingiu maturidade suficiente. Até lá, o desenvolvimento será orientado pela visão de produto, pelas especificações reconciliadas, pela validação técnica e pelo dogfooding em projetos reais. | Aprovada | Uma amostra externa pequena avaliando uma experiência ainda incompleta pode produzir conclusões circunstanciais e induzir mudanças prematuras de direção. A decisão não rejeita definitivamente testes externos; apenas coloca seu momento sob decisão explícita do Product Owner. |
| D022 | 27/07/2026 | Antecipação seletiva e consciente da vertical Cockpit/Impedimentos (registrar, classificar, definir próxima ação, resolver e reabrir um impedimento; persistência, serialização e integração ao ProjectState; tela `/cockpit`; testes e jornada correspondentes), adiantada de Release 3 sem antecipar o restante do escopo daquele release. Responsável: Matheus. | Aprovada | Vertical fina e isolada (coleção independente do catálogo metodológico, sem gerar PendingItem nem alterar o motor de orientação) que já havia sido implementada; formalizar seu escopo em vez de descartar o trabalho. Permanecem em Release 3, não antecipados: quadro completo de execução, gestão de tarefas, dependências entre tarefas, decisões operacionais completas, controle de mudanças, atualização integrada de riscos, métricas, dashboards, colaboração, notificações e priorização inteligente. |
| D023 | 31/07/2026 | Primeira fatia da etapa 3 do roadmap ("Diagnóstico e rota recomendada"): o usuário pode definir explicitamente em qual fase o catálogo o projeto realmente começa (`Project.routeStartPhaseId`, um único sinal estruturado). A rota recomendada não apaga, conclui nem pula fases anteriores — `ActivityProgress` permanece intocado; o percurso completo continua disponível no Mapa; a próxima ação de `/now` passa a operar dentro da rota recomendada; ausência de escolha preserva integralmente o comportamento atual (catálogo completo, sem adaptação). Primeira evolução do schema SQLite desde `0001_init.sql`: aplicada de forma idempotente na inicialização do repositório (`PRAGMA table_info` + `ALTER TABLE` só quando a coluna ainda não existe), fora dos métodos CRUD. Responsável: Matheus. | Aprovada | Investigação prévia confirmou que não existe hoje nenhum sinal estruturado de tipo, aplicabilidade ou contexto de projeto, e que `catalogStatus` não deve ser usado como esse sinal (é propriedade estática de autoria do catálogo, não do projeto). Fora de escopo nesta fatia: classificação/tipos de projeto, aplicabilidade por atividade, status "não aplicável", diagnóstico automático, IA, uso de `catalogStatus` como sinal — permanecem para decisão futura, quando/se necessários. |
| D024 | 31/07/2026 | Última fatia da etapa 3 do roadmap ("Diagnóstico e rota recomendada"): diagnóstico de rota assistido por cinco respostas estruturadas e explícitas do usuário (uma por fase, Descoberta a Execução), em `/map`. A regra é determinística: recomenda a primeira estrutura ainda ausente (primeira resposta "Não" na ordem fixa) ou, se todas forem "Sim", recomenda "Validação e encerramento". As cinco respostas são transitórias — calculadas por uma action própria e nunca persistidas; somente a fase efetivamente aplicada é gravada em `Project.routeStartPhaseId`, pelo mecanismo já existente (D023, `setRouteStartPhase`). A recomendação pode ser ignorada ou substituída manualmente pelo seletor de fase já existente; o percurso completo continua sempre disponível. Sem IA, sem interpretação de texto livre, sem alteração automática de `ActivityProgress`. Com esta fatia atendida, a etapa 3 do roadmap é considerada concluída — o roadmap não exige diagnóstico automático derivado do estado persistido; as respostas estruturadas do próprio usuário representam o diagnóstico do contexto atual nesta primeira versão. Responsável: Matheus. | Aprovada | Fecha a etapa 3 com o menor mecanismo determinístico possível, reaproveitando integralmente a infraestrutura de rota já entregue por D023, sem introduzir schema, entidade, classificação de projeto, contagens de escopo/impedimentos como regra ou qualquer forma de inferência automática — itens que permanecem fora de escopo, para decisão futura quando/se necessários. |
| D025 | 31/07/2026 | Única fatia da etapa 4 do roadmap ("Entregas e backlog estruturados"): itens de escopo do bucket `agora` passam a formar o primeiro backlog executável, com acompanhamento de execução em três estados (`a_fazer`/`em_andamento`/`concluído`, `ScopeItem.executionStatus`). O status operacional é independente da confirmação do escopo: alterá-lo não invalida nem altera `ScopeVersion.confirmedAt`, e só é permitido em itens de uma versão já confirmada. O controle desta primeira versão fica em `/next-version/confirmed` (superfície provisória, sem rota nova); a convergência visual com os mockups de "Entregas" fica para decisão futura. Evolução idempotente do schema SQLite (mesmo mecanismo de D023: `PRAGMA table_info` + `ALTER TABLE` só quando a coluna faltar). Não é criada nenhuma entidade de tarefa nesta primeira versão — decomposição em subtarefas, responsáveis, prazos, dependências, comentários, métricas e Kanban permanecem fora de escopo. Responsável: Matheus. | Aprovada | Fecha a etapa 4 com o menor mecanismo mecânico possível, reaproveitando integralmente `ScopeItem`/`ScopeVersion` e o padrão de status binário já usado em `PendingItem`/`Impediment`, sem introduzir entidade nova, rota nova ou qualquer forma de gestão de tarefas — itens que permanecem para decisão futura, quando/se necessários. |
| D026 | 04/08/2026 | Segunda tela da subetapa 7.4 do roadmap ("Execução e controle"): abandono definitivo do nome "Cockpit" na linguagem do produto, substituído por "Acompanhamento" (título "Acompanhamento do projeto", slug técnico `tracking`); rota migrada de `/projects/[projectId]/cockpit` para `/projects/[projectId]/tracking`, sem rota de redirect legada — o produto ainda não tem usuários externos nem dependências públicas sobre a rota antiga (D021). A superfície passa a consolidar, somente com capacidades e dados já existentes, a situação atual do projeto (fase, atividade e progresso da fase), a síntese de Entregas (contagens por estado e itens realmente em andamento, sem promover automaticamente o primeiro item de "A fazer" a "próxima entrega") e as atenções reais (impedimentos abertos e pendências abertas de `openPendingItems`); a gestão de impedimentos (registrar, tipar, próxima ação, resolver, reabrir) é preservada integralmente. Riscos, marcos, mudanças em análise, decisões pendentes e pessoas/responsáveis ilustrados na referência visual (`12-cockpit.png`) não se tornaram requisitos de domínio — permanecem fora de escopo, sem sustentação estrutural no produto. Responsável: Matheus. | Aprovada | O mockup de referência tinha, em sua maioria, conteúdo sem sustentação no domínio atual (riscos/marcos/mudanças/decisões/pessoas como entidades estruturadas); duas auditorias somente-leitura confirmaram que uma superfície consolidada só com dados reais já existentes (fase/atividade/progresso, Entregas, impedimentos, pendências) atende ao propósito da tela sem criar capacidade de domínio nova. |
| D027 | 04/08/2026 | Decisão exclusivamente documental sobre a terceira posição da ordem da subetapa 7.4 do roadmap ("Execução e controle"): a tela "Detalhe de item de atenção" (referência `13-detalhe-item-atencao.png`) é classificada como `combinada com outra superfície`, sem rota, componente ou fluxo dedicado. Impedimentos já têm gestão completa (registrar, tipar, próxima ação, resolver, reabrir) em Acompanhamento (`/projects/[projectId]/tracking`); pendências abertas já encaminham diretamente ao seu ponto real de resolução em Agora (`/projects/[projectId]/now?activity=...`). Responsável, evidência, estado de validação, beneficiário e workflow de validação de entrega ilustrados no mockup não têm sustentação no domínio atual (`ImpedimentView`, `PendingItemView`) e não se tornam requisitos — a referência visual permanece só como registro histórico de intenção. Nenhuma mudança de domínio, persistência, schema, migrations, orientation engine, rota ou superfície existente foi realizada. Responsável: Matheus. | Aprovada | Uma rota de detalhe separada duplicaria mecânicas já existentes em Acompanhamento e Agora, sem capacidade de domínio real que a sustente; capacidades de domínio não devem ser criadas apenas porque aparecem em uma referência visual (regra de interpretação da etapa 7). Uma superfície dedicada pode voltar a ser considerada no futuro, somente se uma capacidade real (ex.: validação de entrega com evidência e responsável) vier a existir e exigir contexto que não caiba em Acompanhamento ou Agora. |
| D028 | 04/08/2026 | Quarto e último alvo da ordem da subetapa 7.4 do roadmap ("Execução e controle"): Registros (`/projects/[projectId]/records`) permanece como rota própria, classificada como `superfície própria reduzida` — a redução ocorre nos tipos de conteúdo apresentados, não nas fases: respostas de todas as fases do catálogo continuam apresentadas por fase, atividade e campo, com um índice determinístico das fases com resposta. Pendências abertas deixam de pertencer a esta superfície — já cobertas por Acompanhamento (síntese de atenções) e por Agora (ponto real de resolução); pendências resolvidas permanecem como histórico exclusivo, sem outro lugar no produto que as mostre. As seis categorias ilustrativas do mockup de referência (`14-registros.png`) — "Escopo confirmado", "Em construção", "Hipóteses", "Lacunas", "Ambiguidade" e "Entrega" — foram auditadas individualmente contra o domínio e não se tornaram requisitos: nenhuma corresponde a um dado real apresentável como contagem determinística sem duplicar informação já existente em outra superfície ou introduzir capacidade nova. A revisão de uma atividade concluída da Descoberta a partir de Registros usa uma origem própria e explícita (`from=records`, `ReviewOrigin` em `now/+page.server.ts`), com texto, ação e retorno específicos a Registros; a origem equivalente já existente a partir do Resumo (`from=summary`) permanece integralmente preservada, com seu próprio retorno; valores de origem não reconhecidos nunca controlam navegação ou redirect — caem no fluxo normal de Agora. Responsável: Matheus. | Aprovada | Auditoria de sustentação semântica confirmou que as categorias do mockup não correspondem a conceitos determinísticos, não duplicativos e sem heurística no domínio atual; a reutilização inicial de `from=summary` para a revisão a partir de Registros foi identificada como incorreta (ativava texto, CTA e redirect específicos do Resumo) e substituída por uma origem própria antes desta entrega ser considerada pronta. |
| D029 | 04/08/2026 | Composição aprovada para a subetapa 7.5 do roadmap ("Resultados e encerramento"): as três posições da ordem — Resultados e benefícios, Transição e adoção, Encerramento e aprendizado — convergem como uma única superfície própria, `/projects/[projectId]/closure` ("Resultados e encerramento"), com três seções internas na mesma ordem, em vez de três rotas distintas. Resultados e benefícios e Transição e adoção são classificadas como `combinada com outra superfície` (a superfície própria resultante); Encerramento e aprendizado converge como a própria superfície. A responsabilidade da superfície é responder o que foi alcançado e validado, o que precisa continuar depois do projeto, e o que foi aprendido e o que falta para concluir — preservando as responsabilidades já assentadas de Documento do projeto (artefato formal consolidado) e Registros (memória consultável de respostas e pendências resolvidas); o Documento do projeto não é estendido nesta subetapa. Sustentação exclusiva nas seis atividades já existentes da fase `validacao` (`app/src/lib/catalog/closure.ts`): `validar_entregas_criterios` e `coletar_feedback` (Resultados e benefícios); `transicao_proximos_passos` (Transição e adoção); `resolver_pendencias_finais`, `licoes_aprendidas` e `confirmar_encerramento` (Encerramento e aprendizado) — nenhum campo, entidade, contador, checklist, percentual, data ou responsável novo. Direção funcional inicial: superfície consultável e orientadora, respostas reais exibidas integralmente, atividades sem resposta representadas por estado simples sustentado pelo `ActivityProgress`, sem edição inline, sem busca/filtro/exportação, sem novo estado de projeto, sem nova ação formal de encerramento além do que o domínio já suporta, e sem `from=closure` nesta primeira fatia. Continuidade respeita a próxima ação real (`nextActivity`) em vez de assumir a fase de validação: CTA "Continuar encerramento em Agora" quando a próxima atividade real já é da própria fase `validacao`; CTA "Continuar projeto em Agora" quando a próxima atividade real ainda pertence a uma fase anterior; nenhum CTA, substituído por mensagem de conclusão da etapa, quando as seis atividades da fase estão em estado terminal (concluída ou pulada) — sem criar status formal novo de projeto. Estado "Indisponível" ilustrado no protótipo visual foi tratado como não vinculante nesta primeira fatia e não implementado: auditoria confirmou que nenhuma rota do workspace, incluindo esta, tem hoje um ponto de falha isolado e independente do carregamento já resolvido pelo layout do projeto — construir uma tela de erro dedicada sem um caminho de falha real seria código não alcançável pela jornada normal. Responsável: Matheus. | Aprovada | Os três mockups de referência (`16-resultados-e-beneficios.png`, `17-transicao-e-adocao.png`, `18-encerramento-e-aprendizado.png`) têm, em sua maioria, conteúdo sem sustentação no domínio atual — tabela Antes/Meta/Atual, métricas quantitativas, percentuais de progresso, checklist de adoção com status e datas, botão "Marcar como iniciada", contadores estruturados de objetivos/entregas/riscos/mudanças/decisões e subdivisão artificial de lições aprendidas. Auditoria confirmou que uma única superfície apoiada só nas seis respostas de texto livre já persistidas atende à intenção das três telas sem criar capacidade de domínio nova; consolidar em uma rota evita fragmentar em três telas rasas o mesmo conjunto pequeno de respostas. |
| D030 | 05/08/2026 | Decisão de composição para o primeiro item da subetapa 7.6 do roadmap ("Complementos"): "Configurações do projeto" converge como superfície própria reduzida, `/projects/[projectId]/settings` ("Configurações"), última posição da navegação do workspace, depois de "Exportar". O escopo funcional cobre somente capacidades sustentadas pelo domínio atual: exibir o nome atual do projeto; permitir renomeá-lo usando o use case `renameProject` já existente (`server/application/project-use-cases.ts`); exibir a data de criação (`Project.createdAt`) como informação somente leitura; ação "Salvar alterações"; ação "Cancelar", restaurando apenas o valor carregado; link secundário para `/projects/[projectId]/export`. Descrição, responsável, estado editável, última atualização, classificação e tipo, regras de orientação, duplicar projeto, arquivar projeto e excluir projeto — todos ilustrados na referência (`19-configuracoes-do-projeto.png`) — não têm sustentação no domínio atual (`Project` só tem `id`, `name`, `createdAt`, `routeStartPhaseId`; não existe use case de duplicar, arquivar ou excluir) e ficam fora desta fatia, sem virar requisito nem justificar expansão de schema. Esta decisão nasceu exclusivamente de composição e escopo, anterior à implementação; execução confirmada nesta entrega — rota própria reduzida em `/projects/[projectId]/settings`, posicionada depois de "Exportar" no shell; escopo entregue restrito a renomeação e data de criação, exatamente como decidido; validação de nome vazio implementada na camada da rota (action), sem novo erro de domínio; nenhuma mudança de domínio, schema, migrations, persistência, orientation engine ou catálogo foi realizada — só `server/application/types.ts` e `server/application/project-view.ts` ganharam o campo `createdAt` em `ProjectView`, propagando um dado já existente em `Project`; os itens rejeitados (descrição, responsável, estado editável, classificação e tipo, regras de orientação, duplicar, arquivar, excluir) continuam fora do Release 0. Responsável: Matheus. | Aprovada | `renameProject` já existe, testado, na camada de aplicação, sem nenhuma rota que o exponha — a menor fatia real é dar interface a uma capacidade já pronta, em vez de expandir o domínio para sustentar os demais campos do mockup sem necessidade comprovada. |
| D031 | 05/08/2026 | Decisão de composição para o segundo item da subetapa 7.6 do roadmap ("Complementos"): "Exportar" converge como superfície própria em `/projects/[projectId]/export` ("Exportar projeto"), mantendo a posição já usada pela navegação do shell e pelo link existente em Configurações. A página apresenta uma explicação breve de que os dados do projeto serão baixados em JSON e uma ação principal única, "Baixar exportação". O download efetivo passa a ter um endpoint próprio, `/projects/[projectId]/export/download`, para conviver na mesma rota com a página via negociação de conteúdo do SvelteKit (requisição de navegação recebe a página; requisição de download recebe o arquivo). O `+server.ts` atual em `/export` (`GET`, JSON com `content-disposition: attachment`, nome `hydra-{projectId}.json`) é preservado inicialmente, sem alteração, para compatibilidade com requisições não HTML já existentes contra essa rota; nenhuma mudança no use case `exportProject` (`server/application/project-use-cases.ts`) nem no formato ou conteúdo do JSON exportado. Fora de escopo desta fatia: importação, escolha de formato, opções avançadas, histórico de exportações, agendamento, compartilhamento, métricas e qualquer configuração fictícia. Um estado visual próprio de "Projeto não encontrado" não é criado nesta fatia — o tratamento de erro relevante continua no layout pai e no `+server.ts` já existentes; essa ausência é tratada como decisão separada, a revisitar se um caminho de falha isolado e real vier a existir. Esta decisão nasceu exclusivamente de composição e escopo, anterior à implementação; execução confirmada nesta entrega — página própria em `/projects/[projectId]/export`, ação "Baixar exportação" apontando para o novo endpoint `/projects/[projectId]/export/download`; handler legado em `/export` preservado, sem alteração de contrato, para requisições não HTML (negociação de conteúdo do SvelteKit); construção da resposta (nome do arquivo, headers, conteúdo JSON) extraída para um único helper local compartilhado pelos dois endpoints, evitando duplicar a regra entre eles; nome real do arquivo (`hydra-{projectId}.json`) calculado pela mesma regra e exibido na página, sem placeholder; nenhuma mudança de domínio, use case, schema, migrations ou formato do JSON exportado; itens fora de escopo (importação, escolha de formato, histórico, agendamento, compartilhamento, métricas, estado visual próprio de erro) permanecem fora do Release 0, exatamente como decidido. Responsável: Matheus. | Aprovada | O item estava classificado como `não auditada`: a rota `/export` hoje só existe como endpoint de download (`+server.ts`), sem nenhuma página — o link do shell e de Configurações aponta para uma URL que nunca renderiza conteúdo próprio nem reflete `aria-current`, e o erro de projeto inexistente cai na página de erro genérica do SvelteKit, fora da identidade visual do produto. Não há referência visual individual para esta tela (ausente na montagem `hydra-target-screens.png`); a decisão de composição, por isso, precisa anteceder o brief visual em vez de decorrer dele. Separar página e download em rotas distintas evita reescrever o mecanismo de exportação já testado, e permite que a página use o shell real do workspace como as demais telas convergidas. |
