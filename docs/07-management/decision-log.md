# Registro de Decisões

| ID | Data | Decisão | Status | Justificativa |
|---|---|---|---|---|
| D001 | 22/07/2026 | Focar projetos de software | Aprovada | Reduz amplitude e permite linguagem específica |
| D002 | 22/07/2026 | Público inicial: profissionais de tecnologia | Aprovada | Define persona concreta |
| D003 | 22/07/2026 | Aplicação individual no MVP | Aprovada | Reduz complexidade |
| D004 | 22/07/2026 | Execução antes de ensino | Aprovada | Produto deve ser ferramenta |
| D005 | 22/07/2026 | Orientação incorporada à UI | Aprovada | Diferencial central |
| D006 | 22/07/2026 | Orientar sem aprisionar | Aprovada | Respeita tailoring |
| D007 | 22/07/2026 | IA fora do MVP | Aprovada | Reduz risco técnico e custo |
| D008 | 22/07/2026 | Preparar lógica para IA futura | Aprovada | Evita retrabalho estrutural |
| D009 | 22/07/2026 | Ciclos de duas semanas | Aprovada | Compatível com 10h semanais |
| D010 | 22/07/2026 | GitHub como fonte oficial | Aprovada | Centraliza histórico |
| D011 | 22/07/2026 | Claude Code como ambiente | Aprovada | Apoia implementação |
| D012 | 22/07/2026 | Repositório privado até Release 0 | Aprovada | Organizar antes da exposição |
| D013 | 22/07/2026 | MoSCoW | Aprovada | Priorização simples |
| D014 | 22/07/2026 | Persistência simples com JSON | Aprovada | Evita perda e complexidade |
| D015 | 22/07/2026 | Testes automatizados desde Walking Skeleton | Aprovada | Protege regras críticas |
| D016 | 22/07/2026 | Fluxo híbrido de entrada | Aprovada | Orientação inicial com revisão livre |
| D017 | 22/07/2026 | Uma ação principal e alternativas acessíveis | Aprovada | Reduz carga cognitiva |
| D018 | 22/07/2026 | Pular gera explicação e pendência | Aprovada | Flexibilidade controlada |
| D019 | 22/07/2026 | Comparar stacks com vantagem ao SvelteKit | Aprovada | Evita decisão prematura |
| D020 | 22/07/2026 | Arquitetura e contratos antes da implementação assistida | Aprovada | Evita caixa-preta |
| D021 | 25/07/2026 | Testes com usuários externos serão realizados quando Matheus decidir que o Hydra atingiu maturidade suficiente. Até lá, o desenvolvimento será orientado pela visão de produto, pelas especificações reconciliadas, pela validação técnica e pelo dogfooding em projetos reais. | Aprovada | Uma amostra externa pequena avaliando uma experiência ainda incompleta pode produzir conclusões circunstanciais e induzir mudanças prematuras de direção. A decisão não rejeita definitivamente testes externos; apenas coloca seu momento sob decisão explícita do Product Owner. |
| D022 | 27/07/2026 | Antecipação seletiva e consciente da vertical Cockpit/Impedimentos (registrar, classificar, definir próxima ação, resolver e reabrir um impedimento; persistência, serialização e integração ao ProjectState; tela `/cockpit`; testes e jornada correspondentes), adiantada de Release 3 sem antecipar o restante do escopo daquele release. Responsável: Matheus. | Aprovada | Vertical fina e isolada (coleção independente do catálogo metodológico, sem gerar PendingItem nem alterar o motor de orientação) que já havia sido implementada; formalizar seu escopo em vez de descartar o trabalho. Permanecem em Release 3, não antecipados: quadro completo de execução, gestão de tarefas, dependências entre tarefas, decisões operacionais completas, controle de mudanças, atualização integrada de riscos, métricas, dashboards, colaboração, notificações e priorização inteligente. |
| D023 | 31/07/2026 | Primeira fatia da etapa 3 do roadmap ("Diagnóstico e rota recomendada"): o usuário pode definir explicitamente em qual fase o catálogo o projeto realmente começa (`Project.routeStartPhaseId`, um único sinal estruturado). A rota recomendada não apaga, conclui nem pula fases anteriores — `ActivityProgress` permanece intocado; o percurso completo continua disponível no Mapa; a próxima ação de `/now` passa a operar dentro da rota recomendada; ausência de escolha preserva integralmente o comportamento atual (catálogo completo, sem adaptação). Primeira evolução do schema SQLite desde `0001_init.sql`: aplicada de forma idempotente na inicialização do repositório (`PRAGMA table_info` + `ALTER TABLE` só quando a coluna ainda não existe), fora dos métodos CRUD. Responsável: Matheus. | Aprovada | Investigação prévia confirmou que não existe hoje nenhum sinal estruturado de tipo, aplicabilidade ou contexto de projeto, e que `catalogStatus` não deve ser usado como esse sinal (é propriedade estática de autoria do catálogo, não do projeto). Fora de escopo nesta fatia: classificação/tipos de projeto, aplicabilidade por atividade, status "não aplicável", diagnóstico automático, IA, uso de `catalogStatus` como sinal — permanecem para decisão futura, quando/se necessários. |
| D024 | 31/07/2026 | Última fatia da etapa 3 do roadmap ("Diagnóstico e rota recomendada"): diagnóstico de rota assistido por cinco respostas estruturadas e explícitas do usuário (uma por fase, Descoberta a Execução), em `/map`. A regra é determinística: recomenda a primeira estrutura ainda ausente (primeira resposta "Não" na ordem fixa) ou, se todas forem "Sim", recomenda "Validação e encerramento". As cinco respostas são transitórias — calculadas por uma action própria e nunca persistidas; somente a fase efetivamente aplicada é gravada em `Project.routeStartPhaseId`, pelo mecanismo já existente (D023, `setRouteStartPhase`). A recomendação pode ser ignorada ou substituída manualmente pelo seletor de fase já existente; o percurso completo continua sempre disponível. Sem IA, sem interpretação de texto livre, sem alteração automática de `ActivityProgress`. Com esta fatia atendida, a etapa 3 do roadmap é considerada concluída — o roadmap não exige diagnóstico automático derivado do estado persistido; as respostas estruturadas do próprio usuário representam o diagnóstico do contexto atual nesta primeira versão. Responsável: Matheus. | Aprovada | Fecha a etapa 3 com o menor mecanismo determinístico possível, reaproveitando integralmente a infraestrutura de rota já entregue por D023, sem introduzir schema, entidade, classificação de projeto, contagens de escopo/impedimentos como regra ou qualquer forma de inferência automática — itens que permanecem fora de escopo, para decisão futura quando/se necessários. |
| D025 | 31/07/2026 | Única fatia da etapa 4 do roadmap ("Entregas e backlog estruturados"): itens de escopo do bucket `agora` passam a formar o primeiro backlog executável, com acompanhamento de execução em três estados (`a_fazer`/`em_andamento`/`concluído`, `ScopeItem.executionStatus`). O status operacional é independente da confirmação do escopo: alterá-lo não invalida nem altera `ScopeVersion.confirmedAt`, e só é permitido em itens de uma versão já confirmada. O controle desta primeira versão fica em `/next-version/confirmed` (superfície provisória, sem rota nova); a convergência visual com os mockups de "Entregas" fica para decisão futura. Evolução idempotente do schema SQLite (mesmo mecanismo de D023: `PRAGMA table_info` + `ALTER TABLE` só quando a coluna faltar). Não é criada nenhuma entidade de tarefa nesta primeira versão — decomposição em subtarefas, responsáveis, prazos, dependências, comentários, métricas e Kanban permanecem fora de escopo. Responsável: Matheus. | Aprovada | Fecha a etapa 4 com o menor mecanismo mecânico possível, reaproveitando integralmente `ScopeItem`/`ScopeVersion` e o padrão de status binário já usado em `PendingItem`/`Impediment`, sem introduzir entidade nova, rota nova ou qualquer forma de gestão de tarefas — itens que permanecem para decisão futura, quando/se necessários. |
| D026 | 04/08/2026 | Segunda tela da subetapa 7.4 do roadmap ("Execução e controle"): abandono definitivo do nome "Cockpit" na linguagem do produto, substituído por "Acompanhamento" (título "Acompanhamento do projeto", slug técnico `tracking`); rota migrada de `/projects/[projectId]/cockpit` para `/projects/[projectId]/tracking`, sem rota de redirect legada — o produto ainda não tem usuários externos nem dependências públicas sobre a rota antiga (D021). A superfície passa a consolidar, somente com capacidades e dados já existentes, a situação atual do projeto (fase, atividade e progresso da fase), a síntese de Entregas (contagens por estado e itens realmente em andamento, sem promover automaticamente o primeiro item de "A fazer" a "próxima entrega") e as atenções reais (impedimentos abertos e pendências abertas de `openPendingItems`); a gestão de impedimentos (registrar, tipar, próxima ação, resolver, reabrir) é preservada integralmente. Riscos, marcos, mudanças em análise, decisões pendentes e pessoas/responsáveis ilustrados na referência visual (`12-cockpit.png`) não se tornaram requisitos de domínio — permanecem fora de escopo, sem sustentação estrutural no produto. Responsável: Matheus. | Aprovada | O mockup de referência tinha, em sua maioria, conteúdo sem sustentação no domínio atual (riscos/marcos/mudanças/decisões/pessoas como entidades estruturadas); duas auditorias somente-leitura confirmaram que uma superfície consolidada só com dados reais já existentes (fase/atividade/progresso, Entregas, impedimentos, pendências) atende ao propósito da tela sem criar capacidade de domínio nova. |
| D027 | 04/08/2026 | Decisão exclusivamente documental sobre a terceira posição da ordem da subetapa 7.4 do roadmap ("Execução e controle"): a tela "Detalhe de item de atenção" (referência `13-detalhe-item-atencao.png`) é classificada como `combinada com outra superfície`, sem rota, componente ou fluxo dedicado. Impedimentos já têm gestão completa (registrar, tipar, próxima ação, resolver, reabrir) em Acompanhamento (`/projects/[projectId]/tracking`); pendências abertas já encaminham diretamente ao seu ponto real de resolução em Agora (`/projects/[projectId]/now?activity=...`). Responsável, evidência, estado de validação, beneficiário e workflow de validação de entrega ilustrados no mockup não têm sustentação no domínio atual (`ImpedimentView`, `PendingItemView`) e não se tornam requisitos — a referência visual permanece só como registro histórico de intenção. Nenhuma mudança de domínio, persistência, schema, migrations, orientation engine, rota ou superfície existente foi realizada. Responsável: Matheus. | Aprovada | Uma rota de detalhe separada duplicaria mecânicas já existentes em Acompanhamento e Agora, sem capacidade de domínio real que a sustente; capacidades de domínio não devem ser criadas apenas porque aparecem em uma referência visual (regra de interpretação da etapa 7). Uma superfície dedicada pode voltar a ser considerada no futuro, somente se uma capacidade real (ex.: validação de entrega com evidência e responsável) vier a existir e exigir contexto que não caiba em Acompanhamento ou Agora. |
| D028 | 04/08/2026 | Quarto e último alvo da ordem da subetapa 7.4 do roadmap ("Execução e controle"): Registros (`/projects/[projectId]/records`) permanece como rota própria, classificada como `superfície própria reduzida` — a redução ocorre nos tipos de conteúdo apresentados, não nas fases: respostas de todas as fases do catálogo continuam apresentadas por fase, atividade e campo, com um índice determinístico das fases com resposta. Pendências abertas deixam de pertencer a esta superfície — já cobertas por Acompanhamento (síntese de atenções) e por Agora (ponto real de resolução); pendências resolvidas permanecem como histórico exclusivo, sem outro lugar no produto que as mostre. As seis categorias ilustrativas do mockup de referência (`14-registros.png`) — "Escopo confirmado", "Em construção", "Hipóteses", "Lacunas", "Ambiguidade" e "Entrega" — foram auditadas individualmente contra o domínio e não se tornaram requisitos: nenhuma corresponde a um dado real apresentável como contagem determinística sem duplicar informação já existente em outra superfície ou introduzir capacidade nova. A revisão de uma atividade concluída da Descoberta a partir de Registros usa uma origem própria e explícita (`from=records`, `ReviewOrigin` em `now/+page.server.ts`), com texto, ação e retorno específicos a Registros; a origem equivalente já existente a partir do Resumo (`from=summary`) permanece integralmente preservada, com seu próprio retorno; valores de origem não reconhecidos nunca controlam navegação ou redirect — caem no fluxo normal de Agora. Responsável: Matheus. | Aprovada | Auditoria de sustentação semântica confirmou que as categorias do mockup não correspondem a conceitos determinísticos, não duplicativos e sem heurística no domínio atual; a reutilização inicial de `from=summary` para a revisão a partir de Registros foi identificada como incorreta (ativava texto, CTA e redirect específicos do Resumo) e substituída por uma origem própria antes desta entrega ser considerada pronta. |
| D029 | 04/08/2026 | Composição aprovada para a subetapa 7.5 do roadmap ("Resultados e encerramento"): as três posições da ordem — Resultados e benefícios, Transição e adoção, Encerramento e aprendizado — convergem como uma única superfície própria, `/projects/[projectId]/closure` ("Resultados e encerramento"), com três seções internas na mesma ordem, em vez de três rotas distintas. Resultados e benefícios e Transição e adoção são classificadas como `combinada com outra superfície` (a superfície própria resultante); Encerramento e aprendizado converge como a própria superfície. A responsabilidade da superfície é responder o que foi alcançado e validado, o que precisa continuar depois do projeto, e o que foi aprendido e o que falta para concluir — preservando as responsabilidades já assentadas de Documento do projeto (artefato formal consolidado) e Registros (memória consultável de respostas e pendências resolvidas); o Documento do projeto não é estendido nesta subetapa. Sustentação exclusiva nas seis atividades já existentes da fase `validacao` (`app/src/lib/catalog/closure.ts`): `validar_entregas_criterios` e `coletar_feedback` (Resultados e benefícios); `transicao_proximos_passos` (Transição e adoção); `resolver_pendencias_finais`, `licoes_aprendidas` e `confirmar_encerramento` (Encerramento e aprendizado) — nenhum campo, entidade, contador, checklist, percentual, data ou responsável novo. Direção funcional inicial: superfície consultável e orientadora, respostas reais exibidas integralmente, atividades sem resposta representadas por estado simples sustentado pelo `ActivityProgress`, sem edição inline, sem busca/filtro/exportação, sem novo estado de projeto, sem nova ação formal de encerramento além do que o domínio já suporta, e sem `from=closure` nesta primeira fatia. Continuidade respeita a próxima ação real (`nextActivity`) em vez de assumir a fase de validação: CTA "Continuar encerramento em Agora" quando a próxima atividade real já é da própria fase `validacao`; CTA "Continuar projeto em Agora" quando a próxima atividade real ainda pertence a uma fase anterior; nenhum CTA, substituído por mensagem de conclusão da etapa, quando as seis atividades da fase estão em estado terminal (concluída ou pulada) — sem criar status formal novo de projeto. Estado "Indisponível" ilustrado no protótipo visual foi tratado como não vinculante nesta primeira fatia e não implementado: auditoria confirmou que nenhuma rota do workspace, incluindo esta, tem hoje um ponto de falha isolado e independente do carregamento já resolvido pelo layout do projeto — construir uma tela de erro dedicada sem um caminho de falha real seria código não alcançável pela jornada normal. Responsável: Matheus. | Aprovada | Os três mockups de referência (`16-resultados-e-beneficios.png`, `17-transicao-e-adocao.png`, `18-encerramento-e-aprendizado.png`) têm, em sua maioria, conteúdo sem sustentação no domínio atual — tabela Antes/Meta/Atual, métricas quantitativas, percentuais de progresso, checklist de adoção com status e datas, botão "Marcar como iniciada", contadores estruturados de objetivos/entregas/riscos/mudanças/decisões e subdivisão artificial de lições aprendidas. Auditoria confirmou que uma única superfície apoiada só nas seis respostas de texto livre já persistidas atende à intenção das três telas sem criar capacidade de domínio nova; consolidar em uma rota evita fragmentar em três telas rasas o mesmo conjunto pequeno de respostas. |
| D030 | 05/08/2026 | Decisão de composição para o primeiro item da subetapa 7.6 do roadmap ("Complementos"): "Configurações do projeto" converge como superfície própria reduzida, `/projects/[projectId]/settings` ("Configurações"), última posição da navegação do workspace, depois de "Exportar". O escopo funcional cobre somente capacidades sustentadas pelo domínio atual: exibir o nome atual do projeto; permitir renomeá-lo usando o use case `renameProject` já existente (`server/application/project-use-cases.ts`); exibir a data de criação (`Project.createdAt`) como informação somente leitura; ação "Salvar alterações"; ação "Cancelar", restaurando apenas o valor carregado; link secundário para `/projects/[projectId]/export`. Descrição, responsável, estado editável, última atualização, classificação e tipo, regras de orientação, duplicar projeto, arquivar projeto e excluir projeto — todos ilustrados na referência (`19-configuracoes-do-projeto.png`) — não têm sustentação no domínio atual (`Project` só tem `id`, `name`, `createdAt`, `routeStartPhaseId`; não existe use case de duplicar, arquivar ou excluir) e ficam fora desta fatia, sem virar requisito nem justificar expansão de schema. Esta decisão nasceu exclusivamente de composição e escopo, anterior à implementação; execução confirmada nesta entrega — rota própria reduzida em `/projects/[projectId]/settings`, posicionada depois de "Exportar" no shell; escopo entregue restrito a renomeação e data de criação, exatamente como decidido; validação de nome vazio implementada na camada da rota (action), sem novo erro de domínio; nenhuma mudança de domínio, schema, migrations, persistência, orientation engine ou catálogo foi realizada — só `server/application/types.ts` e `server/application/project-view.ts` ganharam o campo `createdAt` em `ProjectView`, propagando um dado já existente em `Project`; os itens rejeitados (descrição, responsável, estado editável, classificação e tipo, regras de orientação, duplicar, arquivar, excluir) continuam fora do Release 0. Responsável: Matheus. | Aprovada | `renameProject` já existe, testado, na camada de aplicação, sem nenhuma rota que o exponha — a menor fatia real é dar interface a uma capacidade já pronta, em vez de expandir o domínio para sustentar os demais campos do mockup sem necessidade comprovada. |
| D031 | 05/08/2026 | Decisão de composição para o segundo item da subetapa 7.6 do roadmap ("Complementos"): "Exportar" converge como superfície própria em `/projects/[projectId]/export` ("Exportar projeto"), mantendo a posição já usada pela navegação do shell e pelo link existente em Configurações. A página apresenta uma explicação breve de que os dados do projeto serão baixados em JSON e uma ação principal única, "Baixar exportação". O download efetivo passa a ter um endpoint próprio, `/projects/[projectId]/export/download`, para conviver na mesma rota com a página via negociação de conteúdo do SvelteKit (requisição de navegação recebe a página; requisição de download recebe o arquivo). O `+server.ts` atual em `/export` (`GET`, JSON com `content-disposition: attachment`, nome `hydra-{projectId}.json`) é preservado inicialmente, sem alteração, para compatibilidade com requisições não HTML já existentes contra essa rota; nenhuma mudança no use case `exportProject` (`server/application/project-use-cases.ts`) nem no formato ou conteúdo do JSON exportado. Fora de escopo desta fatia: importação, escolha de formato, opções avançadas, histórico de exportações, agendamento, compartilhamento, métricas e qualquer configuração fictícia. Um estado visual próprio de "Projeto não encontrado" não é criado nesta fatia — o tratamento de erro relevante continua no layout pai e no `+server.ts` já existentes; essa ausência é tratada como decisão separada, a revisitar se um caminho de falha isolado e real vier a existir. Esta decisão nasceu exclusivamente de composição e escopo, anterior à implementação; execução confirmada nesta entrega — página própria em `/projects/[projectId]/export`, ação "Baixar exportação" apontando para o novo endpoint `/projects/[projectId]/export/download`; handler legado em `/export` preservado, sem alteração de contrato, para requisições não HTML (negociação de conteúdo do SvelteKit); construção da resposta (nome do arquivo, headers, conteúdo JSON) extraída para um único helper local compartilhado pelos dois endpoints, evitando duplicar a regra entre eles; nome real do arquivo (`hydra-{projectId}.json`) calculado pela mesma regra e exibido na página, sem placeholder; nenhuma mudança de domínio, use case, schema, migrations ou formato do JSON exportado; itens fora de escopo (importação, escolha de formato, histórico, agendamento, compartilhamento, métricas, estado visual próprio de erro) permanecem fora do Release 0, exatamente como decidido. Responsável: Matheus. | Aprovada | O item estava classificado como `não auditada`: a rota `/export` hoje só existe como endpoint de download (`+server.ts`), sem nenhuma página — o link do shell e de Configurações aponta para uma URL que nunca renderiza conteúdo próprio nem reflete `aria-current`, e o erro de projeto inexistente cai na página de erro genérica do SvelteKit, fora da identidade visual do produto. Não há referência visual individual para esta tela (ausente na montagem `hydra-target-screens.png`); a decisão de composição, por isso, precisa anteceder o brief visual em vez de decorrer dele. Separar página e download em rotas distintas evita reescrever o mecanismo de exportação já testado, e permite que a página use o shell real do workspace como as demais telas convergidas. |
| D032 | 09/08/2026 | Direção de produto pós-Ciclo 5, decorrente do dogfooding real de C5-01 e de protótipos avaliados no Claude Design (projeto "Atividade guiada: Afetados pelo problema", arquivos `Quem é Afetado.dc.html`, `Como é Tratado Hoje.dc.html`, `Organizar Quem Participa.dc.html`, `Riscos do Projeto.dc.html` — referência de mecânica de interação, não de implementação ou de conteúdo literal como "Documento do projeto"/"Em seguida"). Gramática de produto validada, a governar a evolução da jornada guiada a partir de agora: (1) construção estruturada — o usuário trabalha com decisões e objetos do projeto, não principalmente texto livre; (2) continuidade — objetos criados numa atividade permanecem vivos e são reutilizados nas seguintes sem redigitação; (3) ramificação — respostas e estado do projeto podem fazer perguntas/passos aparecerem ou desaparecerem; (4) profundidade seletiva — o usuário pode manter um objeto simples ou detalhá-lo quando fizer sentido, sem profundidade como padrão; (5) orientação a partir do contexto — o Hydra pode usar dados e relações já existentes para chamar atenção a algo relevante, incluindo ausências (ex.: grupo afetado sem forma de comunicação definida); (6) explicabilidade — toda sugestão contextual mostra a evidência do projeto que a originou; (7) regra central — "estou manipulando o projeto ou preenchendo o Hydra?", e uma boa interação deve favorecer a primeira. A estratégia de implementação passa a ser um corredor contínuo HOME → entrada no projeto → jornada, na mesma ordem de uso real, sem atividades novas isoladas no meio da jornada. Jornada candidata até o ponto já explorado nos protótipos (Descoberta: Contextualizar projeto, Definir problema, Mapear grupos afetados, Entender estado atual, Definir resultado desejado; Definição do produto: Definir direção do produto — incorporando usuário principal, visão do produto e critérios de sucesso —, Escolher o próximo foco — permanece como atividade própria, decisão de escopo/priorização do usuário, não cálculo de próxima ação do sistema; Estruturação: Definir objetivo e entregáveis, Organizar quem participa — fundindo Identificar partes interessadas/Definir papéis e responsabilidades/parte de Comunicação e governança —, Identificar riscos do projeto) é direção, não compromisso de escopo ou de ciclo — cada atividade é redesenhada quando alcançada; decisões de produto relevantes que precisem sobreviver ao contexto da implementação são registradas no decision-log quando necessário (mesmo padrão de D023–D031), sem exigir uma nova entrada formal para cada tela/item. Nenhuma atividade fora deste horizonte (Execução, as demais atividades da Descoberta/Definição/Estruturação, síntese/checkpoint automático) é redesenhada por esta decisão. Responsável: Matheus. | Aprovada | O dogfooding de C5-01 (Ciclo 5) mostrou que o ganho percebido vem de operar objetos vivos do projeto entre atividades, não de reduzir digitação, e revelou granularidade excessiva na jornada atual; os quatro protótipos avaliados no Claude Design deram forma concreta e testável a essa direção antes de qualquer decisão de escopo por ciclo. Registrar a gramática aqui, como decisão, evita que ela dependa de uma conversa específica e permite que cada ciclo futuro (a partir do Ciclo 6) a referencie sem repeti-la. |
| D033 | 10/08/2026 | Reversão da identidade visual papel/tinta/grafite (D032 anterior, etapa 7.1 do roadmap) para uma nova identidade global — tema escuro, acento ciano/teal, fonte Inter, navegação lateral (sidebar) global fixa (Home/Projetos/Configurações) — a partir do artefato aprovado no Claude Design (projeto "Atividade guiada: Afetados pelo problema", arquivo `Home.dc.html`). Migração tela a tela, não simultânea: a Home (`/`) é a primeira tela convergida; as demais telas (Biblioteca, Nova iniciativa, shell interno de projeto e todas as rotas dentro dele) permanecem na identidade papel/tinta/grafite até serem migradas individualmente, uma por vez — o produto convive deliberadamente com duas identidades visuais simultâneas durante essa transição. A sidebar de navegação global é conceitualmente compartilhada (Home/Projetos/Configurações), mas foi implementada apenas dentro da rota Home nesta entrega — ainda não extraída como componente nem aplicada a outras telas. Elementos do artefato sem capacidade real no produto (busca, notificações, perfil de usuário — sem autenticação/multiusuário) foram mantidos como decoração estática, sem nenhum comportamento, por decisão explícita; "Configurações" aparece na navegação sem link, aparência inerte, por não existir Configurações global real ainda. `docs/core/UX_DESIGN_SPEC.md` não foi reconciliado nesta entrega — continua descrevendo só a identidade papel/tinta/grafite; ficará desatualizado para a Home até uma reconciliação futura (ver `PROJECT_STATUS.md`, "Pendências de documentação"). Responsável: Matheus. | Aprovada | Revisão visual direta da Home implementada mostrou que ela não correspondia à direção mais recente aprovada para o produto; a nova referência (`Home.dc.html`) já reflete essa direção atualizada. Migração tela a tela evita reescrever o produto inteiro de uma vez e mantém cada convergência revisável isoladamente, no mesmo espírito de D023–D031. |
| D034 | 10/08/2026 | Redesenho de `/projects/new` e da atividade "Problema ou oportunidade" (renomeada "Entender a situação"), a partir de duas telas do Claude Design (projeto "Redesenho da tela /new": `Novo Projeto.dc.html`, `Entender a Situacao.dc.html`). `/projects/new` vira tela única (nome + nova taxonomia de origem de 7 opções, `catalog/discovery.ts` `ORIGIN_OPTIONS`) sem diagnóstico de rota — o diagnóstico continua existindo em `/map` (D023/D024), só deixou de ser obrigatório na criação (`CreateConfiguredProjectInput.routeStartPhaseId` agora aceita `null`). Origem é gravada atomicamente na criação como a Answer real da atividade `origem` (mesmo campo de sempre, só capturado mais cedo). "Entender a situação" substitui o texto livre por seleção estruturada em 3 passos (o quê/onde/peso) com síntese determinística (sem IA, `catalog/situation-synthesis.ts`), implementada como componente bespoke (`EntenderSituacao.svelte`, mesmo padrão de `priorizar_entregas` em `now/+page.svelte`) — não usa o `ActivityForm` genérico. Taxonomia de "o quê" muda conforme a origem (grupo problema/oportunidade). `situacao` preservado como campo real, agora auto-preenchido com a síntese, para sustentar `suggestedSource` já existente e a leitura de Documento/Registros/Resumo. `hipotese_opt` (único campo `semanticRole: hypothesis` do catálogo) preservado, não exibido na nova UI. `orientation-engine/scope-suggestions.ts` não foi alterado — as regras antigas (`too_many_steps`/`duplicated_information`/`rework`) ficam dormentes, não remapeadas. A atividade "Contexto inicial" foi removida da jornada ativa (não desativada — deletada do catálogo); nome/origem já vêm de `/projects/new`, e os campos sem equivalente (modo de trabalho, nível de experiência, estágio atual) não foram redistribuídos, por decisão explícita — ficam para quando um sinal desse tipo for realmente necessário (tailoring). Shell do workspace (`[projectId]/+layout.svelte`) ganha tema escuro escopado (redefine os tokens `--hydra-*` já existentes), ativo só quando a atividade atual é "Entender a situação" — nenhuma outra atividade foi redesenhada. Responsável: Matheus. | Aprovada | Dogfooding do fluxo de criação mostrou descontinuidade visual e sequencial (claro→escuro→claro→escuro) causada por "Contexto inicial" sobreviver entre a criação (já convergida) e "Entender a situação" (já convergida); decisão explícita de incorporar seus elementos úteis às duas telas novas em vez de manter uma terceira tela sem referência de design aprovada. |
| D035 | 17/08/2026 | ETAPA 5 do rework ("Decidir o modelo canônico de trabalho", `docs/core/HYDRA_PRODUCT_REWORK.md` §35): `Deliverable` e `WorkItem` são conceitos semanticamente distintos, não a mesma entidade — `Deliverable` é a camada de priorização/escopo (o que entra, em que ordem, tamanho aproximado; alimenta o Roadmap, §17) e `WorkItem` é a camada de execução (unidade executável com progresso e, no futuro, `Dependency`/`AcceptanceCriterion`/`Responsible` próprios; alimenta o Kanban, §17), seguindo a cadeia conceitual já registrada em §12 (`DesiredOutcome → Deliverable → WorkItem`). `ScopeItem` histórico é o precursor/candidato de promoção para `Deliverable` — seu `bucket`/`order`/`effort`, nascidos de "Escolha o próximo foco", já capturam a semântica de prioridade/escopo que `Deliverable` precisa; a promoção preserva integralmente a semântica de `bucket` (`agora`/`depois`/`fora`), sem reinterpretar registros históricos como "entrega comprometida" — `fora` continua significando fora do recorte. `PlanningItem` histórico é o precursor semântico de `WorkItem` ("decompor o trabalho" já significa partir em unidades executáveis), mas tecnicamente imaturo (id/texto dentro de `Answer.value` do campo `partes_trabalho`, sem lifecycle, sem tabela própria, sem vínculo com nenhum `ScopeItem`/`Deliverable`) e não auto-converte: qualquer promoção de `PlanningItem` para `WorkItem` exige confirmação/associação explícita do usuário (CONFIRM-TO-CONVERT, regra 13.2 do rework), nunca migração automática. `ScopeItem.executionStatus` (D025) é tratado como compatibilidade histórica, não como definição do novo modelo operacional de `WorkItem` — a ETAPA 6 não deve continuar tratando-o como substituto canônico. Nenhuma entidade nova foi criada, nenhum schema alterado, nenhum dado migrado nesta etapa — decisão exclusivamente semântica/documental; a ETAPA 6 ("Primeiro loop operacional") introduzirá somente a menor representação real de `WorkItem` que seu loop exigir. Responsável: Matheus. | Aprovada | Auditoria de `PlanningItem`/`ScopeItem` mostrou que nenhum dos dois já é `WorkItem`: `ScopeItem` tem tabela e lifecycle, mas sua semântica histórica (`bucket`/`order` de priorização, nascida de decisão de escopo) corresponde a `Deliverable`, não a execução; `PlanningItem` tem a semântica correta ("partes do trabalho" = unidades executáveis), mas nenhuma maturidade técnica nem vínculo com `ScopeItem`. Tratar `ScopeItem.executionStatus` como `WorkItem` colapsaria duas camadas que o modelo conceitual do rework mantém deliberadamente distintas (Roadmap × Kanban, §17); auto-promover `PlanningItem` violaria a regra de promoção (§13.2), pois não há hoje confiança suficiente para inferir a qual `Deliverable` cada parte pertence. |
| D036 | 19/08/2026 | ETAPA 6 do rework ("Primeiro loop operacional", `docs/core/HYDRA_PRODUCT_REWORK.md` §36): `WorkItem` passa a existir como entidade real mínima (`id`, `projectId`, `title`, `status` — `a_fazer`/`em_andamento`/`concluido` — tabela `work_item` própria), entidade de execução distinta de `Deliverable` (D035; `Deliverable` continua não implementada). "Trabalho" (`/projects/[id]/work`) substitui semanticamente "Entregas" (`/deliveries`, removida) como a superfície de WorkItem — nenhuma segunda board coexistindo. `Impediment` ganha `workItemId` opcional (nullable, `ALTER TABLE` idempotente sobre bancos existentes, mesmo padrão de D023/D025) — impedimento no nível do projeto sem vínculo continua válido e inalterado. "Bloqueado" nunca é persistido nem é status/coluna: é sempre derivado de existir um `Impediment` aberto com `workItemId` apontando para o `WorkItem` (`hasOpenImpediment`, `domain/transitions.ts`); `moveWorkItem` recusa a transição para `concluido` enquanto essa condição for verdadeira, tanto na UI quanto no servidor (validado por teste com bypass direto da UI). Em Acompanhamento, "Precisa de você" é a única projeção acionável para impedimentos vinculados a um WorkItem — esses impedimentos são excluídos de "Atenções" e de "Gestão de impedimentos" (`tracking-view.ts`, `buildImpediments` filtra `workItemId === null`) para não duplicar o mesmo fato operacional em três superfícies concorrentes; impedimentos sem WorkItem mantêm o comportamento anterior integralmente. A ação de resolver segue um padrão de dois passos, consolidado depois de três rodadas de dogfooding humano: "Atualizar situação" nunca muta nada — abre um estado de confirmação contextual (explica que a resolução aconteceu no projeto real, que o Hydra só está sendo informado, e que o status operacional não muda); só "Confirmar que foi resolvido" dispara `resolveImpediment`; "Cancelar" sempre disponível; feedback pós-mutação explícito (WorkItem desbloqueado, estado operacional preservado, link "Continuar em Trabalho"). Fora de escopo, por decisão explícita: `Deliverable`, promoção de `PlanningItem`, `Risk`/`Decision`/`Change`/`Dependency`/`Milestone`, scheduling, event log/histórico, health score, e a divergência visual Home (escura)/Biblioteca/workspace (papel/tinta/grafite) — achado real, registrado para decisão de corte futura, não tratado aqui. Responsável: Matheus. | Aprovada | Três rodadas de dogfooding humano localizaram defeitos reais que mudaram a interação original: o card de WorkItem não comunicava ser abrível (corrigido com afordance explícita "Abrir ›"); "Resolver impedimento"/"Marcar como resolvido" pareciam mutação definitiva de um clique só (corrigido com o padrão de dois passos); um impedimento vinculado a WorkItem aparecia simultaneamente em três superfícies de Acompanhamento (corrigido filtrando a origem única); a movimentação no Kanban pareceu travada por ~2s (medição isolou a causa em overhead de recompilação de módulo do `vite dev` por requisição — ~13ms depois de aquecido —, não um custo persistente; ainda assim, feedback de pendência imediato foi adicionado por ser a resposta correta independentemente da causa). Só depois da quarta rodada (sem novos achados) o dogfood foi aprovado. |
| D037 | 20/08/2026 | ETAPA 7 do rework ("Event log incremental", `docs/core/HYDRA_PRODUCT_REWORK.md` §37): introduzido um log de eventos append-only (`project_event`, tabela nova) cobrindo só o loop WorkItem/Impediment (D036) — taxonomia fechada de quatro tipos (`work_item.created`, `work_item.status_changed`, `impediment.registered`, `impediment.status_changed`), sem taxonomia genérica, sem event sourcing/replay/CQRS/snapshot: `ProjectState` continua a única fonte de verdade, o log é histórico auxiliar, nunca reconstrói estado. Cada evento é emitido explicitamente pelo use-case correspondente (nunca por diff automático de `ProjectState`), na mesma transação SQLite do `save`/`insert` do estado — `ProjectRepository.save`/`insert` ganham um segundo parâmetro `events?: ProjectEvent[]` opcional (default `[]`), preservando toda chamada existente sem evento. Operação idempotente (mover para o mesmo status, resolver já resolvido) nunca emite evento duplicado. Projeto pré-S7 abre com histórico vazio — sem backfill: nenhum evento é sintetizado a partir de `createdAt`/`updatedAt` existentes, por não haver fato histórico confiável (só o estado final é conhecido, não a sequência real de transições). Export/import preserva eventos (`serializeProjectState`/`deserializeProjectEvents`, campo `events` aditivo no mesmo envelope `version: 1` — export anterior à S7 sem essa chave importa normalmente como histórico vazio, nenhuma mudança de formato incompatível). `/records` é a única projeção cronológica desta versão ("Atividade recente", mais recente primeiro, filtro por entidade via `?entityId=` repetível); Trabalho ("Ver histórico") e Acompanhamento ("Ver mudanças relacionadas") só navegam para lá, nunca desenham histórico próprio. Refinamento visual/interacional das três superfícies (`/records`, `/work`, `/tracking`) seguiu Design Gate aprovado e congelado via Claude Design MCP ("Design Gate - S7 Event Log"), com uma divergência resolvida sem seguir o mockup literalmente: o gate usa a cor de acento teal do design system do shell escuro para links/chip, mas as três superfícies vivem na paleta papel/tinta/grafite, que `app.css` documenta como deliberadamente sem cor de acento própria (accent == text) — a estrutura/hierarquia/mecânica do gate (chip nomeado + ×, posição de "Ver histórico", anatomia da linha, empty states) foi preservada com a paleta ink/neutra já existente, não com teal, para não migrar essas superfícies de tema (proibição explícita do próprio Design Gate). Responsável: Matheus. | Aprovada | Escopo definido a partir do contrato canônico já registrado em `HYDRA_PRODUCT_REWORK.md` §37 (usos iniciais: atividade recente, "o que mudou?", explicação de sinais, histórico de item; "não fazer": event sourcing) e do scouting prévio que descartou alternativas sem persistência própria (não distinguiam `fromStatus`/`toStatus` nem sobreviviam a resolve→reopen). Dogfood funcional (fluxo completo criar/mover WorkItem, registrar/resolver/reabrir Impediment vinculado, export/import) e dogfood visual do Design Gate aprovados explicitamente por Matheus antes do fechamento. |
| D038 | 21/08/2026 | Remediação R7–R10 ("tornar cortes pequenos baratos"; numeração continua a série R1–R6 já usada e concluída, para não colidir com aqueles identificadores). Corte R7: `project_event.type`/`entity_type` deixam de ter `CHECK` de enumeração — a validação da taxonomia passa a viver só na união fechada `ProjectEvent` (`domain/events.ts`), exaustiva e checada pelo compilador. Bancos anteriores são convertidos por rebuild da tabela (`ensureProjectEventTaxonomyOpen`, transacional). Regra derivada: `CHECK` de banco para invariante realmente fechado (ex.: `work_item.status`); sem `CHECK` para discriminante deliberadamente extensível; toda `CHECK` que permanecer no schema deve ser **nomeada**. | Aprovada | O `CHECK` original recusava tipos de evento novos em bancos já existentes e, como estado e eventos são gravados na mesma transação, o INSERT recusado derrubava a operação de domínio inteira — falha de escrita, não de log. `ALTER TABLE ... DROP CONSTRAINT` (SQLite 3.53) não alcança constraints anônimas como as originais: verificado empiricamente neste runtime antes de escolher rebuild. |
| D039 | 21/08/2026 | ETAPA 8 do rework ("Dependency + Milestone + Roadmap/Timeline", `docs/core/HYDRA_PRODUCT_REWORK.md` §38), **primeiro microcorte — somente `Dependency`**: `Dependency` passa a existir como relação dirigida `WorkItem → WorkItem` do mesmo projeto (tabela `dependency` própria: `id`, `project_id`, `work_item_id`, `depends_on_work_item_id`, `created_at`), na camada de execução, como D035 já previa ("`WorkItem` … com, no futuro, `Dependency`/`AcceptanceCriterion`/`Responsible` próprios") e como a cadeia conceitual do §12 posiciona. Semântica: **precedência/prontidão planejada** — "A depende da conclusão de B; enquanto B não estiver concluído, A está aguardando B" — e nunca "A é tecnicamente proibido de ser concluído enquanto B não concluir": `moveWorkItem` não ganha nenhuma recusa nova, `Impediment` continua sendo o único mecanismo de bloqueio operacional real (D036, invariante inalterada). `Dependency` não tem status próprio: o par (status do dependente × status do predecessor) é sempre derivado na montagem da view (`WorkItemView.dependsOn`, mesmo padrão de `blockedBy`), em três apresentações — `pronto` (predecessor concluído), `aguardando` (dependente aberto, predecessor não concluído) e `pendente` (dependente já concluído, predecessor não concluído; a precedência não satisfeita continua sendo fato visível, mas um item concluído não "aguarda" nada). A relação é imutável (só nasce e é removida, mesmo molde de `Evidence`, sem `updated_at`) e concluir o dependente nunca a remove nem a satisfaz. O grafo deve permanecer acíclico: auto-referência, par duplicado e ciclo — direto (A ↔ B) ou transitivo (A → B → C → A) — são inválidos e recusados no domínio (`addDependency`) e na desserialização, nunca só na interface; auto-referência e par duplicado também viram `CONSTRAINT` nomeada no schema (`dependency_no_self_reference`, `dependency_unique_pair`, conforme a regra de D038), enquanto ciclo transitivo, não expressável em `CHECK`, fica só no domínio. `ProjectState.dependencies` é a única fonte canônica de fatos estruturados de dependência. `dependencias_trabalho` (atividade `mapear_dependencias`) permanece READ-LEGACY: continua legível e inalterado, nunca é auto-convertido em `Dependency`, e não tem autoridade em nenhuma projeção de execução; a retirada da escrita legada e a reconciliação do catálogo ficam para o rework de Planejamento (ETAPA 9, §39), que é o corte que possui essa superfície — este microcorte não toca catálogo nem Planejamento. Fora de escopo por decisão explícita, ainda **DEFER dentro da própria S8**: a relação `ImpedimentType.dependencia_externa` ↔ `Dependency` prevista no §13.4 **não** foi reconciliada (a tag continua classificando a causa do impedimento, sem vínculo com `Dependency`), dependência externa como objeto não existe, e nenhum event type de `Dependency` foi criado (a taxonomia fechada de `ProjectEvent` continua sendo a do loop S6/S7, D037 — tipo novo exige decisão explícita de corte). `Milestone`, `Roadmap`/`Timeline` e os sinais do §38 não foram iniciados: S8 permanece em andamento. Responsável: Matheus. | Aprovada | A análise prévia descartou `Deliverable`→`Deliverable` (entidade inexistente) e `PlanningItem`→`PlanningItem` (valor dentro de `Answer.value`, sem lifecycle nem tabela, que D035 proíbe auto-promover): `WorkItem` é hoje a única unidade executável real com lifecycle e superfície própria. Dependência externa foi deixada fora porque uma espera externa que já bloqueia é exatamente um `Impediment` com tipo `dependencia_externa` — criar um segundo objeto para o mesmo fato produziria duas fontes concorrentes e fundiria dois conceitos que o §13.4 manda manter distintos. A ausência de hard block segue "orientar sem aprisionar" e evita duplicar em `Dependency` a responsabilidade que `Impediment` já exerce. A apresentação `pendente` nasceu de defeito real encontrado em dogfood humano: um `WorkItem` concluído continuava sendo exibido como "Aguardando" um predecessor aberto — afirmação falsa, já que Dependency deliberadamente não impede a conclusão. Ciclo foi tratado como invariante da própria relação de precedência ("A depois de B" + "B depois de A" não descreve ordem possível), não como antecipação de scheduling. Divergência deliberada e aceita no `hydra-living-object-verifier` (13/15): `DependencyView` e `ProjectView.dependencies` não foram criadas — a projeção consumida é `WorkItemView.dependsOn`, e uma coleção paralela no `ProjectView` seria uma segunda projeção dos mesmos fatos sem consumidor; a seção 4 do `LIVING_OBJECT_CHECKLIST.md` registra que o verifier não decide questões de relacionamento/UX. |
| D040 | 23/08/2026 | ETAPA 8 do rework ("Dependency + Milestone + Roadmap/Timeline", `docs/core/HYDRA_PRODUCT_REWORK.md` §38), **segundo microcorte — somente `Milestone`**: `Milestone` passa a existir como objeto real, checkpoint de progresso **declarado** no nível do projeto (tabela `milestone` própria: `id`, `project_id`, `title`, `status`, `reached_at`, `created_at`, `updated_at`) — um marco não é executado, é alcançado; ninguém trabalha "no marco", trabalha nos itens que o tornam verdadeiro. Distinto de `WorkItem` (unidade executável que se move no board), de `Dependency` (precedência entre dois trabalhos, D039) e de `ScopeItem` (camada de escopo). Lifecycle explícito e reversível — `aberto`/`alcancado` mais `reachedAt` —, com invariante fechada única: `aberto => reachedAt === null` e `alcancado => reachedAt !== null`; `reachMilestone`/`reopenMilestone` alteram o par atomicamente, ambos idempotentes (alcançar marco já alcançado nunca reescreve o `reachedAt` original), a invariante é recusada na desserialização e também expressa como `CONSTRAINT` nomeada no schema (`milestone_status_values`, `milestone_reached_at_matches_status`, conforme a regra de D038). Associação **N:N opcional dos dois lados** com `WorkItem` (tabela de relação própria `milestone_work_item`, imutável — só nasce e é removida, mesmo molde de `Dependency`/`Evidence` —, com `CONSTRAINT milestone_work_item_unique_pair`): significa "trabalho relacionado/contribuinte para este marco", **nunca** conjunto exaustivo de condições necessárias — marco existe sem nenhum WorkItem, WorkItem existe sem nenhum marco, o usuário associa só o que considerar relevante, o mesmo trabalho pode contribuir para mais de um marco. Consequência congelada: **o estado do marco nunca é inferido dos WorkItems** — trabalho relacionado aberto NÃO impede `reachMilestone`, concluir todos os relacionados NÃO alcança o marco, associar/desassociar nunca toca `status`, e um marco explicitamente alcançado convivendo com trabalho relacionado ainda aberto é estado legítimo (validado em dogfood e coberto por teste). `moveWorkItem` não ganha nenhuma recusa nova: marco não bloqueia trabalho, assim como trabalho não alcança marco. A contagem de relacionados concluídos (`MilestoneView.relatedConcluded`) é **projeção contextual** derivada na montagem da view (mesmo padrão de `blockedBy`/`dependsOn`), apresentada como "3 de 5 trabalhos relacionados concluídos" e **nunca** como percentual, barra ou qualquer afirmação de completion do marco; marco sem trabalho relacionado exibe "Sem trabalhos relacionados", nunca "0 de 0". Sem `plannedDate` (datas planejadas pertencem ao futuro microcorte de Timeline, §38 — sem Timeline e sem sinais, uma data seria campo sem leitor e prometeria um alerta de atraso que o Hydra não daria), sem `order` (ordenação/recortes pertencem a Roadmap, §38) e sem `description` (o título comporta a frase, mesma superfície mínima de `WorkItem`). `ProjectState.milestones` + `ProjectState.milestoneWorkItems` são a única fonte canônica de fatos estruturados de marco. `marcos_principais` (atividade `definir_marcos`) permanece READ-LEGACY, como `dependencias_trabalho` em D039: continua legível e inalterado, nunca é auto-convertido em `Milestone`, e não tem autoridade em nenhuma projeção de execução; a retirada da escrita legada fica para o rework de Planejamento (ETAPA 9, §39), que é o corte que possui essa superfície — este microcorte não toca catálogo nem Planejamento. Trabalho (`/projects/[id]/work`) é a primeira e única superfície consumidora: seção "Marcos" no nível do projeto, irmã do board e nunca uma coluna dele (um marco não se move entre estados de execução), mais associação/desassociação no painel de detalhe do WorkItem; nenhuma página nova. Nenhum event type de `Milestone` foi criado — a taxonomia fechada de `ProjectEvent` continua sendo a do loop S6/S7 (D037), e a consequência (`/records` não mostra marcos) é limitação consciente, não defeito. `Milestone`, `Roadmap`/`Timeline` e os sinais do §38 além deste corte não foram iniciados: S8 permanece em andamento. Responsável: Matheus. | Aprovada | O DEFER da reconciliação `ImpedimentType.dependencia_externa` ↔ `Dependency` (§13.4) foi reanalisado antes deste corte e mantido aberto: `Dependency` é precedência interna e a tag classifica causa externa — conjuntos disjuntos, sem dois fatos concorrentes, e o caso real (A depende de B; B bloqueado externamente) já é derivável de `dependsOn` + `blockedBy` sem campo novo. `status` declarado foi escolhido contra derivação porque derivar produziria afirmação falsa em dois casos concretos: marco sem itens ficaria "alcançado" vacuamente, e o usuário associa apenas alguns dos trabalhos que compõem o marco, tornando a soma sistematicamente otimista — além disso "o fluxo funciona de ponta a ponta" é julgamento, não soma. "Marcar como alcançado" permanece mutação direta de um clique, sem o padrão de confirmação em dois passos de D036, por decisão explícita depois do dogfood: é reversível por "Reabrir", não altera nem desbloqueia nenhum WorkItem e não produz efeito operacional oculto — ao contrário de resolver impedimento, que desbloqueia execução. Uma correção de apresentação saiu do dogfood humano: com um único marco no projeto, a formulação plural ("todos os marcos existentes") soava errada, e virou escolha pura testável (`allMilestonesLinkedHint`, `work-view.ts`). Divergência deliberada e aceita no `hydra-living-object-verifier`: `Milestone` passa 15/15, mas `MilestoneWorkItem` fica em 14/15 — `ProjectView.milestoneWorkItems` não foi criada, pela mesma razão registrada em D039 para `DependencyView`: a projeção consumida é `MilestoneView.relatedWorkItems`, e uma coleção paralela no `ProjectView` seria uma segunda projeção dos mesmos fatos sem consumidor; a seção 4 do `LIVING_OBJECT_CHECKLIST.md` registra que o verifier não decide questões de relacionamento/UX. |
| D041 | 23/08/2026 | ETAPA 8 do rework ("Dependency + Milestone + Roadmap/Timeline", `docs/core/HYDRA_PRODUCT_REWORK.md` §38), **microcorte de Timeline — `Milestone.plannedDate` + primeira "Linha do tempo"**: `Milestone` ganha `plannedDate` opcional (coluna `milestone.planned_date` nullable), o primeiro fato temporal PLANEJADO do Hydra. Semântica: **"o dia em que a equipe planeja alcançar este marco"** — intenção declarada pelo usuário, e nada além disso. Não é prazo (compromisso com consequência), duração, baseline, atraso nem scheduling: nenhum objeto lê a data para decidir coisa alguma, não existe início/fim, não existe propagação. Também é distinta de `createdAt`/`updatedAt` (fatos de sistema, escritos pelo `Clock`) e de `reachedAt` (fato consumado, instante gravado pelo `Clock`). Representação: **data civil estrita `YYYY-MM-DD`, nunca timestamp** — a semântica é dia, e um instante seria deslocado de um dia por qualquer formatação com timezone; validada por `isCivilDate` (`domain/civil-date.ts`, shape + calendário real), aplicada na transição e na desserialização, **jamais por `Date.parse`**, que aceitaria timestamp completo; a string nunca passa por `new Date(...)` em round-trip nem em formatação (a Linha do tempo formata `dd/mm/aaaa` por manipulação de string). O `CHECK` nomeado `milestone_planned_date_format` (GLOB) é honestamente só defesa de FORMATO — recusa timestamp e formato local, aceitaria `2026-02-30` —, e a validade calendárica vive em um único lugar, o domínio; nenhuma segunda implementação da regra existe no schema. `plannedDate` é **ortogonal ao lifecycle de D040**: definir, reagendar e limpar são uma única operação (`setMilestonePlannedDate`, mesmo molde de `setImpedimentNextAction`), idempotente de verdade (repetir o valor atual preserva o objeto, inclusive `updatedAt`), que NÃO muda `status`, NÃO muda `reachedAt`, não alcança nem reabre o marco, não altera `WorkItem` e não afeta `Dependency`; simetricamente, `reachMilestone`/`reopenMilestone` preservam a data. Marco alcançado com `plannedDate` futura OU passada é estado legítimo, exibido como fato declarado — o Hydra não corrige nem interpreta essa combinação. Não há histórico de reagendamento, razão de mudança, `Change` nem baseline. **Trabalho** (`/projects/[id]/work`) continua dono da edição: campo de data por marco, com rótulo VISÍVEL "Planejado para" (nunca "prazo"/"vencimento"/"data limite") e "Limpar data"; nenhuma página nova. **Acompanhamento** (`/projects/[id]/tracking`) é o primeiro consumidor temporal e recebe a seção **"Linha do tempo"** (§16), que só existe quando pelo menos um marco tem data (§17, prontidão "datas/marcos") — sem seção vazia e sem placeholder de feature futura; um único marco datado já basta. A Timeline é **lista cronológica declarada, não Gantt**: inclui apenas marcos com data, cada entrada mostra data planejada, título, status declarado e `reachedAt` quando alcançado, e a ordenação é totalmente determinística — `plannedDate` ASC, `createdAt` ASC, `id` ASC —, sem depender da estabilidade do `sort` nem da ordem incidental recebida (`MilestoneView.createdAt` foi exposta só como fato de desempate, nunca exibida). Marcos sem data continuam normalmente em Trabalho e ficam fora da projeção temporal. **Nenhuma inferência de atraso, adiantamento, variação, proximidade, folga, caminho crítico, baseline ou scheduling** — `plannedDate` e `reachedAt` nunca são comparados; `order` continua pertencendo a Roadmap. `marcos_principais` (atividade `definir_marcos`) permanece READ-LEGACY, como em D040. `data_alvo_entrega` (atividade `consolidar_plano_entrega`) **não é precursor de `Milestone.plannedDate`** e não é convertida: escopo (entrega/projeto, não marco) e granularidade diferentes, e é texto livre inclusive relativo ("dentro de um mês") — §13.2 proíbe interpretar texto livre sem confiança suficiente; este microcorte não toca catálogo nem Planejamento. Compatibilidade: bancos criados antes da coluna abrem normalmente (`ensureMilestonePlannedDateColumn`, PRAGMA + `ALTER TABLE ADD COLUMN` idempotente, sétima evolução do schema desde `0001_init.sql`), marcos existentes ficam com `plannedDate` null, snapshot exportado sem a chave importa como null, e **nenhuma data é sintetizada** — nem de `created_at`, nem de texto legado. Continuam DEFER: o sinal "marco próximo com trabalho aberto" (agora tecnicamente derivável quanto à ordenação, mas "está próximo" exige janela/threshold, que é política de produto, e "trabalho aberto" depende de `MilestoneWorkItem`, deliberadamente não exaustivo), `Roadmap` (depende de `Deliverable` + ordenação, inexistentes), scheduling/Gantt (§42), event types de `Milestone` (taxonomia fechada de `ProjectEvent`, D037) e a reconciliação `ImpedimentType.dependencia_externa` ↔ `Dependency` (§13.4, aberta desde D039). S8 permanece em andamento. Responsável: Matheus. | Aprovada | D040 não é reescrita: ela continua historicamente correta ao registrar que, sem Timeline e sem sinais, uma data seria campo sem leitor — `plannedDate` foi promovida agora, em outro microcorte, exatamente quando o leitor passou a existir, e a data nasceu junto dele na mesma entrega. Data civil venceu timestamp por defeito concreto, não por gosto: `2026-09-01T00:00:00Z` formatado em pt-BR (UTC-3) exibe 31/08 — o dia planejado erraria em toda a base brasileira. A operação única (definir/reagendar/limpar) seguiu `setImpedimentNextAction` porque os três são a mesma escrita do mesmo fato; separá-las produziria três caminhos para um campo só. A idempotência que preserva `updatedAt` foi escolhida contra "sempre reescrever": salvar de novo a mesma data não é uma mudança do marco, e marcar o objeto como atualizado sem mudança tornaria `updatedAt` inútil como fato. Acompanhamento venceu rota própria e seção em Trabalho: §16 já posiciona "Linha do tempo" ali, Trabalho é workspace de mutação e duplicaria a seção "Marcos" a poucos pixels, e uma rota própria para N marcos seria exatamente a aba vazia que §17 proíbe. Dois hardenings saíram do dogfood humano: o rótulo do campo era apenas `visually-hidden` — insuficiente para um conceito novo, e por isso virou "Planejado para" visível; e o desempate da Timeline dependia da estabilidade do `sort` mais da ordem incidental da projeção — trocado por comparação explícita em três níveis, com falsificador que embaralha a ordem de entrada. |
| D042 | 24/08/2026 | ETAPA 8 do rework ("Dependency + Milestone + Roadmap/Timeline", `docs/core/HYDRA_PRODUCT_REWORK.md` §38) está **concluída**: fechamento de boundary de etapa, sem nenhuma mudança de código, schema, dado persistido ou comportamento — decisão exclusivamente semântica/documental. Resultado macro entregue: `Dependency` + `Milestone` + **temporalidade planejada inicial** sobre o modelo operacional de `WorkItem` — precedência declarável entre itens do mesmo projeto com prontidão derivada e sem hard block (D039), impacto downstream de predecessor bloqueado explicado em Acompanhamento (`15d86b1`), marco como checkpoint declarado com lifecycle reversível e relação N:N com trabalho que nunca infere completion (D040), e `plannedDate` civil opcional consumida pela primeira "Linha do tempo" cronológica (D041). Detalhamento em `docs/08-delivery/s8-dependency-milestone-timeline.md`. Esta decisão **não altera D039–D041**: ela registra o boundary e **reclassifica** as pendências de §38 que permaneciam abertas dentro da S8, movendo-as para fora do escopo da etapa. Continuam DEFER, agora fora da S8: (a) **Roadmap** — exige `Deliverable` canônico com ordenação/priorização próprias, que não existe; `ScopeItem` não deve ser promovido implicitamente a entrega só para viabilizar a superfície (D035); a arquitetura futura de Roadmap **não** é decidida aqui. (b) Sinal **"marco próximo com trabalho aberto"** — a ordenação cronológica já existe (Linha do tempo), mas "está próximo" exige política explícita de produto, e a semântica disponível hoje não justifica inventar precisão; nenhum threshold de proximidade é decidido aqui. (c) Sinal **"item sem responsável"** — `Responsible` ainda não existe como fato canônico em nenhum objeto; o modelo de `Responsible` **não** é decidido aqui. (d) Reconciliação **`ImpedimentType.dependencia_externa` ↔ `Dependency`** (§13.4) — hoje os dois fatos são distintos (precedência interna × classificação de causa de impedimento) e o caso relevante (A depende de B; B bloqueado externamente) já é representável e explicável nas projeções atuais; nenhum formato futuro de dependência externa é decidido aqui. (e) **Escrita legada** de `dependencias_trabalho`, `marcos_principais` e `data_alvo_entrega` — permanecem READ-LEGACY, sem autoridade em nenhuma projeção de execução e sem dual-write canônico; a retirada da escrita e a reconciliação do catálogo pertencem ao rework de Planejamento (ETAPA 9, §39), dono dessa superfície. (f) **Event types** de `Dependency`/`Milestone` — não são requisito de S8; a taxonomia fechada de `ProjectEvent` (D037) só deve ser estendida quando o histórico demonstrar valor real de uso. Cada DEFER acima registra a **condição semântica** que o reabre, deliberadamente sem congelar arquitetura ainda não decidida. `docs/core/CURRENT_WORK.json` passa a apontar para `S9` com status `not_started`: fechar a S8 **não** inicia nem autoriza a ETAPA 9. Responsável: Matheus. | Aprovada | Auditoria de fechamento READ-ONLY (24/08/2026) verificou contra o repo publicado, item a item, que nenhuma pendência restante de §38 é microcorte maduro de S8: `Deliverable` e `Responsible` não existem como objetos (só como comentário de intenção em `state-types.ts`), não há superfície Roadmap, e os campos legados aparecem apenas em `lib/catalog/planning.ts` e na serialização de compatibilidade — nenhuma projeção de execução os lê. Os três sinais do §38 foram avaliados separadamente: "dependência bloqueada" foi entregue e é o único derivável honestamente hoje; "marco próximo" confunde duas coisas diferentes ("próximo cronologicamente", já resolvido pela ordenação da Linha do tempo, e "está próximo", que é janela/política); e afirmar "com trabalho aberto" a partir de `MilestoneWorkItem`, associação deliberadamente não exaustiva por D040, produziria sinal sistematicamente enganoso — um marco com trabalho real não associado apareceria como sem trabalho aberto —, o que L5 e §15 proíbem. Portanto o critério de fechamento foi satisfeito pelas três condições exigidas, e não por "já fizemos bastante", suíte verde ou checklist esgotado: o resultado macro existe no produto, não resta capacidade própria madura, e todo o restante depende semanticamente de etapas futuras — cinco das seis pendências têm §39 como etapa dona. |
| D043 | 25/08/2026 | ETAPA 9 do rework ("Estruturação e Planejamento reworkados", `docs/core/HYDRA_PRODUCT_REWORK.md` §39; Design Gate aprovado e congelado "Design Gate S9 — Do escopo ao trabalho: corredor de entrega"), **primeiro microcorte — somente `Deliverable` e sua superfície**: `Deliverable` passa a existir como entidade real (tabela `deliverable` própria: `id`, `project_id`, `title`, `bucket` — `agora`/`depois`/`fora` —, `effort` — `pequeno`/`medio`/`grande`/null —, `item_order`, `source_scope_item_id`, `created_at`, `updated_at`), fechando a lacuna que D035 já registrava: camada de **priorização/escopo**, distinta de `ScopeItem` (camada de decisão de escopo do catálogo) e de `WorkItem` (camada de execução, D036). `bucket`/`effort` usam **tipos próprios** (`DeliverableBucket`/`DeliverableEffort`), mesmo com literais hoje coincidentes com `ScopeItem` — são vocabulários de objetos diferentes e devem poder divergir sem arrastar o outro.  **CONFIRM-TO-CONVERT `ScopeItem` → `Deliverable`**: a promoção é o ÚNICO caminho de conversão e só acontece por ação explícita do usuário (`promoteScopeItemToDeliverable`, exposta apenas pela ação "Promover para entrega" em `/next-version`). Nenhum load, migration, factory, abertura de página, import ou confirmação de `ScopeVersion` promove nada — não existe chamador automático em lugar nenhum do código. Promover a mesma origem duas vezes é **recusado com erro de domínio explícito** (`deliverable_already_promoted`), nunca no-op silencioso.  `source_scope_item_id` é **proveniência única e imutável, jamais canal de sincronização**: null para entrega nativa, único quando não-null (índice único **parcial** no schema, que ignora as nativas). Não existe propagação em direção nenhuma — editar a entrega nunca reescreve o `ScopeItem`, editar o `ScopeItem` nunca altera a entrega —, portanto **não existe dual-write**. Remover o `ScopeItem` de origem NÃO remove nem altera a `Deliverable`: **proveniência órfã é estado válido**, e é exatamente por isso que a coluna deliberadamente **não** tem FK operacional para `scope_item` (uma FK, com ou sem `ON DELETE`, mudaria essa semântica). A desserialização valida a unicidade, mas deliberadamente **não** valida a existência da origem.  **Preservação na promoção**: `title` é cópia por valor de `ScopeItem.text`; `bucket` preservado exatamente (inclusive `fora`); `effort` preservado exatamente (inclusive null). O `ScopeItem` permanece intacto e `ScopeVersion.confirmedAt` permanece intacto — nenhuma transição de `Deliverable` chama `invalidateScopeConfirmation`, e `/next-version` mantém integralmente suas responsabilidades atuais sobre `ScopeItem`/`ScopeVersion`. `sourceSuggestionId` e `executionStatus` **não atravessam**: são semântica da camada de escopo (sugestões estruturadas; D025, compatibilidade histórica) sem equivalente aqui. O estado "já promovido" exibido em `/next-version` é FATO derivado de `Deliverable.sourceScopeItemId`, nunca um campo novo no `ScopeItem`.  **Prioridade relativa**: `order` só existe em `agora` (null nos demais recortes), em sequência contígua 0..N-1 mantida após inserção, movimentação e remoção. Entrega nativa criada em `agora` entra no fim (append). Na promoção, a prioridade relativa de origem sobrevive à ordem em que o usuário clica em "promover": a entrega é inserida antes da primeira entrega promovida cuja origem tenha `ScopeItem.order` maior; não havendo nenhuma, depois da última promovida; não havendo promovida alguma, no fim. Entregas nativas preservam sua posição relativa. O número escalar de `ScopeItem.order` **nunca** é copiado — são sequências independentes, e copiá-lo quebraria a contiguidade.  **Sem status nem progresso derivado**: `Deliverable` não tem `status`, `progress`, percentual, health, `description`, `responsible`, `acceptanceCriteria` nem datas, e nenhuma projeção deriva agregado sobre ela. Concluir trabalho não conclui entrega.  **Contrato futuro já congelado, ainda NÃO implementado**: 1 `Deliverable` → 0..N `WorkItem`, `WorkItem` → 0..1 `Deliverable`. Nenhuma relação com `WorkItem` existe neste corte.  **Superfície**: `/projects/[projectId]/deliverables`, rótulo de produto "Entregas", capacidade operacional na navegação ao lado de Agora/Acompanhamento. Materializa apenas a fatia lista/CRUD/reorder do Design Gate; nenhuma rota existente foi removida ou renomeada, e a rota/semântica antiga `/deliveries` (removida em D036) **não** foi ressuscitada.  **Compatibilidade**: tabela nova via `CREATE TABLE IF NOT EXISTS`, **zero backfill** — banco pré-S9 abre com coleção vazia, nenhuma linha é criada e nenhum `ScopeItem` é convertido; ao contrário de `current_treatment`/`cause_exploration` (1:1 com project, que exigiram `ensureX`), aqui não há linha a inventar. Snapshot sem `deliverables` importa como `[]`; export/import preserva entregas; campo aditivo opcional no **mesmo envelope `version: 1`**, seguindo o precedente de D037.  **DEFER explícitos dentro da própria S9** (não iniciados, cada um exigindo corte próprio): detalhe/decomposição de entrega em `WorkItem`, filtro por entrega no Quadro, `Responsible`, `AcceptanceCriterion`, realocação de Marcos para Entregas, Roadmap temporal, e os placeholders classe B do Design Gate (o produto não mostra bloco vazio, placeholder nem controle inerte até haver contrato). Nenhum event type de `Deliverable` foi criado — a taxonomia fechada de `ProjectEvent` continua a do loop S6/S7 (D037), consequência aceita: `/records` não mostra entregas. **S9 permanece em andamento.** Responsável: Matheus. | Aprovada | `Deliverable` era a única camada da cadeia conceitual do §12 ainda ausente: D035 decidiu o modelo canônico de trabalho e registrou explicitamente que `Deliverable` existiria como camada distinta, e D036 implementou `WorkItem` deixando-a para depois. CONFIRM-TO-CONVERT foi escolhido em vez de auto-conversão porque converter escopo em entrega é uma decisão de produto do usuário, não um fato derivável: auto-promover todo `ScopeItem` criaria entregas que ninguém decidiu entregar, e projetos existentes passariam a ter um plano que o usuário nunca declarou. Recusa explícita na segunda promoção (em vez de no-op) porque a segunda tentativa é um engano do usuário e precisa ser dita — silenciá-la esconderia que a entrega já existe em outro lugar. Proveniência sem FK e sem sincronização segue a mesma lógica: um `Deliverable` promovido é um objeto novo com vida própria, e amarrá-lo ao `ScopeItem` por FK ou por propagação reintroduziria as duas fontes concorrentes que o rework vem eliminando desde D035 — além de tornar impossível remover um item de escopo sem destruir uma entrega já em execução. A regra de ordem por prioridade relativa (em vez de append puro) nasceu de um risco concreto: com append, a prioridade real do escopo dependeria da ordem acidental de cliques do usuário, transformando ruído de interação em decisão de plano. Falsificadores cobertos por teste antes do dogfood, incluindo o risco específico de banco existente (fixture que rebaixa um banco válido removendo a tabela `deliverable` e o reabre: 0 entregas, escopo e `ScopeVersion` byte-idênticos), proveniência órfã sobrevivendo à remoção da origem, recusa de origem duplicada pelo próprio schema, e a ordem relativa sobrevivendo a promoções feitas na sequência 2, 0, 1. Dogfood humano aprovado por Matheus no runtime isolado (criar/editar/mover/reordenar/remover, promoção a partir de escopo existente, ausência de redigitação, `ScopeItem` antigo intacto, desktop e ~390px, console limpo). |
| D044 | 27/08/2026 | ETAPA 9 do rework ("Estruturação e Planejamento reworkados", `docs/core/HYDRA_PRODUCT_REWORK.md` §39; mesmo Design Gate de D043), **segundo microcorte — implementa o contrato `Deliverable ↔ WorkItem` que D043 já havia congelado, ainda sem tocar o restante do Gate**: `WorkItem` ganha `deliverableId` opcional (coluna `work_item.deliverable_id`, nullable, com `REFERENCES deliverable (id)` — ao contrário de `deliverable.source_scope_item_id`, que é proveniência deliberadamente sem FK, este é vínculo de pertencimento real). Cardinalidade **1 `Deliverable` → 0..N `WorkItem`, `WorkItem` → 0..1 `Deliverable`**, exatamente como congelado em D043. Associação é sempre **explícita e mutável**, nunca inferida por título, `ScopeItem`, `PlanningItem`, posição, bucket ou `Milestone`: criar um `WorkItem` já dentro de uma `Deliverable` associa no mesmo ato (`addWorkItem` ganha `deliverableId` opcional); um `WorkItem` já existente e sem entrega pode ser associado, trocado de entrega ou desassociado por ação própria (`setWorkItemDeliverable`, mesmo molde de `addImpediment`/`workItemId`), idempotente quando o valor não muda (preserva identidade do objeto, inclusive `updatedAt`). Associar, trocar ou desassociar **nunca altera** `status`, `Dependency`, `Impediment` ou `Milestone` do `WorkItem`, nem de qualquer outro objeto — só `deliverableId` (e `updatedAt` do próprio `WorkItem`) muda. **Remover a `Deliverable` preserva todo `WorkItem` associado**: nunca cascade-delete, sempre desassociação (`removeDeliverable` agora também põe `deliverableId = null` nos `WorkItem` que apontavam para ela, antes de gravar) — um `WorkItem` sem `Deliverable` continua exatamente tão legítimo quanto sempre foi (D036). **Nenhum rollup**: `Deliverable` continua sem `status`, `progress`, percentual, contagem de trabalho ou completion derivada — concluir todos os `WorkItem` associados não conclui a entrega, mesma invariante de D043. Sem evento de histórico: a taxonomia fechada de `ProjectEvent` permanece a do loop S6/S7 (D037), e associação a uma entrega não é um passo desse loop — consequência aceita, `/records` não mostra a associação. **Superfícies**: o detalhe de uma `Deliverable` é um ESTADO da própria surface Entregas (painel expansível por entrega, client-side, sem nova rota) — mostra os `WorkItem` associados com status/bloqueio/dependência já derivados, permite criar `WorkItem` ali, associar um `WorkItem` existente sem entrega e remover a associação sem apagar o `WorkItem`; Trabalho passa a tornar perceptível a `Deliverable` de origem quando existir (badge no card e no painel de detalhe, via `WorkItemView.deliverable` denormalizado), sem nova coluna, sem transformar `Deliverable` em status e sem prioridade própria de `WorkItem`. Filtro por `Deliverable` no Quadro continua **DEFER**, exatamente como D043 já registrava. **Compatibilidade**: coluna nova em tabela existente, via `ALTER TABLE` idempotente (`ensureWorkItemDeliverableIdColumn`, mesmo mecanismo de `ensureImpedimentWorkItemIdColumn`/ETAPA 6) — bancos e `WorkItem` criados antes deste corte abrem com `deliverableId` null, nenhum vínculo é sintetizado; snapshot exportado sem a chave importa como null (campo aditivo opcional no mesmo envelope `version: 1`, precedente D037/D043); a desserialização valida a integridade referencial quando não-null (a `Deliverable` referenciada precisa existir no mesmo snapshot) — diferente de `sourceScopeItemId`, aqui proveniência órfã só poderia vir de estado corrompido e é recusada, não tolerada. Demais DEFER de D043 (`Responsible`, `AcceptanceCriterion`, realocação de Marcos para Entregas, Roadmap temporal, placeholders classe B) permanecem intocados. **S9 permanece em andamento.** Responsável: Matheus. | Aprovada | O modelo real (FK + validação de domínio), em vez de repetir o desenho sem-FK de `sourceScopeItemId`, foi escolhido porque as duas relações significam coisas diferentes: proveniência é histórico de origem que pode ficar órfão sem consequência, enquanto pertencimento a uma `Deliverable` é um fato de planejamento vigente — o precedente correto é `Impediment.workItemId` (D035/D036), não `Deliverable.sourceScopeItemId`. Desassociar-em-vez-de-cascatear na remoção segue a mesma regra que já protege `ScopeItem`/`Deliverable` desde D043: apagar um objeto de planejamento não pode destruir trabalho de execução já em andamento. Detalhe como estado da mesma surface (não rota nova) seguiu instrução explícita do handoff e evita duplicar Entregas como uma segunda Kanban — mover status continua responsabilidade exclusiva de Trabalho. Falsificadores cobertos por teste antes do dogfood: banco existente sem a coluna abre com `WorkItem` desassociados (fixture que remove a coluna e reabre), criação/associação/desassociação/troca persistem sem duplicar pertencimento, remover `Deliverable` preserva `WorkItem` com `status`/`Impediment`/`Dependency`/`Milestone` intocados, remover `WorkItem` não afeta `Deliverable`, round-trip de export/import preserva a associação, e snapshot com `deliverableId` órfão é recusado. Dogfood humano aprovado por Matheus no runtime real (abrir uma entrega, criar trabalho dentro dela, ver o mesmo item em Trabalho, associar um item existente, desassociar sem apagar, confirmar fatos intocados, remover a entrega e ver o trabalho sobreviver, desktop e ~390px, console limpo). |
| D045 | 28/08/2026 | ETAPA 9 do rework ("Estruturação e Planejamento reworkados", `docs/core/HYDRA_PRODUCT_REWORK.md` §39), **terceiro microcorte — reconciliação da decomposição/priorização legada**: `PlanningItem`/`partes_trabalho` (atividade `decompor_trabalho`, C5-01) passam a **READ-LEGACY** (§13.2) — continuam legíveis em `Answer`, Agora e Registros, e preservados integralmente em export/import; **sem nenhuma autoridade operacional**, sem conversão automática, sem migração, sem promoção em lote e sem dual-write. `decompor_trabalho` e `priorizar_entregas` deixam de ser `required_fields`/fonte de dados e passam a **intervenções/checkpoints `explicit_confirmation` absorvidos pelas surfaces canônicas**: nova decomposição vive em `WorkItem` (Entregas/Trabalho); nova priorização vive em `Deliverable.bucket`/`order` (Entregas). `confirmDecomposition` (nova, `domain/transitions.ts`) confirma "Decompor o trabalho" **somente quando existe ao menos um `WorkItem` real** (erro `decomposition_no_work_items` caso contrário); `confirmPlanningPriority` (reescrita) confirma "Priorizar entregas" **somente quando existe ao menos uma `Deliverable` real** (erro `priorization_no_deliverables` caso contrário) — nenhuma das duas cria, converte, reordena ou modifica `WorkItem`/`Deliverable`/`PlanningItem`; a confirmação altera exclusivamente `ActivityProgress` (e resolve o `PendingItem`, quando pulada). `allowsSkip` permanece `true` nas duas: pular continua legítimo, e a pendência criada é resolvida pelo mecanismo já existente ao retomar e confirmar depois que o estado canônico existir (mesmo padrão de D036/D040). `answerActivity` recusa escrita em `decompor_trabalho` com `wrong_completion_mode` — a recusa de nova escrita em `partes_trabalho` é do domínio, não só da interface. `now/+page.svelte` mostra o legado (quando existe) somente leitura e o botão de confirmação só quando o objeto canônico correspondente já existe; `records-view.ts` preserva a exibição do legado fora do loop genérico (que não alcança mais `decompor_trabalho`, agora `explicit_confirmation`), sem link de edição. CONFIRM-TO-CONVERT de `PlanningItem` legado para `WorkItem` **continua DEFER**, fora deste corte. **S9 permanece em andamento.** Responsável: Matheus. | Aprovada | Manter as duas Activities como `required_fields` depois que `WorkItem` (D036) e `Deliverable` (D043) passaram a existir como objetos reais perpetuaria a segunda fonte de verdade concorrente que §13.2 proíbe — `PlanningItem` era precursor semântico de `WorkItem` (D035), nunca deveria continuar sendo caminho de escrita depois que o objeto real existe. `explicit_confirmation` contra existência real (WorkItem/Deliverable) foi escolhido por já ser padrão comprovado do próprio catálogo (Resumo, Mapa de Impacto, Como é tratado hoje), evitando qualquer mecanismo novo de completion. Primeira versão deste corte cometeu exatamente o erro que a reconciliação deveria evitar — confirmação ainda dependente da existência de PlanningItem legado, e as duas Activities sem caminho honesto de conclusão para projeto novo — corrigido antes do dogfood ser aprovado. Falsificado por dogfood real (runtime) e testes automatizados: sem WorkItem/Deliverable real a confirmação é recusada; skip → pendência → criar objeto canônico na surface real → retomar → confirmar resolve a pendência; projeto legado com `partes_trabalho` preenchido permanece intacto, somente leitura, sem ganhar autoridade só por existir; `answerActivity` recusa escrita nova em `decompor_trabalho`. |
| D046 | 04/09/2026 | ETAPA 9 do rework ("Estruturação e Planejamento reworkados", `docs/core/HYDRA_PRODUCT_REWORK.md` §39), **quarto microcorte — reconciliação de dependências legadas**: `dependencias_trabalho` (atividade `mapear_dependencias`) passa a **READ-LEGACY** (§13.2) — continua legível em `Answer`, Agora e Registros, e preservado integralmente em export/import; **sem nenhuma autoridade operacional**, sem conversão automática, sem migração e sem dual-write. `mapear_dependencias` deixa de ser `required_fields`/fonte de dados e passa a **checkpoint `explicit_confirmation`** absorvido pela camada canônica já operada em Trabalho: `Dependency` (D039) é a única fonte operacional de dependência real. Ao contrário de D045 (`decompor_trabalho`/`priorizar_entregas`, que exigem ao menos um objeto canônico para confirmar), aqui **ZERO `Dependency` é resultado válido**: confirmar significa "revisei as dependências reais do projeto e o estado canônico atual está correto", nunca "existe pelo menos uma relação" — 0..N `Dependency` são igualmente confirmáveis. `confirmDependencyMapping` (nova, `domain/transitions.ts`) nunca recusa por ausência de `Dependency` e nunca cria, remove ou altera nenhuma `Dependency`; a confirmação altera exclusivamente `ActivityProgress` (e resolve o `PendingItem`, quando pulada). `allowsSkip` permanece `true`: pular continua legítimo, e a pendência criada é resolvida pelo mecanismo já existente ao retomar e confirmar (mesmo padrão de D036/D040/D045), inclusive quando o estado canônico correto for zero `Dependency`. `answerActivity` recusa escrita em `mapear_dependencias` com `wrong_completion_mode` — a recusa de nova escrita em `dependencias_trabalho` é do domínio, não só da interface. `now/+page.svelte` mostra o legado (quando existe) somente leitura, explica que a gestão real acontece em Trabalho, oferece link direto e torna perceptível a contagem real de `Dependency` (derivada de `WorkItemView.dependsOn`, sem projeção nova); `records-view.ts` preserva a exibição do legado fora do loop genérico, sem link de edição. Criação/remoção real de `Dependency` continua exclusivamente na capacidade canônica já existente em Trabalho (D039). **S9 permanece em andamento.** Responsável: Matheus. | Aprovada | A divergência deliberada de D045 (que exige um objeto canônico mínimo para confirmar) reflete que dependência é uma relação opcional entre trabalhos: "nenhuma dependência" é um fato tão legítimo quanto "existem N dependências", e exigir ao menos uma reproduziria a mesma distorção que D040 já evitou para `Milestone` (marco sem trabalho relacionado não é estado inválido). Manter `dependencias_trabalho` como caminho de escrita depois que `Dependency` (D039) existe como objeto real perpetuaria a segunda fonte de verdade concorrente que §13.2 proíbe. Falsificado por dogfood real — import/deserialização de ponta a ponta pelo endpoint real (`/?/import`), não inserção direta em banco — e por testes automatizados: confirmação com zero `Dependency` conclui a atividade; confirmação com uma `Dependency` real criada em Trabalho conclui sem alterá-la; skip → pendência → revisar/criar em Trabalho → retomar → confirmar resolve a pendência mesmo com zero relações; snapshot legado com `dependencias_trabalho` preenchido permanece intacto, somente leitura, sem ganhar autoridade e sem ser auto-convertido, tanto em Registros quanto num export subsequente; `answerActivity` recusa escrita nova em `mapear_dependencias`. Durante este corte foi corrigido, no código e em teste de regressão, um defeito de compatibilidade já coberto por D045 (`decompor_trabalho::partes_trabalho` nunca havia sido registrado como campo legado em `domain/legacy-answers.ts`, o que recusava a importação de snapshots anteriores a D045 contendo essa Answer) — cumprimento tardio do contrato já publicado em D045, não uma decisão nova, e D045 não foi reescrita. |
| D047 | 07/09/2026 | ETAPA 9 do rework ("Estruturação e Planejamento reworkados", `docs/core/HYDRA_PRODUCT_REWORK.md` §39), **quinto microcorte — reconciliação de marcos legados**: `marcos_principais` (atividade `definir_marcos`) passa a **READ-LEGACY** (§13.2) — continua legível em `Answer`, Agora e Registros, e preservado integralmente em export/import; **sem nenhuma autoridade operacional**, sem conversão automática, sem migração e sem dual-write. `definir_marcos` deixa de ser `required_fields`/fonte de dados e passa a **checkpoint `explicit_confirmation`** absorvido pela camada canônica já operada em Trabalho: `Milestone` (D040/D041) é a única fonte operacional de marco real — nenhum invariante ou lifecycle daquelas decisões foi reaberto ou alterado. Mesmo molde de D046 (`mapear_dependencias`/`Dependency`): **ZERO `Milestone` é resultado válido**: confirmar significa "revisei os marcos reais do projeto e o estado canônico atual está correto", nunca "existe pelo menos um marco" — 0..N `Milestone` são igualmente confirmáveis. `confirmMilestoneReview` (nova, `domain/transitions.ts`) nunca recusa por ausência de `Milestone` e nunca cria, remove, edita, alcança, reabre, associa ou agenda nenhum `Milestone`; a confirmação altera exclusivamente `ActivityProgress` (e resolve o `PendingItem`, quando pulada). `allowsSkip` permanece `true`: pular continua legítimo, e a pendência criada é resolvida pelo mecanismo já existente ao retomar e confirmar (mesmo padrão de D036/D040/D045/D046), inclusive quando o estado canônico correto for zero `Milestone`. `answerActivity` recusa escrita em `definir_marcos` com `wrong_completion_mode` — a recusa de nova escrita em `marcos_principais` é do domínio, não só da interface. `now/+page.svelte` mostra o legado (quando existe) somente leitura, explica que a gestão real acontece em Trabalho, oferece link direto e torna perceptível o estado real de `Milestone` (contagem total e alcançados, derivada de `ProjectView.milestones`, sem projeção nova); `records-view.ts` preserva a exibição do legado fora do loop genérico, sem link de edição. Criação/edição/exclusão real de `Milestone` continua exclusivamente na capacidade canônica já existente em Trabalho (D040/D041). O helper de jornadas Playwright `e2e/helpers/generic-activity.ts` ganhou o mesmo reconhecimento por affordance já usado para "Mapear dependências" (botão "Confirmar revisão dos marcos"), sem o qual a suíte `full` quebrava ao tentar preencher um formulário que não existe mais nesta atividade. Realocação de Marcos para Entregas permanece **DEFER** (D042). **S9 permanece em andamento.** Responsável: Matheus. | Aprovada | Mesma divergência deliberada de D045 já registrada em D046 se aplica aqui: marco é um checkpoint opcional, não uma obrigação estrutural — "nenhum marco definido ainda" é um fato tão legítimo quanto "existem N marcos", e exigir ao menos um reproduziria a distorção que D040 já evitou (marco sem trabalho relacionado não é estado inválido; a mesma lógica vale para "projeto sem nenhum marco declarado"). Manter `marcos_principais` como caminho de escrita depois que `Milestone` (D040/D041) existe como objeto real perpetuaria a segunda fonte de verdade concorrente que §13.2 proíbe. Falsificado por `hydra-verify full` (889 testes unitários, 22 jornadas Playwright, `npm run check`) e por dogfood real num build de produção isolado (banco temporário, servidor efêmero): confirmação com zero `Milestone` conclui a atividade; confirmação com um `Milestone` real criado em Trabalho conclui sem alterar status/reachedAt/plannedDate/relações; skip → pendência → revisar/criar em Trabalho → retomar → confirmar resolve a pendência mesmo com zero marcos; snapshot legado com `marcos_principais` preenchido, importado pelo endpoint real (`/?/import`), permanece intacto, somente leitura, sem ganhar autoridade e sem criar nenhum `Milestone` adicional, tanto em Registros quanto num export subsequente; `answerActivity` recusa escrita nova em `definir_marcos`; sem overflow horizontal em ~390px; console sem erros novos. |
| D048 | 09/09/2026 | ETAPA 9 do rework ("Estruturação e Planejamento reworkados", `docs/core/HYDRA_PRODUCT_REWORK.md` §39) está **concluída**: fechamento de boundary de etapa, sem nenhuma mudança de código, schema, dado persistido ou comportamento — decisão exclusivamente semântica/documental, mesmo papel que D042 teve no fechamento da S8. Resultado macro entregue: `Deliverable` como objeto real com `bucket`/`effort`/`order` (D043); Entregas absorvendo Roadmap mínimo/priorização por bucket e ordem relativa (D043); vínculo explícito e mutável `Deliverable ↔ WorkItem`, 1:0..N, sem rollup nem cascade-delete (D044); decomposição/priorização legadas (`partes_trabalho`, `decompor_trabalho`/`priorizar_entregas`) reconciliadas para READ-LEGACY com confirmação condicionada à existência de objeto canônico (D045); dependências legadas (`dependencias_trabalho`/`mapear_dependencias`) reconciliadas para READ-LEGACY com confirmação aceitando zero `Dependency` (D046); marcos legados (`marcos_principais`/`definir_marcos`) reconciliados para READ-LEGACY com confirmação aceitando zero `Milestone` (D047); Quadro filtrando `WorkItem` por `Deliverable` (`450a076`). Esta decisão **não altera** D043–D047: ela registra o boundary e **reclassifica**, sem resolver, os residuais que permaneciam abertos dentro da S9, movendo-os para fora do escopo da etapa, cada um DEFER até a condição semântica indicada: (a) **`estimar_esforco_capacidade`/Capacity** — DEFER até existir decisão sobre `Capacity`/`Responsible`; `Deliverable.effort` não é substituto de capacidade. (b) **`criterios_aceitacao_entrega`/`AcceptanceCriterion`** — DEFER até resolver onde `AcceptanceCriterion` pende (`WorkItem` vs `Deliverable`). (c) **`consolidar_plano_entrega`/`data_alvo_entrega`** — DEFER até capacidade e aceitação estarem resolvidas e existir decisão explícita sobre datas em `Deliverable`. (d) **`Responsible`/pessoas/responsabilidades** — DEFER até distinguir responsabilidade de projeto, `Deliverable` e `WorkItem`. (e) **restrições/premissas/governança** — DEFER até existir decisão explícita de promoção/objeto. (f) **realocação de Marcos para `Deliverable`** — DEFER até cardinalidade/semântica serem decididas (aberto desde D042). (g) **Roadmap temporal** — DEFER; o Roadmap mínimo já está absorvido por Entregas (D043). **`Risk` não é residual da S9**: pertence à ETAPA 10 (§40), e não é tratado nem antecipado aqui. Nenhum desses conflitos é resolvido nesta decisão, nenhum contrato novo é criado, e nenhuma `Answer` legada restante é transformada em objeto por inferência. `docs/core/CURRENT_WORK.json` passa a apontar para `S10` ("Risk como objeto vivo", §40) com status `in_progress`: fechar a S9 inicia o ponteiro operacional da ETAPA 10, mas não implementa, planeja em detalhe nem antecipa nenhuma decisão de `Risk` — isso é trabalho de uma passagem futura. Responsável: Matheus. | Aprovada | Auditoria final de boundary (09/09/2026) confirmou, item a item contra o estado publicado em `450a076`, que não resta nenhum residual maduro e desbloqueado dentro da S9: os sete pontos listados dependem de decisão semântica ainda não tomada (capacidade, aceitação, responsabilidade, restrições/premissas/governança, cardinalidade Marco↔Entrega, datas) e nenhum deles tem contrato canônico suficiente para virar corte agora sem inventar semântica. Exceção consciente à regra genérica de decision-log-só-para-Nível-3: este pacote é Nível 1 (só documentação/ponteiro), mas D048 registra uma decisão durável de boundary e reclassificação de residuais — o mesmo papel que D042 exerceu no fechamento da S8 — e por isso fica registrada aqui, não só no backlog do ciclo. |
| D049 | 09/09/2026 | ETAPA 10 do rework ("Risk como objeto vivo", `docs/core/HYDRA_PRODUCT_REWORK.md` §40), **primeiro microcorte**: `Risk` passa a existir como objeto canônico, **estritamente em nível de projeto** — sem vínculo obrigatório ou automático com `WorkItem`, `Deliverable`, `Milestone`, `Impediment`, `Issue` ou pessoa/responsável nesta fatia. Cada `Risk` tem identidade própria, uma **declaração textual obrigatória**, e lifecycle declarado `aberto \| encerrado` com `closedAt` sempre coerente ao par (null quando aberto, preenchido quando encerrado) — igual invariante fechada de `Milestone` (D040), com `status` como única autoridade, nunca inferido. Operações manuais: criar (nasce aberto), editar a declaração (nunca altera lifecycle), encerrar e reabrir (ambas idempotentes, nunca reescrevem `closedAt` original) — sem delete nesta primeira fatia. `encerrado` significa apenas "não está mais sendo acompanhado como risco ativo", nunca que o risco ocorreu, foi mitigado, aceito ou virou impedimento. Operado inteiramente em Acompanhamento (`/tracking`, nova seção "Riscos"), **nunca** promovido automaticamente a "Precisa de você" nem a "Atenções" — sem avaliação/urgência estruturada, isso fingiria acionabilidade que o modelo ainda não conhece. As três capturas legadas de texto livre (`riscos_identificados`/`resposta_inicial_riscos` em `riscos_projeto`; `riscos_atualizados` em `atualizar_riscos`) passam a **READ-LEGACY** (§13.2) — continuam legíveis em `Answer`, Agora e Registros, preservadas integralmente em export/import, **sem nenhuma autoridade operacional, parsing, migração automática, dual-write ou associação implícita com nenhum `Risk` individual**. `riscos_projeto` e `atualizar_riscos` deixam de ser `required_fields`/fonte de escrita e passam a **checkpoints `explicit_confirmation`** contra a mesma coleção canônica de `Risk` — mesmo molde de D046/D047: **ZERO `Risk` é resultado válido**, confirmar significa "revisei o estado real dos riscos do projeto", nunca "existe pelo menos um"; `confirmRiskIdentification`/`confirmRiskUpdate` (novas, `domain/transitions.ts`) nunca recusam por ausência de `Risk` e nunca criam, editam, encerram ou reabrem nenhum. `answerActivity` recusa escrita nova nas duas atividades com `wrong_completion_mode` — mecanismo de domínio já existente, sem código novo. Tabela `risk` nova via `CREATE TABLE IF NOT EXISTS` no mecanismo aditivo já existente (sem migration machinery nova): bancos existentes abrem com a coleção vazia, sem backfill. **DEFER, não decidido nesta fatia**: probabilidade/impacto/severidade/score, resposta estruturada, owner/`Responsible`/`Participant`, histórico/notas de revisão, novos tipos de evento, `Risk → Impediment`/`Issue`, relações com `WorkItem`/`Deliverable`/`Milestone`, razão de encerramento, premortem/sugestões, IA. Este microcorte não completa a ETAPA 10 nem fecha S10. Responsável: Matheus. | Aprovada | Molde de `Milestone`/D040 (lifecycle declarado, invariante fechada `status`↔`closedAt`) e de D046/D047 (conversão READ-LEGACY + `explicit_confirmation` com zero-é-válido) reaproveitado integralmente por já ser padrão comprovado do catálogo, evitando qualquer mecanismo novo de completion ou de persistência. Vínculo automático com outros objetos, avaliação estruturada e materialização em Issue foram deliberadamente adiados: nenhum desses tem contrato canônico suficiente ainda, e antecipá-los inventaria semântica não pedida por este corte. Falsificado por 8 testes automatizados novos (lifecycle idempotente, edição não altera status/closedAt, zero `Risk` válido nos dois checkpoints, snapshot anterior ao corte sem a chave `risks` importa como coleção vazia, snapshot legado com os três campos preservado sem conversão) e por dogfood real (runtime, banco de desenvolvimento): criar dois riscos, editar um, encerrar e reabrir, reload preserva estado, ambos os checkpoints confirmam com zero `Risk` sem mutar a coleção, risco aberto não aparece em "Precisa de você"/Atenções, console limpo, sem overflow em ~390px. |
| D050 | 09/09/2026 | ETAPA 10 do rework ("Risk como objeto vivo", `docs/core/HYDRA_PRODUCT_REWORK.md` §40), **segundo microcorte**: `Risk` ganha `reviewedAt: string \| null` — o instante da última revisão confirmada, fato individual sem histórico, sem contagem, sem nota e sem usuário/responsável da revisão. Nasce `null` na criação (identificação inicial não é revisão). Nova transição `reviewRisk` (`domain/transitions.ts`) confirma revisão sem exigir nenhuma outra mudança — o caso comum "revisei, e continua igual" — e sempre aceita se o `Risk` existe (cada chamada real avança `reviewedAt`, não é idempotência de estado). `editRiskStatement`, `closeRisk` e `reopenRisk` já provam reconsideração real do risco e também passam a atualizar `reviewedAt` quando causam mudança de fato (declaração de fato alterada; lifecycle de fato transicionado) — os no-ops já existentes de cada uma (mesma declaração; já encerrado; já aberto) continuam retornando o estado inalterado por referência e **não** avançam `reviewedAt`, exatamente como já preservavam `closedAt`. `confirmRiskIdentification`/`confirmRiskUpdate` continuam sem tocar a coleção `risks` (D049): checkpoint de catálogo nunca é revisão em lote de nenhum `Risk` individual. Sem periodicidade, prazo, "atrasado" ou qualquer noção de SLA: ausência de revisão (`reviewedAt: null`, inclusive indefinidamente) não cria pendência nem sinal, e `Risk` continua fora de "Precisa de você"/Atenções — `reviewedAt` é fato factual ("quando alguém olhou por último"), nunca urgência computada. Acompanhamento exibe a última revisão como data absoluta (`Última revisão: DD/MM/AAAA`, mesmo tratamento de `Milestone.reachedAt`) ou "Sem revisão registrada"; nunca "há N dias"/stale/badge/cor de alerta/ranking. Persistência: coluna `reviewed_at` nullable na tabela `risk` — bancos criados desde D049 (sem a coluna) recebem-na via `ALTER TABLE` idempotente (`ensureRiskReviewedAtColumn`, mesmo padrão de `ensureWorkItemDeliverableIdColumn`/D043), sem backfill; `Risk`s já persistidos ficam com `reviewedAt: null`, nunca sintetizado de `createdAt`/`updatedAt`/`closedAt`. Snapshot exportado antes deste corte importa `reviewedAt` ausente como `null`. **DEFER, não decidido nesta fatia**: histórico/notas/eventos de revisão, cadence/SLA/stale, avaliação/probabilidade/impacto/severidade/score, resposta estruturada, owner/`Responsible`/`Participant`, `Risk → Issue`/`Impediment`, relações com outros objetos, premortem/sugestões, IA. Este microcorte não completa a ETAPA 10 nem fecha S10. Responsável: Matheus. | Aprovada | Monitoramento/revisão foi escolhido, entre as capacidades ainda abertas de §40 (avaliação, resposta, monitoramento, owner, materialização), por ser a única madura sem decisão de produto adicional: reaproveita integralmente o lifecycle já congelado por D049 (mesmo padrão de fato declarado, não inferido) sem exigir nenhuma escala inventada (não há precedente canônico de probabilidade/impacto no catálogo), nenhuma entidade ainda inexistente (`Responsible`/`Issue` continuam não implementados em lugar nenhum do domínio), e sem transformar Acompanhamento em ficha de cadastro (um único fato — quando foi revisado — não um formulário). Mesmo espírito do sinal estreito e explicável já usado para `TrackingBlockedWorkItem` (Bloqueios, ETAPA 6): fato narrado, nunca score. Falsificado por testes automatizados novos em `domain/serialization.spec.ts` (criação nasce `reviewedAt: null`; `reviewRisk` não altera statement/status/closedAt; edição/encerramento/reabertura materiais atualizam `reviewedAt`; os mesmos três no-ops preservam `reviewedAt` por referência; checkpoints nunca alteram `reviewedAt`; snapshot sem `reviewedAt` importa como `null`) e em `server/persistence/sqlite-project-repository.spec.ts` (round-trip preserva `reviewedAt`; banco pós-D049 sem a coluna `reviewed_at` abre, ganha a coluna de forma idempotente, e `Risk`s existentes ficam com `reviewedAt: null` sem perda de nenhum outro dado) — e por dogfood real em servidor e banco isolados (fora do repositório): criar risco mostra "Sem revisão registrada"; "Revisar" grava e persiste após reload sem alterar mais nada; editar a declaração e encerrar/reabrir também avançam a revisão; risco revisado ou não continua fora de "Precisa de você"/Atenções; checkpoint "Atualizar riscos" não altera `reviewedAt` de nenhum `Risk`; console limpo; sem overflow em ~390px. |
| D051 | 11/09/2026 | ETAPA 10 do rework ("Risk como objeto vivo", `docs/core/HYDRA_PRODUCT_REWORK.md` §40), **terceiro microcorte**: `Risk` ganha avaliação qualitativa declarada (`likelihood: 'baixa' \| 'media' \| 'alta' \| null`, `impact: 'baixo' \| 'medio' \| 'alto' \| null`) e resposta planejada textual (`response: string \| null`), ambas opcionais e pertencentes ao `Risk` individual. Avaliação ausente é **sempre** o par `likelihood: null, impact: null`; quando presente, o par é **sempre** completo — nunca um valor sozinho, invariante fechada garantida por `setRiskAssessment` (`domain/transitions.ts`) e reforçada na desserialização (`validateInvariants`), mesmo molde da invariante `status`↔`closedAt` de D049. Sem score, produto probabilidade×impacto, prioridade, health, ranking, matriz ou porcentagem — só os dois literais declarados, nunca calculados nem inferidos. `response` é o mesmo tipo de fato declarado que `statement` (mesmo molde de `setImpedimentNextAction`): nunca cria `WorkItem`, `Impediment` ou `Issue`, e nunca altera `status`/`closedAt`. `setRiskAssessment`/`setRiskResponse` (novas, `domain/transitions.ts`) seguem o padrão já congelado por D050: uma mudança material (par ou texto efetivamente diferente, incluindo limpar) atualiza `reviewedAt`/`updatedAt`; salvar exatamente o mesmo valor é no-op e preserva ambos por referência. `editRiskStatement`/`closeRisk`/`reopenRisk` continuam preservando avaliação/resposta integralmente (spread do item existente) — lifecycle permanece ortogonal à avaliação/resposta, exatamente como já era ortogonal a `reviewedAt`. Acompanhamento (`/tracking`) exibe declaração, avaliação atual (ou "Sem avaliação registrada"), resposta planejada (ou "Nenhuma resposta registrada") e última revisão dentro da mesma seção "Riscos" já existente, sem novo formulário fixo de fase; `Risk` continua fora de "Precisa de você"/Atenções — nenhuma regra de atenção automática, threshold ou promoção foi introduzida. Persistência: três colunas novas (`likelihood`, `impact`, `response`, todas `TEXT` nullable) na tabela `risk`, com três `CHECK` **nomeadas** nesta CREATE TABLE (`risk_likelihood_values`, `risk_impact_values`, `risk_assessment_pair`, mesmo padrão nomeado de D038/D049) — mas essa proteção de banco só cobre uma tabela criada do zero por este corte; um banco já existente (pós-D049/D050) recebe as três colunas só via `ALTER TABLE ADD COLUMN` idempotente (`ensureRiskAssessmentAndResponseColumns`, mesmo padrão de `ensureRiskReviewedAtColumn`), sem CHECK nenhuma sobre elas — SQLite não permite anexar CHECK a uma tabela existente por `ALTER TABLE`, e reconstruir a tabela só para simetria cosmética não foi feito. A invariante do par é garantida **sempre** pelo domínio (`setRiskAssessment`, `validateInvariants`), e pelo SQLite **apenas** em bancos novos — falsificado explicitamente por teste dedicado. Bancos pré-existentes abrem sem perda, `Risk`s já persistidos (lifecycle e `reviewedAt` reais inclusive) ficam com os três campos novos `null`, sem nenhum backfill nem síntese a partir de Answer legada. As três capturas legadas (`riscos_identificados`/`resposta_inicial_riscos`/`riscos_atualizados`) permanecem **READ-LEGACY** exatamente como D049 decidiu — `confirmRiskIdentification`/`confirmRiskUpdate` não foram tocados neste corte e continuam sem ler, escrever ou popular nenhum dos três campos novos. Snapshot exportado antes deste corte importa os três campos ausentes como `null`. Este microcorte completa as duas capacidades incondicionais restantes da primeira versão de `Risk` (avaliar, resposta) descritas em §40, mas **não fecha S10** — owner (condicional a `Responsible`/`Participant`, ainda inexistentes) e materialização em `Issue` (condicional a um modelo ainda não escolhido) continuam em aberto, e o fechamento documental de S10 fica para uma auditoria de boundary separada. **DEFER, não decidido nesta fatia**: owner/`Responsible`/`Participant`; `Risk → Issue`/`Impediment`/`WorkItem`; score/matriz; atenção automática; SLA/stale; histórico/eventos de avaliação ou resposta; razão de encerramento; premortem/sugestões; IA. Responsável: Matheus. | Aprovada | Avaliação e resposta foram escolhidas, entre as capacidades ainda abertas de §40, por serem as duas incondicionais restantes da primeira versão declarada no rework — owner e materialização continuam explicitamente condicionais ("quando necessário"/"se o modelo escolhido fizer sentido"), então antecipá-las exigiria decidir semântica não pedida por este corte (entidade `Responsible`/`Participant` ainda inexistente; destino `Issue` ainda sem modelo escolhido). Vocabulário qualitativo (baixa/media/alta; baixo/medio/alto) sem score foi escolhido para não inventar precisão numérica que o catálogo nunca teve precedente de sustentar, e para não fingir acionabilidade (priorização, matriz, ranking) que nenhuma decisão de produto pediu ainda. Falsificado por 11 testes automatizados novos — 8 em `domain/serialization.spec.ts` (nasce sem avaliação/resposta; avaliação recusa par parcial; definir/alterar/limpar avaliação atualiza revisão e no-op preserva; definir/alterar/limpar resposta atualiza revisão e no-op preserva; lifecycle nunca altera nem é alterado por avaliação/resposta; round-trip; snapshot anterior sem os três campos importa como `null`; desserialização recusa avaliação parcial) e 3 em `server/persistence/sqlite-project-repository.spec.ts` (round-trip via SQLite; banco pós-D050 sem as três colunas abre, ganha-as idempotentemente, e `Risk` existente com lifecycle/`reviewedAt` reais preserva tudo sem síntese de Answer legada; falsificação explícita de que a CHECK do par protege um banco criado do zero mas não um banco upgradeado) — e por dogfood real (servidor e banco de desenvolvimento): criar risco nasce sem avaliação/resposta; avaliar (probabilidade média, impacto alto) e registrar resposta persistem e sobrevivem a reload; encerrar/reabrir preserva avaliação/resposta; risco avaliado ou não continua fora de "Precisa de você"/Atenções; console limpo; sem overflow em ~390px. |

| D052 | 11/09/2026 | ETAPA 10 do rework ("Risk como objeto vivo", `docs/core/HYDRA_PRODUCT_REWORK.md` §40) está **concluída**: fechamento de boundary de etapa, sem nenhuma mudança de código, schema, dado persistido ou comportamento — decisão exclusivamente semântica/documental, mesmo papel que D042 e D048 tiveram nos fechamentos da S8 e da S9. Resultado macro entregue: a primeira versão de `Risk` permite identificar, avaliar, registrar resposta planejada e monitorar/revisar continuamente, com lifecycle próprio (`aberto`/`encerrado`), fonte canônica única e sem transformar risco em formulário fixo de fase — registrado em D049 (objeto canônico + lifecycle + reconciliação READ-LEGACY das três capturas legadas), D050 (`reviewedAt`, revisão como fato factual sem SLA) e D051 (avaliação qualitativa `likelihood`/`impact` e `response` planejada, ambas opcionais). Os requisitos condicionais de §40 — owner "quando necessário" e materialização em `Issue` "se o modelo escolhido fizer sentido" — não impedem o fechamento: são explicitamente condicionais no próprio rework, não capacidades incondicionais pendentes. Esta decisão **não altera** D049–D051: ela registra o boundary e **reclassifica**, sem resolver, os residuais que permaneciam abertos dentro da S10, movendo-os para fora do escopo da etapa, cada um DEFER até a condição semântica indicada: (a) **owner/`Responsible`/`Participant`** — DEFER até existir necessidade real de accountability e um modelo canônico de pessoas/responsabilidade. (b) **`Risk` → `Issue`/materialização** — DEFER até existir `Issue` canônico ou decisão explícita de outro destino semântico. (c) **`Risk` → `Impediment`** — DEFER; não inferir equivalência; reabrir somente se houver caso real e contrato explícito. (d) **relações com `WorkItem`/`Deliverable`/`Milestone`** — DEFER até necessidade operacional concreta exigir essa relação. (e) **histórico/notas/event types de `Risk`** — DEFER até histórico demonstrar valor de uso real. (f) **cadence/SLA/stale/atenção automática** — DEFER até existir política de produto que explique por que um `Risk` merece atenção naquele momento. (g) **score/matriz/priorização derivada** — DEFER até necessidade comprovada; não inferir a partir de likelihood × impact. (h) **razão de encerramento** — DEFER. (i) **premortem/sugestões** — DEFER. (j) **IA** — DEFER. Nenhum desses conflitos é resolvido nesta decisão, nenhum contrato novo é criado. `docs/core/CURRENT_WORK.json` passa a apontar para `S11` ("Decision e Change", §41) com status `in_progress`: o repo já possui objetos estruturados suficientes para calcular impacto (`WorkItem`, `Deliverable`, `Dependency`, `Milestone`, `Risk`), condição que §41 exige para abrir a etapa — mas abrir o ponteiro **não** decide modelo de `Decision`, modelo de `Change`, relações, schema, superfície, promoção do legado ou como calcular impacto; isso é trabalho de uma passagem futura. Responsável: Matheus. | Aprovada | Auditoria de fechamento (11/09/2026) confirmou, contra o repo publicado em `a52ca95`, que a primeira versão de `Risk` entrega as quatro capacidades incondicionais de §40 (identificar, avaliar, responder, monitorar/revisar) e que os dois requisitos restantes do parágrafo são condicionais por texto do próprio rework, não pendências maduras sendo adiadas por conveniência — mesmo critério de boundary já aplicado em D042/D048 (resultado macro existe no produto; não resta capacidade própria madura; o restante depende de decisão semântica futura, não de "já fizemos bastante" ou suíte verde). Exceção consciente à regra genérica de decision-log-só-para-Nível-3: este pacote é Nível 1 (só documentação/ponteiro), mas D052 registra uma decisão durável de boundary e reclassificação de residuais — mesmo papel de D042/D048. |

| D053 | 11/09/2026 | ETAPA 11 do rework ("Decision e Change", `docs/core/HYDRA_PRODUCT_REWORK.md` §41), **primeiro microcorte**: `Decision` e `Change` passam a existir como objetos canônicos de nível de projeto, sem vínculo obrigatório com `WorkItem`, `Deliverable`, `Milestone`, `Risk`, `Impediment` ou pessoa/responsável nesta primeira fatia — mesmo adiamento deliberado que `Risk` fez em D049. Os dois nascem juntos porque compartilham uma única captura legada mista (`decisoes_mudancas_recentes`, atividade `decisoes_mudancas`), que misturava decisão e mudança em texto livre — promover só um dos dois obrigaria manter uma segunda fonte de verdade para o outro ou retirar a única captura existente antes de o segundo objeto existir. `Decision`: `subject` obrigatório; `options` (texto narrativo) e `dueDate` (data civil `YYYY-MM-DD`, mesmo validador `isCivilDate` de `Milestone.plannedDate`, D038) opcionais; `status: 'pendente' \| 'tomada'`, declarado, nunca inferido; invariante fechada — `pendente` exige `outcome === null && decidedAt === null`, `tomada` exige os dois preenchidos (`outcome` não vazio), garantida por `addDecision`/`decideDecision`/`editDecisionOutcome` (`domain/transitions.ts`) e reforçada na desserialização (`validateInvariants`). `subject`/`options`/`dueDate` só são editáveis (`editDecision`) enquanto `pendente` — depois de tomada ficam congelados (`editDecision` recusa com `decision_already_decided`), e a única correção possível a partir daí é o `outcome`, via `editDecisionOutcome`, que exige a decisão já `tomada`, texto não vazio, preserva `status`/`decidedAt` originais e só atualiza `updatedAt`. `decideDecision` só transiciona `pendente → tomada` uma vez — chamar de novo sobre uma decisão já tomada é recusado (`decision_already_decided`), nunca usado como caminho de edição, para não reescrever `decidedAt` silenciosamente. Sem `revertida`/`cancelada`/`reopen`/`delete`/owner textual/score/prioridade/alerta por prazo nesta fatia. `Change`: `statement` obrigatório, `impact` opcional/limpável (`setChangeImpact`), sem lifecycle e sem `delete`. `Change` **não é** `ProjectEvent` — a taxonomia de `domain/events.ts` permanece fechada (D037), cobrindo só o loop `WorkItem`/`Impediment`; nenhuma captura automática foi criada (§41 permite "quando possível", e hoje não existe sinal de código suficiente — o event log não cobre `Milestone` — para congelar essa detecção honestamente). Ambos operados em Acompanhamento (`/tracking`, seções "Decisões"/"Mudanças", mesmo molde visual/funcional de "Riscos"), fora de "Precisa de você"/Atenções — ter prazo declarado não implica nenhuma regra de urgência automática. `decisoes_mudancas_recentes` passa a **READ-LEGACY** (`domain/legacy-answers.ts`): a atividade `decisoes_mudancas` deixa de ser `required_fields` e vira `explicit_confirmation` (`confirmDecisionsAndChangesReview`, ZERO Decision + ZERO Change é resultado válido, mesmo molde de `confirmRiskIdentification`/`confirmRiskUpdate`) — checkpoint nunca cria/edita/remove Decision nem Change, só altera `ActivityProgress`/`PendingItem`; `answerActivity` recusa nova escrita no campo legado (`wrong_completion_mode`). Auditoria pós-dogfood encontrou e corrigiu um defeito real de compatibilidade: `records-view.ts` só projeta genericamente Answers de atividades `required_fields` (mesmo mecanismo que já exigiu blocos dedicados para `decompor_trabalho`/`mapear_dependencias`/`definir_marcos`/`riscos_projeto`/`atualizar_riscos` em S9/S10) — sem um bloco equivalente para `decisoes_mudancas`, um projeto anterior a S11 com a Answer legada preenchida ficaria invisível em Registros; corrigido com o mesmo mecanismo estreito dos precedentes (`editHref` sempre `null`), sem generalizar nem tocar os casos legados existentes. Persistência: tabelas `decision`/`change` novas (`0001_init.sql`), com `CHECK` **nomeada** (`decision_status_values`, `decision_outcome_matches_status`, conforme a regra de D038) cobrindo exatamente a invariante fechada acima; sem coluna/tabela para `Responsible`, relações ou Event Log. Contrato de upgrade é aditivo: bancos pré-S11 abrem normalmente via `CREATE TABLE IF NOT EXISTS` (tabelas novas, não uma coluna em tabela existente — sem necessidade de `ALTER TABLE` idempotente como os microcortes de `Risk`), tabelas surgem vazias, todo estado anterior permanece intacto, segunda abertura é idempotente, Answer legada nunca popula as tabelas novas, snapshot pré-S11 importa `decisions: []`/`changes: []`. Este microcorte **não fecha S11** — DEFER explícito: `Responsible`/`Participant`/`Stakeholder`; entidades afetadas estruturadas; qualquer relação `Decision`/`Change` ↔ outro objeto; reconciliação `Decision` ↔ `ImpedimentType.decisao_pendente` (§13.4, tag continua classificando sem vínculo); sinais derivados de `dueDate`; lifecycle de reversão/cancelamento; captura automática de `Change` a partir de manipulações; Event Log para `Decision`/`Change`; histórico/audit trail; IA. Responsável: Matheus. | Aprovada | `Decision`/`Change` foram escolhidos como primeiro corte de §41 (contra Decision-só ou Change-só) porque o único precedente do legado (`decisoes_mudancas_recentes`) já mistura os dois conceitos — cindir a captura sem promover os dois ao mesmo tempo criaria uma segunda fonte de verdade para metade do dado. Contrato de `Decision` (subject/options/dueDate/status/outcome/decidedAt, sem responsável/entidades afetadas) replica deliberadamente o primeiro microcorte de `Risk` (D049): objeto mínimo, sem as associações que exigiriam entidades ainda inexistentes (`Responsible`) ou decisões de produto não pedidas nesta fatia. Falsificado por testes automatizados focados — `domain/transitions.spec.ts` (`Decision` nasce pendente; `editDecision` recusa subject vazio e `dueDate` não civil; `editDecision` recusa alterar subject/options/dueDate de decisão já tomada; `decideDecision` exige outcome, grava status/outcome/decidedAt juntos, recusa decidir de novo; `editDecisionOutcome` corrige outcome preservando status/decidedAt/createdAt, recusa decisão pendente e outcome vazio; `Change` nasce sem impact, recusa statement vazio, edita statement/impact independentemente; `confirmDecisionsAndChangesReview` aceita zero/qualquer combinação de Decision/Change sem nunca mutá-las, aceita skip/retomada, `answerActivity` recusa escrita no legado), `domain/serialization.spec.ts` (round-trip completo; snapshot sem as chaves `decisions`/`changes` importa como `[]`; invariante recusa par pendente-com-outcome e tomada-sem-outcome; `dueDate` recusa timestamp ISO completo e mês/dia inexistentes; Answer legada preservada, nunca convertida), `server/persistence/sqlite-project-repository.spec.ts` (round-trip via SQLite incluindo Decision tomada e Change com/sem impacto; `CHECK decision_outcome_matches_status` recusa escrita direta que viole o par; banco sem as tabelas novas abre e importa coleções vazias) e `records-view.spec.ts` (regressão específica do defeito de Registros: Answer legada continua visível, somente leitura, `editHref: null`; atividade sem Answer legada não aparece) — e por dogfood real (servidor e banco de desenvolvimento, projeto "QA S10 Risk"): criar Decision pendente, registrar opções e prazo juntos (defeito de formulário dividido corrigido durante o dogfood — o form HTML original submetia `options`/`dueDate` em requisições separadas, cada uma sobrescrevendo o outro campo com `null`, porque `editDecision` sempre escreve os três campos juntos), reload preserva; marcar como tomada com resultado, resultado e `decidedAt` persistem; criar Change com impacto, editar/limpar impacto, reload preserva; projeto avançado via skip real até "Registrar decisões e mudanças" mostrou a contagem real (1 pendente/1 mudança) e o checkpoint concluiu corretamente; console limpo; sem overflow em ~390px. |
| D054 | 11/09/2026 | ETAPA 11 do rework ("Decision e Change", `docs/core/HYDRA_PRODUCT_REWORK.md` §41/§13.4), **segundo microcorte**: `Impediment` ganha `decisionId: string \| null` — vínculo opcional com a `Decision` que responde "qual decisão está pendente neste impedimento?", reconciliando `ImpedimentType.decisao_pendente` com o objeto canônico introduzido por D053, exatamente como §13.4 previa desde S6. Precedente estrutural reaproveitado: mesmo molde de `Impediment.workItemId` (ETAPA 6) — relação opcional, explícita, mutável, independente do lifecycle dos dois objetos. Cardinalidade: 1 `Impediment` → 0..1 `Decision`; 1 `Decision` → 0..N `Impediment` (schema permite; nenhuma interface desta rodada cria mais de um vínculo por chamada). `null` continua sendo o estado normal e permanentemente válido — nenhum `Impediment` existente precisa ganhar `Decision`, e nenhum vínculo é inferido de `tipo === 'decisao_pendente'` nem do texto do impedimento. Nova transição `setImpedimentDecision` (`domain/transitions.ts`) associa, troca ou desassocia (`decisionId: null`) — recusa `decision_not_found` quando a `Decision` não existe, recusa `impediment_decision_requires_pending_type` quando `tipo !== 'decisao_pendente'`, e é idempotente (reassociar ao mesmo valor é no-op, preserva `updatedAt` por referência). Uma `Decision` já `tomada` pode ser relacionada ou permanecer relacionada — o vínculo é factual ("qual decisão é essa"), não uma condição de decisão em aberto; nenhuma restrição nova foi adicionada com base em `Impediment.status` (aberto/resolvido), mesmo espírito de `workItemId`, que também nunca discriminou por status do impedimento. `setImpedimentType` (já existente, D035) passa a recusar mudar `tipo` com `impediment_type_change_blocked_by_decision` enquanto `decisionId` estiver preenchido — nunca limpa a relação silenciosamente; desassociar primeiro (via `setImpedimentDecision`) é sempre o único caminho para depois trocar o tipo. Lifecycles totalmente independentes, provado por teste dedicado: `decideDecision`/`editDecisionOutcome` nunca tocam `Impediment`; `resolveImpediment`/`reopenImpediment` nunca tocam `Decision` e preservam `decisionId` integralmente. Acompanhamento (`/tracking`) exibe e permite manipular a relação dentro do card do próprio `Impediment` já existente (sem nova página/surface/modal): quando `tipo === 'decisao_pendente'`, um select "Decisão relacionada" (pendentes e tomadas juntas) aparece no modo de edição; fora desse tipo, a associação não é oferecida; o card em modo leitura (aberto e resolvido) mostra "Decisão relacionada: `<subject>`" quando presente. Nenhum sinal/Atenção novo — ter uma `Decision` relacionada não promove o `Impediment` a "Precisa de você" além do que já valia. `ProjectView.ImpedimentView` ganha `decisionId`/`decisionSubject` (o segundo denormalizado em `project-view.ts` a partir de `state.decisions`, mesmo espírito de `blockedBy` em `buildWorkItemView`) — nenhum outro campo de `ProjectState` é exposto. Persistência: `decision_id TEXT REFERENCES decision (id)` nullable na tabela `impediment`, presente tanto no `CREATE TABLE` fresh de `0001_init.sql` quanto via `ALTER TABLE` idempotente (`ensureImpedimentDecisionIdColumn`, mesmo padrão de `ensureImpedimentWorkItemIdColumn`/D035) para bancos criados antes deste corte (inclusive os criados entre D053 e este) — sem CHECK relacionando `decision_id` a `tipo`, mesma razão de `work_item_id` nunca ter tido CHECK contra `status`: a invariante é garantida pelo domínio (`setImpedimentDecision`/`setImpedimentType`) e reforçada na desserialização (`validateInvariants`), nunca duplicada em SQL. `insert`/`save` passaram a gravar `decision` antes de `work_item`/`impediment` (FK `impediment.decision_id` checada imediatamente, `foreign_keys = ON`); `DELETE` já apagava `impediment` antes de `decision`, ordem que permanece correta sem alteração. Sem backfill, sem inferência a partir de `tipo`/texto — `Impediment`s já persistidos ficam com `decisionId: null`. Desserialização recusa `decisionId` apontando para `Decision` inexistente e `decisionId` preenchido com `tipo !== 'decisao_pendente'`; snapshot exportado antes deste corte importa `decisionId` ausente como `null`. Este microcorte **não fecha S11** — DEFER explícito: entidades afetadas de `Decision`; qualquer relação polimórfica; `Decision` ↔ `WorkItem`/`Deliverable`/`Milestone`/`Risk`; `Responsible`/`Participant`/`Stakeholder`; owner textual; resolução automática de `Impediment` ao tomar a `Decision`; criação automática de `Impediment` a partir de `Decision`; sinais/Atenções derivados; captura automática de `Change`; `ProjectEvent` novo; lifecycle adicional de `Decision`/`Change`; IA. Responsável: Matheus. | Aprovada | Reconciliar `decisao_pendente` foi escolhido como segundo microcorte de §41 (contra promover `Responsible` primeiro, ou capturar `Change` automaticamente primeiro) por ser o único caminho que fecha requisito citado explicitamente por §13.4 sem depender de modelagem ainda inexistente (`Responsible`/`Participant`, que exigiria decisão de produto não pedida nesta fatia) nem de sinal de código inexistente (o event log não cobre `Milestone`, então captura automática de `Change` continua sem base honesta, mesma conclusão de D053). Contrato replica deliberadamente `Impediment.workItemId` (D035) — mesma cardinalidade 0..1, mesma independência de lifecycle, mesma ausência de CHECK contra o campo companheiro (`status`/`tipo`) em bancos upgradeados. Falsificado por testes automatizados focados — `domain/transitions.spec.ts` (`Impediment` nasce com `decisionId: null`; associa/troca/desassocia; mesmo valor é no-op; `decision_not_found` para `Decision` inexistente; `impediment_decision_requires_pending_type` quando `tipo` não é `decisao_pendente`; `setImpedimentType` recusa mudar tipo com vínculo presente e preserva `Impediment`/`Decision` integralmente; permite depois de desassociar; permite relacionar `Decision` já `tomada`; `decideDecision`/`editDecisionOutcome` não alteram `Impediment`; `resolveImpediment`/`reopenImpediment` preservam `decisionId` e não alteram `Decision`), `domain/serialization.spec.ts` (round-trip; `decisionId` ausente importa como `null`; recusa referência a `Decision` inexistente; recusa `decisionId` preenchido com `tipo` incompatível; recusa valor que não é string/null/ausente), `server/persistence/sqlite-project-repository.spec.ts` (round-trip via SQLite; associar/trocar/desassociar persiste; resolver/reabrir preserva `decisionId` sem alterar `Decision`; banco anterior a este corte sem a coluna abre, ganha-a idempotentemente, `Impediment`s existentes ficam com `decisionId: null`, segunda abertura não falha nem duplica coluna/índice) e `server/application/project-use-cases.spec.ts`/`project-view.spec.ts` (uso de caso associa/troca/desassocia e reflete `decisionSubject`; recusa `decision_not_found`/`impediment_decision_requires_pending_type`/`impediment_type_change_blocked_by_decision`; projeção pública expõe exatamente a forma de `ImpedimentView`, sem vazar `ProjectState` bruto) — e por dogfood real (servidor e banco isolados, projeto "QA S11 Decision-Impediment"): criar `Decision` pendente e `Impediment` `decisao_pendente`, associar, reload preserva; trocar para outra `Decision`; desassociar; tentar mudar tipo com vínculo presente mostra "Remova a decisão relacionada antes de mudar o tipo deste impedimento." e não altera nada no servidor; desassociar e mudar tipo com sucesso; voltar o tipo e reassociar; tomar a `Decision` — `Impediment` permanece com o vínculo e lifecycle independente; resolver/reabrir o `Impediment` preserva o vínculo sem alterar a `Decision`; console limpo; sem overflow em ~1280px/~390px. |
| D055 | 12/09/2026 | ETAPA 11 do rework ("Decision e Change", `docs/core/HYDRA_PRODUCT_REWORK.md` §41), **terceiro microcorte**: `Decision` ganha `DecisionAffectedWorkItem` — relação N:N explícita e tipada que significa especificamente "este `WorkItem` foi afetado por esta `Decision`", primeira semântica concreta de "entidades afetadas" (§41), provada com um único tipo canônico (`WorkItem`). Precedente estrutural reaproveitado: mesmo molde de `MilestoneWorkItem` (ETAPA 8) — entidade de junção própria (`id`, `projectId`, as duas FKs, `createdAt`, sem `updatedAt`), imutável depois de criada (só nasce e é removida), par único. `MilestoneWorkItem` é tratado como precedente de MECANISMO, não de semântica — a semântica nova ("afetado pela decisão") é distinta de "trabalho relacionado/contribuinte" e não reaproveita nenhuma regra derivada daquela relação. Cardinalidade: 1 `Decision` → 0..N `WorkItem`; 1 `WorkItem` → 0..N `Decision`; zero relações é estado normal e permanentemente válido. Novas transições `linkWorkItemToDecision`/`unlinkWorkItemFromDecision` (`domain/transitions.ts`) — recusam `decision_not_found`/`work_item_not_found` para referências inexistentes e `decision_work_item_already_linked` para o par duplicado, mesmo padrão de `linkWorkItemToMilestone`/`unlinkWorkItemFromMilestone`. A relação é puramente factual e corrigível a qualquer momento, independente do lifecycle da `Decision` (`pendente`/`tomada`) — mesmo espírito de `Impediment.decisionId`/`WorkItem.deliverableId`: associar/desassociar nunca decide a `Decision`, nunca altera `outcome`/`decidedAt`/`dueDate`/`status`, nunca move o `WorkItem` nem altera seu `status`, nunca cria `Impediment`/`Change`/sinal; e no sentido inverso, `decideDecision`/`editDecisionOutcome`/`moveWorkItem` nunca tocam as relações. Deliberadamente NÃO se introduziu congelamento das relações depois de `tomada` — o conjunto de `WorkItem`s realmente afetados por uma decisão frequentemente só fica claro depois de executá-la. Acompanhamento (`/tracking`) exibe e permite manipular a relação dentro do card da própria `Decision` já existente (sem nova página/surface/modal), rotulada "Trabalhos afetados" — deliberadamente não "Entidades afetadas", porque o produto só suporta `WorkItem` nessa relação por ora; disponível tanto com a `Decision` pendente quanto tomada; o seletor de associação exclui `WorkItem`s já relacionados (nunca oferece duplicata que o domínio recusaria); projeto sem `WorkItem`s disponíveis simplesmente não mostra o seletor, sem placeholder nem estado fictício. Nenhuma superfície reversa nova em `WorkItem`/Trabalho ("quais decisões me afetam") foi criada — sem consumidor real pedido nesta fatia, seria simetria antecipada, não necessidade comprovada. Nenhum sinal/Atenção novo. `ProjectView.DecisionView` ganha `affectedWorkItems: DecisionAffectedWorkItemView[]` (`decisionAffectedWorkItemId`/`workItemId`/`title`, título denormalizado a partir de `state.workItems`, mesmo espírito de `MilestoneWorkItemView`) — nenhuma projeção genérica `affectedEntities` foi criada, e nenhum outro campo de `ProjectState` é exposto. Persistência: tabela `decision_affected_work_item` nova (`0001_init.sql`) com `decision_id`/`work_item_id` como FK reais (`REFERENCES decision (id)`/`REFERENCES work_item (id)`) e `CONSTRAINT decision_affected_work_item_unique_pair UNIQUE (decision_id, work_item_id)` **nomeada**, conforme a regra de D038 — deliberadamente SEM coluna `entity_type`/`entity_id`: uma relação polimórfica foi avaliada e rejeitada por não ter precedente real no repositório (`MilestoneWorkItem` é par tipado, não prova padrão polimórfico), por quebrar integridade referencial via FK verdadeira, e por contrariar §12 do rework ("não criar uma entidade genérica universal... para imitar o diagrama"). Pelo mesmo motivo, o nome final é `DecisionAffectedWorkItem`, não o genérico `DecisionWorkItem` — que poderia futuramente confundir "afetado pela decisão" com "trabalho necessário para decidir", "trabalho que implementa a decisão" ou "evidência da decisão". `insert`/`save` gravam `decision_affected_work_item` depois de `decision` e de `work_item` (FKs checadas imediatamente, `foreign_keys = ON`); `DELETE` apaga `decision_affected_work_item` antes de `decision` e de `work_item`. Nem `Decision` nem `WorkItem` têm operação de remoção canônica hoje — nenhuma política de cascade/guard foi inventada; um corte futuro que introduza remoção de qualquer um dos dois lados decide o destino desta relação explicitamente, quando existir. Contrato de upgrade é aditivo: bancos pós-D054 (sem esta tabela) abrem normalmente via `CREATE TABLE IF NOT EXISTS`, a tabela surge vazia, todo estado anterior permanece intacto, segunda abertura é idempotente, sem backfill nem inferência a partir de nenhum outro dado. Desserialização recusa `decisionId`/`workItemId` inexistentes e par duplicado; snapshot exportado antes deste corte importa `decisionAffectedWorkItems: []`. Este microcorte **não fecha S11** — `responsável` continua sendo requisito explícito de §41, ainda sem nenhum objeto `Responsible`/`Participant`/`Stakeholder` vivo no repositório. DEFER explícito (ainda sem caso de produto maduro nesta fatia, não excluídos permanentemente): `Decision` ↔ `Deliverable`/`Milestone`/`Risk`/`Dependency`/`Change`/`AffectedGroup`; `Responsible`/`Participant`/`Stakeholder`; captura automática de `Change`; `ProjectEvent`/histórico para esta relação; sinais/Atenções derivados; IA. EXCLUÍDOS deste canal por semântica, não por prioridade: `Impediment` — D054 já possui `Impediment.decisionId` com semântica própria ("qual decisão está pendente aqui"); introduzir `Impediment` também como "entidade afetada" criaria dois relacionamentos concorrentes e não observáveis entre o mesmo par `Decision`↔`Impediment`, sem caso de produto que justifique as duas semânticas simultâneas; `ScopeItem` — `Deliverable` é o modelo canônico da camada de priorização/escopo (§13.1 proíbe enriquecer os dois em paralelo como modelos concorrentes). Responsável: Matheus. | Aprovada | `WorkItem` foi escolhido como o único tipo-alvo deste terceiro microcorte (contra materializar `Deliverable`/`Milestone` juntos, ou não materializar nada ainda) por ser a camada de execução mais concretamente acionável por uma decisão, com leitor imediato óbvio (a própria `Decision` em Acompanhamento) e por replicar mecanismo já maduro e testado (`MilestoneWorkItem`, D039-D042) sem exigir nenhuma decisão de schema nova além do par de FKs. Relação polimórfica (`entityType`/`entityId`) e nome genérico `DecisionWorkItem` foram avaliados e rejeitados explicitamente — ver corpo da decisão. Falsificado por testes automatizados focados — `domain/transitions.spec.ts` (zero relações é estado normal; associa `WorkItem` existente; associa vários `WorkItem`s à mesma `Decision`; mesmo `WorkItem` associado a várias `Decision`s; recusa par duplicado, `Decision` inexistente e `WorkItem` inexistente; remove associação; remover relação inexistente é recusado; `Decision` pendente e tomada aceitam igualmente a associação; `decideDecision`/`editDecisionOutcome` preservam as relações; `moveWorkItem` preserva as relações e não altera a `Decision`; associar/desassociar nunca muda `status`/`outcome`/`decidedAt` da `Decision` nem `status` do `WorkItem`), `domain/serialization.spec.ts` (round-trip completo; snapshot sem a chave `decisionAffectedWorkItems` importa como `[]`; recusa referência inválida e par duplicado vindos de estado persistido), `server/persistence/sqlite-project-repository.spec.ts` (round-trip via SQLite preservando N:N nos dois sentidos; `UNIQUE` nomeada recusa escrita direta duplicada; remover persiste sem afetar `Decision`/`WorkItem`; banco anterior a este corte sem a tabela abre, importa coleção vazia e volta a aceitar escrita) e `server/application/project-use-cases.spec.ts`/`project-view.spec.ts` (uso de caso associa/reflete `affectedWorkItems` pela mesma porta que a interface usa; associa vários `WorkItem`s à mesma `Decision` e o mesmo `WorkItem` a várias `Decision`s; recusa vínculo entre projetos diferentes e duplicado; `Decision` tomada continua permitindo corrigir a associação sem alterar `outcome`/`status`; projeção pública não vaza `ProjectState` bruto) — e por dogfood real (servidor e banco isolados, projeto "dogfood-s11"): criar `Decision` pendente e dois `WorkItem`s, associar ambos, reload preserva; remover um; tomar a `Decision`; associar novamente já com a `Decision` tomada; console limpo; sem overflow em ~390px. |
| D056 | 12/09/2026 | ETAPA 11 do rework ("Decision e Change", `docs/core/HYDRA_PRODUCT_REWORK.md` §41), **quarto microcorte**: `Decision` ganha `responsible: string \| null` — o último requisito explícito de §41 ainda em aberto após D053–D055. Semântica declarada e congelada: "quem é responsável por conduzir/ser dono desta Decision específica até ela ser resolvida" — deliberadamente **não** `decisor_principal` (fato geral de governança do projeto, atividade `papeis_responsabilidades`), **não** autoridade formal de governança, **não** executor do resultado da decisão, e **não** usuário/sessão autenticada do Hydra (não existe conceito de conta/login no produto). Cardinalidade 0..1; `null` é estado normal e permanentemente válido — nenhuma Decision é obrigada a ter responsável, e `decideDecision` nunca exige o campo. Valor é texto livre simples, sem estrutura nome/papel/contato e sem id de pessoa — nenhum objeto `Participant`/`Stakeholder`/`User` foi criado; normalização (trim; vazio → `null`) acontece no mesmo boundary que já normaliza `options` (`readString` em `+page.server.ts`), não no domínio, mesmo tratamento dos irmãos `options`/`dueDate`. `responsible` entra no mesmo grupo declarativo de `subject`/`options`/`dueDate`: `editDecision` (`domain/transitions.ts`) ganha um quinto parâmetro posicional e passa a recusar alteração de qualquer um dos quatro com `decision_already_decided` quando a `Decision` já está `tomada` — a única correção possível a partir daí continua sendo o `outcome`, via `editDecisionOutcome`, que nunca toca `responsible` (preservado por construção: a transição só sobrescreve `outcome`/`updatedAt` via spread do item original). `addDecision` inicializa `responsible: null`. Nenhuma relação (`DecisionAffectedWorkItem`, `Impediment.decisionId`, `Change`) é lida ou alterada por `editDecision` — a função só mapeia sobre `state.decisions`, o restante do estado é preservado por referência. `ProjectView.DecisionView` ganha o campo `responsible`, projetado por `buildDecisionView` a partir de `Decision.responsible`, sem expor nenhum outro campo de `ProjectState`. Persistência: coluna `responsible TEXT` nullable na tabela `decision`, presente tanto no `CREATE TABLE IF NOT EXISTS` fresh de `0001_init.sql` quanto via `ALTER TABLE` idempotente (`ensureDecisionResponsibleColumn`, décima segunda evolução do schema, mesmo padrão de `ensureRiskReviewedAtColumn`/`ensureImpedimentDecisionIdColumn`) para bancos criados entre D053/D055 e este corte; sem `CHECK` de obrigatoriedade. `insert`/`select` de `decision` passam a incluir a coluna; nenhuma outra tabela/ordem de escrita muda. Desserialização trata `responsible` ausente (snapshot exportado antes deste corte) como `null` — mesmo espírito de `Impediment.decisionId` — e rejeita valor que não seja string/`null`/ausente; round-trip completo preserva o campo. Nenhum backfill/inferência a partir de `decisor_principal`, `forma_decisao` ou qualquer outro Answer legado — os dois campos legados permanecem exatamente como estão, sem dual-write, porque descrevem fatos diferentes (papel geral do projeto e forma geral de decidir), não o responsável de uma Decision individual. Acompanhamento (`/tracking`) ganha um campo de texto "Responsável" dentro do formulário de edição já existente da `Decision` pendente (`decision.responsible ?? ''`), rotulado com o mesmo padrão dos demais (`decisionResponsibleLabel`: "Responsável: X" ou "Sem responsável definido"); a `Decision` tomada exibe o valor como contexto somente leitura, sem oferecer edição (o formulário de correção pós-tomada só contém `outcome`) — nenhuma nova página/seletor de pessoas/avatar/cadastro foi criado. Responsável: Matheus. | Aprovada | Estratégia `Decision.responsible: string \| null` (texto livre no próprio objeto) foi escolhida contra quatro alternativas avaliadas em scout READ-ONLY prévio: (B) identidade mínima específica de Decision — rejeitada por criar uma tabela/objeto sem consumidor além de Decision, abstração sem necessidade comprovada; (C) promover `Participant`/`Stakeholder` agora — rejeitada porque contradiria diretamente D053/D054/D055, que já deferiram esse mesmo objeto três vezes por "sem caso de produto maduro nesta fatia", e porque nenhum outro objeto do repo (`WorkItem`, `Risk`, `Milestone`, `Deliverable`) tem hoje consumidor concreto de identidade de pessoa — reuso seria hipotético, não comprovado; (D) referenciar objeto vivo existente (`AffectedGroup`) — rejeitada por não haver correspondência semântica real (`AffectedGroup` é rótulo de grupo/impacto, não pessoa individual, ver §13.3); (E) não materializar e manter S11 aberta — rejeitada por travar indefinidamente um requisito trivial de §41 sem motivo técnico. Contrato replica deliberadamente o mesmo adiamento que `Risk` fez para `owner` (D049) e o mesmo grupo declarativo de `subject`/`options`/`dueDate` que D053 já havia estabelecido. Falsificado por testes automatizados focados — `domain/transitions.spec.ts` (`Decision` nasce com `responsible: null`; `editDecision` define/troca/limpa `responsible` independentemente de `status`/`outcome`/`decidedAt`; `decideDecision` não exige `responsible` — `Decision` sem responsável também pode ser tomada; `editDecisionOutcome` corrige `outcome` sem alterar `responsible`; `editDecision` recusa alterar `subject`/`options`/`dueDate`/`responsible` de uma `Decision` já tomada), `domain/serialization.spec.ts` (round-trip completo incluindo `responsible`; `responsible` ausente importa como `null`; rejeita valor que não é string/`null`/ausente) e `server/persistence/sqlite-project-repository.spec.ts` (round-trip via SQLite preservando `responsible`; banco anterior a este corte sem a coluna abre, adiciona-a idempotentemente, `Decision`s existentes ficam com `responsible: null`, segunda abertura não falha nem duplica coluna) — e por dogfood real (servidor e banco isolados sob `os.tmpdir()`, projeto "QA S11 Decision Responsible"): criar `Decision` sem responsável (válido, "Sem responsável definido"); definir "Ana (produto)", reload preserva; trocar para "Bruno", reload preserva; limpar para `null`, reload preserva "Sem responsável definido"; definir novamente e tomar a `Decision` com resultado — sucesso mesmo com responsável preenchido; seção "Tomadas" exibe "Responsável: Ana (produto)" somente leitura, sem campo de edição disponível (só "Resultado"); console limpo; sem overflow em ~1280px/~390px. |
| D057 | 12/09/2026 | ETAPA 11 do rework ("Decision e Change", `docs/core/HYDRA_PRODUCT_REWORK.md` §41) está **concluída**: fechamento de boundary de etapa, sem nenhuma mudança de código, schema, dado persistido ou comportamento — decisão exclusivamente semântica/documental, mesmo papel que D042, D048 e D052 tiveram nos fechamentos da S8, S9 e S10. Resultado macro entregue por D053–D056: `Decision` e `Change` existem como objetos canônicos separados de nível de projeto — `Decision` com `subject`/`options`/`responsible`/`dueDate`/`status`/`outcome`/`decidedAt`, lifecycle fechado `pendente → tomada` (D053); `Change` com `statement`/`impact`, sem lifecycle (D053); a primeira semântica concreta de "entidades afetadas" provada por `DecisionAffectedWorkItem`, N:N tipada e explícita com `WorkItem` como único tipo-alvo, rejeitando relação polimórfica (D055); `ImpedimentType.decisao_pendente` reconciliado com o objeto canônico via `Impediment.decisionId` opcional, exatamente como §13.4 previa desde S6 (D054); `responsible` como último requisito explícito de §41, texto livre declarativo sem identidade de pessoa (D056); o legado misto `decisoes_mudancas_recentes` preservado como READ-LEGACY (`domain/legacy-answers.ts`, atividade `decisoes_mudancas` como `explicit_confirmation`), nunca segunda fonte de verdade; Acompanhamento (`/tracking`) operando os objetos reais nas seções "Decisões"/"Mudanças". Confronto objetivo com a lista explícita de §41 — `Decision`: decisão/`subject` ✓ (D053), opções ✓ (D053), responsável ✓ (D056), prazo ✓ (D053), entidades afetadas ✓ primeira implementação concreta via `DecisionAffectedWorkItem`/`WorkItem` (D055), resultado ✓ (D053); `Change`: objeto canônico ✓, `statement` ✓, `impact` ✓ (D053); reconciliação `decisao_pendente` ✓ (D054) — todos os itens explícitos estão satisfeitos. A regra "quando possível, mudanças devem nascer naturalmente de manipulações" (§41) não bloqueia o fechamento: nenhuma captura automática de `Change` foi criada porque não existe sinal de código determinístico e honesto para isso nesta fatia (o event log de `domain/events.ts`, D037, não cobre `Milestone` nem qualquer superfície de `Change`, mesma conclusão já registrada em D053) — "quando possível" é condicional por texto do próprio rework, não uma capacidade incondicional sendo adiada por conveniência. Da mesma forma, não é exigido que todas as classes possíveis de "entidade afetada" (`Deliverable`/`Milestone`/`Risk`/`Dependency`/`Change`/`AffectedGroup`) sejam materializadas agora — `WorkItem` é o primeiro tipo canônico provado e suficiente para satisfazer o requisito de §41; os demais continuam expansão futura por necessidade real, não segunda fonte de verdade nem pendência madura. Esta decisão **não altera** D053–D056: ela registra o boundary e **reclassifica**, sem resolver e sem cancelar, os residuais que permaneciam abertos dentro da S11, cada um DEFER até a condição semântica indicada — são capacidades futuras reabertas por necessidade real, não proibições: (a) **ampliar entidades afetadas** para `Deliverable`/`Milestone`/`Risk`/`Dependency`/`Change`/`AffectedGroup` — DEFER até existir caso de produto concreto para cada tipo. (b) **`Participant`/`Stakeholder`/pessoas canônicas** — DEFER até existir necessidade transversal real comprovada por múltiplos consumidores concretos (ver D056). (c) **captura automática/natural de `Change`** — DEFER até existir sinal estrutural confiável (ex.: `Milestone` no event log). (d) **novos `ProjectEvent`s/histórico** para `Decision`/`Change`/`DecisionAffectedWorkItem` — DEFER até necessidade de auditoria/histórico demonstrar valor de uso real. (e) **lifecycle adicional** (reversão/cancelamento/reopen/delete/score/prioridade) — DEFER até necessidade de produto comprovada. (f) **sinais/Atenções derivados** de `dueDate`/`Decision` — DEFER até existir política de produto que explique por que uma `Decision` merece atenção automática. (g) **IA** — DEFER. Nenhum desses conflitos é resolvido nesta decisão, nenhum contrato novo é criado. `docs/core/CURRENT_WORK.json` passa a apontar para `S12` ("Scheduling e Gantt", §42) com status `in_progress`: abrir o ponteiro **não** decide se as pré-condições de §42 (`WorkItem`/`Deliverable` estáveis, datas, durações, dependências, marcos) já estão maduras, nem nenhum modelo de regra de precedência, propagação, folga, caminho crítico ou Gantt — isso é trabalho de uma passagem futura, mesmo espírito de D048/D052 ao abrir S10/S11. Responsável: Matheus. | Aprovada | Auditoria de fechamento (12/09/2026) confrontou o repo real (branch `main`, árvore de trabalho desta sessão) contra a lista explícita de §41 e confirmou que todos os seis itens nomeados (`decisão`, `opções`, `responsável`, `prazo`, `entidades afetadas`, `resultado`) e os dois campos de `Change` (`statement`/`impact`) existem como objetos vivos testados, e que a reconciliação de `ImpedimentType.decisao_pendente` exigida por §13.4 está implementada — mesmo critério de boundary já aplicado em D042/D048/D052 (resultado macro existe no produto; não resta capacidade incondicional madura; o restante depende de decisão semântica futura ou de sinal de código ainda inexistente, não de "já fizemos bastante" ou suíte verde). Exceção consciente à regra genérica de decision-log-só-para-Nível-3: este pacote é Nível 1 (só documentação/ponteiro), mas D057 registra uma decisão durável de boundary e reclassificação de residuais — mesmo papel de D042/D048/D052. |
| D058 | 12/09/2026 | ETAPA 12 do rework ("Scheduling e Gantt", `docs/core/HYDRA_PRODUCT_REWORK.md` §42), **primeiro microcorte, fundacional**: `WorkItem` ganha `plannedStart: string \| null` e `durationDays: number \| null` — o primeiro fato temporal MANUAL da camada de execução, anterior a qualquer regra de precedência calculada, propagação, folga, caminho crítico, baseline ou Gantt (itens 1–6 da lista incremental de §42, nenhum implementado aqui). Decisão de boundary (scout + delta-scout READ-ONLY desta sessão): §42 exige "WorkItems/Deliverables estáveis; datas; durações; dependencies; milestones" antes de começar a lista incremental, e nenhuma etapa anterior do rework introduziu data/duração em `WorkItem` — S12 é dona desse pré-requisito fundacional, mesmo espírito de `Milestone.plannedDate` ter nascido como fato isolado antes de qualquer consumidor mais sofisticado (ETAPA 8, D040/D041). `WorkItem`, não `Deliverable`, é a unidade de scheduling deste corte — é o único objeto que ocupa tempo de execução, e é onde `Dependency` já vive na cadeia conceitual do §12; `Deliverable` não ganha nenhum campo de data neste corte. `plannedStart`/`durationDays` são um par atômico fechado: ambos `null` (sem schedule, caso normal e permanentemente válido) ou ambos preenchidos — nunca um sozinho, garantido por `setWorkItemSchedule` (`domain/transitions.ts`) e reforçado na desserialização (`validateInvariants`), mesmo molde do par `likelihood`/`impact` de `Risk` (D051). `plannedStart` é data CIVIL `YYYY-MM-DD` (mesmo `isCivilDate` de `Milestone.plannedDate`, nunca timestamp/`Date`); `durationDays` é dias corridos, inteiro >= 1, contagem INCLUSIVA — fórmula semântica congelada `fim = plannedStart + (durationDays - 1)` dias (`2026-09-12` + 1 dia = fim `2026-09-12`; `2026-09-12` + 3 dias = fim `2026-09-14`), decidida agora porque fixa sem ambiguidade a consequência futura de uma `Dependency` finish-to-start com lag zero (`início do sucessor = fim do predecessor + 1 dia`) — mas `plannedEnd` **não é calculado nem persistido** nesta fatia, e nenhum helper morto foi criado para adiantar esse cálculo sem consumidor real. `setWorkItemSchedule` cobre definir, alterar e limpar (os dois `null`) como uma única operação atômica, idempotente (gravar o mesmo par preserva o objeto por referência, inclusive `updatedAt`), e não altera `status`, `deliverableId`, `Dependency` ou `Impediment` do `WorkItem` nem de nenhum outro objeto — falsificado por teste dedicado que cria `Dependency`/`Impediment` reais e confirma que `moveWorkItem` continua recusado exclusivamente por `Impediment` aberto (D036), nunca por schedule. `Dependency` mantém integralmente a semântica atual — precedência declarada entre `WorkItem`s, nunca bloqueio operacional, sem tipo de precedência, sem lag, sem SS/FF/SF, sem propagação, e `moveWorkItem` não ganha nenhuma recusa nova por causa dela. `Milestone.plannedDate` permanece fato independente, não redefinido nem derivado de `WorkItem` — a Linha do tempo em Acompanhamento passa a ser o primeiro consumidor real do novo fato, unificando `Milestone` e `WorkItem` agendado numa mesma lista cronológica (`TrackingTimelineEntry`, agora união discriminada `{ kind: 'milestone' \| 'workItem' }`), ordenada deterministicamente por data civil ASC, depois `createdAt` ASC, depois `id` ASC (mesmo tie-breaker já usado para `Milestone`, estendido ao `id` comum da união) — sem barra, sem escala, sem "hoje", sem `plannedEnd` exposto e sem sugerir precedência/propagação que ainda não existe; `WorkItem` sem schedule completo simplesmente não entra na lista. Persistência: colunas `planned_start TEXT`/`duration_days INTEGER` na tabela `work_item`, com três `CONSTRAINT` nomeadas no `CREATE TABLE IF NOT EXISTS` fresh de `0001_init.sql` (`work_item_planned_start_format`, `work_item_duration_days_positive`, e a CHECK cruzada `work_item_schedule_pair` protegendo o par completo, mesmo padrão de `risk_assessment_pair`/D051) — mas, como toda CHECK cruzada adicionada por este corte, essa proteção de banco só cobre uma tabela criada do zero: um banco existente (criado antes deste corte, inclusive entre a ETAPA 6 e este) recebe as duas colunas só via `ALTER TABLE ADD COLUMN` idempotente (`ensureWorkItemScheduleColumns`), cada `ALTER` só podendo carregar CHECK sobre a própria coluna nova (limite do SQLite) — logo um banco upgradeado ganha formato/positividade por coluna, mas **não** a proteção do par, falsificado explicitamente em teste dedicado que grava metade do par via SQL direto num banco upgradeado (aceito) e recusa a mesma escrita num banco fresh (`work_item_schedule_pair`). Nenhum backfill, nenhuma inferência: `WorkItem`s já persistidos ficam com `plannedStart`/`durationDays` `null`; snapshot exportado antes deste corte importa os dois campos ausentes como `null`. Explicitamente **não tocado e mantido independente** — `estimativa_esforco`/`capacidade_disponivel` (Answers de projeto sobre esforço/capacidade, D048 DEFER (a)), `data_alvo_entrega` (Answer de `consolidar_plano_entrega`, texto livre não estruturado, D048 DEFER (c)) e `Deliverable.effort` — os três descrevem fatos diferentes de `durationDays` (esforço estimado, capacidade disponível, intenção narrativa de prazo geral, respectivamente, nunca elapsed time por `WorkItem`) e nenhum dos três foi lido, convertido ou usado para inferir schedule. Nenhuma data foi decidida para `Deliverable` neste corte — nem data-alvo declarada (`targetDate`) nem forecast derivado dos `WorkItem`s associados continuam sendo dois conceitos potencialmente distintos, ambos em aberto, sem que este corte feche a porta para nenhum dos dois. Este microcorte **não fecha S12** — `docs/core/CURRENT_WORK.json` permanece apontando para `S12` com status `in_progress`; nenhuma seção do rework, roadmap ou `CHANGELOG.md` foi tocada, porque nenhum boundary de etapa foi alcançado. DEFER explícito, cada um até a condição semântica que o reabre: (a) **precedência temporal calculada** (item 1 de §42) — DEFER até este corte existir de fato como fundação consumível. (b) **propagação de datas** — DEFER até haver decisão sobre conflito entre valor manual e recálculo. (c) **folga/caminho crítico** — DEFER, dependem de (a)/(b). (d) **baseline/replanejamento com histórico** — DEFER. (e) **Gantt** — DEFER até (a)–(d) existirem; `HYDRA_PRODUCT_REWORK.md` §49 proíbe Gantt sem scheduling real. (f) **`targetDate` declarada de `Deliverable`** — DEFER até decisão de produto explícita e distinta desta. (g) **forecast derivado de `Deliverable`** (agregação dos `WorkItem`s associados) — DEFER até (a) existir e até decisão explícita de que `Deliverable` deve exibir esse agregado. (h) **calendário de trabalho/dias úteis/feriados/timezone** — DEFER; este corte é dias corridos, deliberadamente sem calendário. (i) **tipos de precedência SS/FF/SF e lag não-zero** — DEFER; `Dependency` só pode honestamente suportar finish-to-start lag zero até existir caso de produto real para os demais. Responsável: Matheus. | Aprovada | `WorkItem` (não `Deliverable`, nem os dois) foi escolhido como unidade de scheduling por ser a camada de execução que efetivamente ocupa tempo e por já hospedar `Dependency` na cadeia conceitual do §12 — decisão já preparada pelo scout+delta-scout READ-ONLY desta sessão, que também descartou a hipótese de que uma futura data de `Deliverable` seria "sempre derivada": D048(c) já reserva essa decisão como própria e futura, não antecipada aqui. início+duração (fim sempre derivado, nunca persistido) foi escolhido sobre início+fim por ser o único modelo coerente com o próximo item de §42 (precedência), que precisa de fim sempre calculável a partir do predecessor; a convenção inclusiva e a fórmula do fim foram congeladas agora, mesmo sem consumidor imediato, porque mudar essa convenção depois seria migração silenciosa de significado sobre dados já gravados. `estimativa_esforco`/`capacidade_disponivel`/`data_alvo_entrega` foram mantidos intocados por serem fatos de granularidade e semântica diferentes (nível de projeto, texto livre, sem correspondência 1:1 com um `WorkItem` individual) — usá-los para inferir `durationDays` criaria uma segunda fonte do mesmo fato ou uma conversão automática de texto livre, ambas proibidas por §13.2. Falsificado por testes automatizados focados — `domain/transitions.spec.ts` (`WorkItem` nasce com `plannedStart`/`durationDays` `null`; define schedule válido; recusa `work_item_not_found`; recusa estado parcial nos dois sentidos; recusa data em formato inválido — timestamp completo, formato local, dia inexistente; recusa duração zero/negativa/fracionária/não numérica; limpa atomicamente; idempotência real preserva o objeto por referência; alterar schedule não muda `status`/`deliverableId`/`Dependency`/`Impediment`, e `moveWorkItem` continua recusado só por `Impediment` aberto), `domain/serialization.spec.ts` (round-trip completo; snapshot sem os dois campos importa como `null`/`null`; recusa schedule parcial vindo de estado persistido nos dois sentidos; recusa formato de data inválido; recusa duração não inteira ou menor que 1) e `server/persistence/sqlite-project-repository.spec.ts` (round-trip via SQLite definindo/alterando/limpando; banco anterior a este corte sem as colunas abre, ganha-as idempotentemente, `WorkItem`s existentes ficam com `null`/`null`, segunda abertura não falha nem duplica coluna; falsificação explícita de que `work_item_schedule_pair` protege um banco criado do zero mas não um banco upgradeado) — e por dogfood real, em duas rodadas (servidor e banco isolados sob `os.tmpdir()`, projetos "Dogfood S12 Scheduling" e "Dogfood S12 copy fix"): criar dois `WorkItem`s, um sem schedule; definir início + duração de 1 dia, reload preserva e aparece na Linha do tempo como "Trabalho"; alterar para 3 dias, reload preserva; limpar, reload confirma que some da Linha do tempo (seção inteira desaparece quando vazia); definir novamente; status do `WorkItem` nunca mudou em nenhum passo; `Dependency`/`Impediment` reais no projeto não recalcularam nem bloquearam o schedule; `Milestone` com `plannedDate` continuou aparecendo corretamente lado a lado; rótulos "Cronograma"/"Salvar cronograma"/"Limpar cronograma" confirmados após correção de copy (defeito de dogfood: rótulo em inglês "Schedule" destoava do restante da superfície em português); console limpo (o único evento registrado, 404 de `/favicon.ico`, é pré-existente e não relacionado); sem overflow em ~1280px/~390px. |
| D059 | 13/09/2026 | ETAPA 12 do rework ("Scheduling e Gantt", `docs/core/HYDRA_PRODUCT_REWORK.md` §42), **segundo microcorte**: regra de precedência temporal DERIVADA — item 1 da lista incremental de §42 ("regras de precedência"), consumindo o par `plannedStart`/`durationDays` fundado por D058. A regra é pura leitura, nunca invariante de escrita: nem `addDependency` nem `setWorkItemSchedule` (`domain/transitions.ts`) passam a se consultar mutuamente, e nenhum dos dois ganha recusa nova — ambos continuam aceitando estados temporalmente incompatíveis, exatamente como D058 já previa. `Dependency` mantém integralmente a semântica de D035/D058: precedência declarada, nunca bloqueio operacional; `moveWorkItem` não ganha nenhuma recusa nova por causa desta regra. Fórmula congelada: para `A` depende da conclusão de `B`, com ambos agendados, `semanticEnd(B) = B.plannedStart + (B.durationDays - 1)` dias corridos (fórmula do fim semântico já congelada por D058), `requiredStart(B) = semanticEnd(B) + 1` dia (finish-to-start, lag zero, também já congelado por D058 para a relação existente), e há conflito quando `A.plannedStart < requiredStart(B)`. Múltiplos predecessores: `knownRequiredStart = max(requiredStart(P))` entre os predecessores de `A` que possuem schedule completo — um predecessor sem schedule não participa do cálculo, nunca é tratado como conflito e nunca esconde um conflito já provado por outro predecessor agendado; esse conhecimento é deliberadamente PARCIAL, nunca apresentado como "início mínimo compatível" absoluto nem como "cronograma válido" quando existe predecessor sem schedule. Status de execução (`a_fazer`/`em_andamento`/`concluido`) é ortogonal e nunca entra no cálculo — um predecessor `concluido` cujo schedule planejado ainda viola a precedência continua produzindo o aviso, porque o fato observado é sobre coerência de PLANO, não de execução; a relação inversa também vale, e `Dependency`/`Impediment` continuam com os mesmos papéis já congelados por D035/D054. Conflito e `knownRequiredStart` são inteiramente DERIVADOS a cada leitura (`findWorkItemPrecedenceConflict`, `domain/transitions.ts`, mais o helper de aritmética `addCivilDays`, `domain/civil-date.ts`) — nenhum `plannedEnd`, `earliestStart`, status de conflito, tipo de precedência ou lag é persistido; nenhuma coluna, migration ou CHECK novos. `addCivilDays` deliberadamente usa `Date.prototype.setUTCFullYear` em vez de `Date.UTC`/do construtor `Date` com o ano como argumento: estes remapeiam legado qualquer ano de 0–99 para 1900–1999 (`Date.UTC(99, 0, 1)` vira 1999), o que corromperia silenciosamente uma data civil legitimamente aceita por `isCivilDate` nessa faixa; `setUTCFullYear` não tem esse caso especial. Um resultado fora da faixa representável em `YYYY-MM-DD` (ano < 0000 ou > 9999) falha alto em vez de produzir uma string civil truncada/inválida silenciosamente — sem nova recusa em `setWorkItemSchedule`, sem schema novo: é a própria aritmética derivada recusando devolver um valor que ela sabe ser inválido. Primeiro e único consumidor: Cronograma do WorkItem em Trabalho (`WorkItemView.precedenceConflict`, projetado em `buildWorkItemView`/`project-view.ts` a partir de `findWorkItemPrecedenceConflict` mais o título denormalizado do predecessor, mesmo espírito de `blockedBy`/`dependsOn`) — mensagem nomeia o predecessor que prova o limite e o `knownRequiredStart`, formatada por `precedenceConflictMessage` (`work-view.ts`), que só formata, nunca decide; nenhum botão que aplique a data sugerida. Linha do tempo em Acompanhamento não foi tocada. Nenhuma escrita automática, nenhum recálculo em cadeia, nenhuma alteração de terceiros — remover a `Dependency` ou o schedule do predecessor faz o conflito desaparecer por derivação, e mudar o predecessor recalcula a leitura sem mover o sucessor. Este microcorte **não fecha S12** — `docs/core/CURRENT_WORK.json` permanece apontando para `S12` com status `in_progress`; nenhuma seção do rework, roadmap ou `CHANGELOG.md` foi tocada. Com D058 (fatos temporais manuais) + D059 (esta decisão), o **item 1 de §42 ("regras de precedência") está entregue**. DEFER explícito para o item 2 de §42 em diante, cada um até a condição semântica que o reabre: propagação automática de datas; botão/ação que aplique `knownRequiredStart` como escrita; cascata downstream após alterar um predecessor; tipos de precedência SS/FF/SF e lag não-zero; folga; caminho crítico; baseline/replanejamento; Gantt (`HYDRA_PRODUCT_REWORK.md` §49 proíbe Gantt sem scheduling real). Responsável: Matheus. | Aprovada | Estratégia "correção explícita derivada" (mostrar o conflito e o limite conhecido, nunca bloquear nem escrever automaticamente) foi escolhida contra invariante rígida (rejeitada: tornaria estados hoje válidos, publicados por D058, retroativamente inválidos, e acoplaria `Dependency`/schedule que D058 desenhou como independentes) e contra "apenas mostrar sem explicar o limite" (insuficiente para "explicabilidade", princípio de produto do Hydra) — decisão já preparada por scout READ-ONLY desta sessão. Múltiplos predecessores usam o maior limite conhecido, e não "qualquer predecessor sem schedule invalida todo o cálculo", porque cada `Dependency` é uma aresta independente (D035/D058) e ausência de dado não deveria pesar mais que presença de dado — esconder um conflito real provado por um predecessor agendado só porque outro não tem schedule seria pior para o usuário do que mostrar o conhecimento parcial honestamente. `setUTCFullYear` sobre `Date.UTC` foi decisão de hardening desta mesma sessão, depois de falsificar o remapeamento de ano de dois dígitos com teste dedicado (`domain/civil-date.spec.ts`) — sem essa correção, `addCivilDays` corromperia silenciosamente qualquer data civil com ano 0–99, que `isCivilDate` aceita legitimamente. Falsificado por testes automatizados focados — `domain/civil-date.spec.ts` (aritmética de data civil: fim de mês, fevereiro bissexto e não-bissexto, virada de ano em ambas as direções, anos 0000–0099 sem o remapeamento legado de `Date.UTC`, falha explícita fora da faixa representável), `domain/transitions.spec.ts` (fórmula exata do conflito; múltiplos predecessores; predecessor sem schedule não cria nem esconde conflito; status de execução irrelevante; simetria entre criar `Dependency` depois do schedule e editar schedule depois da `Dependency`; remover `Dependency`/limpar schedule faz o conflito desaparecer; `addDependency`/`setWorkItemSchedule` sem recusa nova) e `server/application/project-use-cases.spec.ts` (ponta a ponta pelo mesmo caminho da interface: conhecimento parcial com predecessor agendado e não-agendado simultâneos; conflito desaparece quando o schedule do dependente passa a respeitar o limite) — e por dogfood real (servidor e banco isolados sob diretório temporário, projeto "S12 Dogfood Precedencia"): `B` agendado, `A` agendado em conflito e dependente de `B`, aviso aparece com o limite correto (15/09/2026); corrigir `A` para o limite faz o aviso sumir; voltar ao conflito; concluir `B` mantém o aviso (conflito de plano, não de execução); aumentar a duração de `B` recalcula o limite (18/09/2026) sem mover `A`; adicionar segundo predecessor sem schedule mantém o aviso conhecido sem afirmar compatibilidade completa; remover a `Dependency` faz o aviso desaparecer; console limpo; sem overflow em ~1280px/~390px. |
| D060 | 13/09/2026 | ETAPA 12 do rework ("Scheduling e Gantt", `docs/core/HYDRA_PRODUCT_REWORK.md` §42), **terceiro microcorte**: propagação EXPLÍCITA e ATÔMICA do cronograma — item 2 da lista incremental de §42 ("propagação"), consumindo D058 (fatos temporais manuais) e D059 (precedência derivada). Propagação é FORWARD-ONLY: satisfaz somente o limite mínimo conhecido de precedência entre predecessores agendados, nunca antecipa nenhum WorkItem — predecessor movido para mais cedo, encurtado, ou `Dependency` removida nunca puxam um sucessor para trás. Trigger é sempre gesto humano explícito no Cronograma do WorkItem (nunca automático por `addDependency`/`setWorkItemSchedule`/`removeDependency`, que continuam sem efeito colateral algum); a cascata percorre downstream agendado a partir do item aberto até estabilizar (nenhum novo conflito conhecido), com múltiplos predecessores usando o maior `requiredStart` conhecido entre os agendados. Item sem schedule nunca recebe schedule inferido — nem como raiz nem como sucessor de uma aresta — e a ausência de schedule em qualquer predecessor/sucessor torna o resultado explicitamente parcial (`partial`), nunca escondido. Usuário recebe preview (WorkItems afetados, data de origem, nova data, predecessor que a prova, e a nota de parcialidade quando aplicável) antes de qualquer escrita; confirmação é uma transição de domínio única e atômica (`applySchedulePropagation`, `domain/transitions.ts`) que SEMPRE recalcula contra o estado atual do projeto antes de aplicar — nunca confia no plano que o navegador enviou. Recalcular sozinho não bastava: se o plano recém-calculado não corresponder ao que a interface efetivamente mostrou e o usuário confirmou (WorkItems afetados, `plannedStart` de origem esperado, novo `plannedStart`, predecessor relevante, e `partial`), a confirmação é recusada por inteiro como preview obsoleto (`work_item_precedence_stale_preview`) — nunca aplicada parcialmente, nunca por uma versão global artificial que rejeitaria também mudanças irrelevantes ao plano. A ordem observável do plano (`SchedulePropagationPlan.changes`) é determinística por construção (tie-break `createdAt`/`id` do WorkItem, mesmo padrão já usado por `TrackingTimelineEntry`), independente da ordem de inserção de `Dependency` ou da ordem de visita da fila de propagação — diamonds convergem ao mesmo resultado e à mesma ordem observável. Status de execução (a_fazer/em_andamento/concluido) continua ortogonal à matemática, congelado por D059. Quando a aritmética de precedência (`addCivilDays`) exigiria uma data fora da faixa civil 0000-9999, tanto o cálculo de propagação quanto a leitura de conflito comum (`findWorkItemPrecedenceConflict`, reparo desta mesma etapa) tratam isso como estado DERIVADO seguro — `unrepresentable` para leitura (nunca lança, nunca quebra `ProjectView`) e falha explícita e atômica (zero escrita) para a operação de propagação — nunca clamp, nunca data inventada. Nenhum estado de propagação, plano, staleness ou irrepresentabilidade é persistido — tudo é recalculado a cada leitura/confirmação a partir de `plannedStart`/`durationDays`/`Dependency` já existentes (D058/D035). Zero schema novo. Primeiro e único consumidor: Cronograma do WorkItem em Trabalho; Linha do tempo em Acompanhamento não ganha UI nova, só reflete as datas resultantes por já projetar WorkItems agendados (D058). Com D058 + D059 + D060, **o item 2 de §42 ("propagação") está entregue**. DEFER explícito para os itens seguintes de §42, cada um até a condição semântica que o reabre: folga/free float/total float (item 3); caminho crítico (item 4); baseline/replanejamento com histórico (item 5); Gantt (item 6, `HYDRA_PRODUCT_REWORK.md` §49 proíbe Gantt sem scheduling real). Também continuam DEFER: tipos de precedência SS/FF/SF e lag não-zero; calendário de trabalho/dias úteis/feriados; `pinned`/`fixed`/`autoScheduled`/`provenance`/`constraintType`; auto-scheduling sem confirmação humana. Este microcorte **não fecha S12** — `docs/core/CURRENT_WORK.json` permanece apontando para `S12` com status `in_progress`; nenhuma seção do rework, roadmap ou `CHANGELOG.md` foi tocada. Responsável: Matheus. | Aprovada | Recalcular contra o estado atual (suficiente para "nunca aplicar dado obsoleto de leitura") foi julgado insuficiente para consentimento: o usuário aprova um PLANO específico, não uma instrução genérica "corrija o que for preciso agora" — aplicar um plano diferente do confirmado, mesmo que também "correto" segundo o estado atual, quebraria a garantia de que nada muda sem o usuário ver exatamente o que vai mudar antes de confirmar. Comparar contra uma expectativa extraída do próprio preview (nunca um token/versão persistido, nunca schema novo) foi escolhido sobre um mecanismo genérico de optimistic locking — mais amplo do que este caso pede e capaz de rejeitar mudanças concorrentes totalmente alheias à propagação. A comparação é deliberadamente cega ao id da `Dependency` e a títulos — nenhum dos dois foi mostrado ao usuário no preview, e rejeitar por causa deles seria a "versão global artificial" que o hardening pediu para evitar, punindo mudanças irrelevantes ao consentimento (ex.: renomear um WorkItem, ou trocar uma `Dependency` por outra entre o mesmo par produzindo o mesmo resultado). Ordenação determinística por `createdAt`/`id` foi adicionada porque o plano virou dado comparável (antes só era exibido): sem isso, dois recálculos do mesmo estado lógico poderiam divergir em ORDEM apenas por causa de como as `Dependency` foram inseridas, produzindo falsos-stale. O reparo de `findWorkItemPrecedenceConflict` (retornar um estado `unrepresentable` em vez de lançar) foi tratado como parte do mesmo contrato durável, não uma decisão separada, porque a leitura de conflito comum e o cálculo de propagação compartilham a mesma aritmética de precedência — protegê-la em só um dos dois pontos deixaria o outro quebrando `ProjectView` em produção. Falsificado por testes automatizados focados — `domain/transitions.spec.ts` (preview obsoleto: predecessor muda, `Dependency` relevante removida, alteração irrelevante ao plano não gera falso stale, `partial` muda mesmo com as mesmas datas, overflow no recálculo da confirmação, diamond com igualdade exata de array sem normalizar por sort, leitura de conflito nunca lança mesmo com estouro pelo `+1` dia final ou pela própria duração do predecessor) e `server/application/project-use-cases.spec.ts` (ponta a ponta pela mesma porta que a interface usa: preview não persiste nada, confirmação aplica quando nada mudou desde o preview, confirmação recusada como stale com zero escrita quando o predecessor muda depois do preview, leitura de `ProjectView` sobrevive a um predecessor cujo `requiredStart` estoura a faixa civil). |
| D061 | 13/09/2026 | ETAPA 12 do rework ("Scheduling e Gantt", `docs/core/HYDRA_PRODUCT_REWORK.md` §42), **quarto microcorte**: folga conhecida do cronograma — item 3 da lista incremental de §42 ("folga"), consumindo D058-D060. Escopo explicitamente estreito: **folga conhecida do cronograma ATUAL** (quanto um `WorkItem` agendado ainda pode deslizar para frente antes de pressionar o `plannedStart` de um sucessor direto já agendado) — não total float, não uma afirmação de CPM completo, e não uma promessa de margem/reserva/intenção do usuário. Fórmula FS lag zero, reaproveitando integralmente D059/D060: `requiredStart = semanticEnd(A) + 1 dia`; `slackDays` é a distância civil (`civilDaysBetween`, novo helper de subtração em `civil-date.ts`, mesmo motor `setUTCFullYear` de `addCivilDays`) entre esse `requiredStart` e o `plannedStart` do sucessor. Múltiplos sucessores diretos agendados: vence o MENOR gap conhecido; empate exato (mesmo gap, ou mesma severidade de conflito) é resolvido por tie-break determinístico neutro `createdAt`/`id` do `WorkItem` (mesmo padrão já usado por `computeSchedulePropagationPlan`, D060) — nunca pela ordem de inserção da `Dependency`, que é acidental. Sete estados nunca colapsados (`findWorkItemKnownFreeSlack`, `domain/transitions.ts`): `null` quando o próprio item não tem schedule completo OU quando o próprio item tem `precedenceConflict` de ENTRADA não resolvido (D059 domina — a folga de saída nunca é calculada sobre um schedule que o item ainda viola, e nunca duplica esse warning com um número); `unrepresentable` quando o próprio `semanticEnd+1` excede a faixa civil 0000-9999; `conflict` quando qualquer aresta de SAÍDA já viola precedência (avaliada por essa aresta específica A→S, nunca pelo conflito agregado de S — nunca produz gap negativo); `known` com o menor gap e `partial: true` quando também existe sucessor sem schedule que não entrou na conta; `unknown` quando existem `Dependency` sucessoras mas nenhuma tem sucessor agendado; `no_known_limit` quando não existe nenhuma `Dependency` sucessora (nunca `0`, nunca `Infinity` — ausência de limite não é a mesma coisa que "não pode atrasar"). Status de execução (a_fazer/em_andamento/concluido) continua ortogonal, congelado por D059. Sem traversal downstream e sem backward pass — olha só as arestas de saída diretas, nunca atravessa um diamond além do primeiro nível (item 4, caminho crítico, permanece fora de alcance por esta razão, não só por falta de anchor). Milestone.plannedDate não participa — continua fato independente, sem papel de constraint/deadline (D040/D041). Zero schema novo, zero persistência: `slackDays`/estado nunca gravados, tudo recalculado a cada leitura a partir de `plannedStart`/`durationDays`/`Dependency` já existentes. Primeiro e único consumidor: Cronograma do WorkItem em Trabalho (`WorkItemView.knownFreeSlack`, `WorkItemKnownFreeSlackView` em `types.ts`); Linha do tempo em Acompanhamento não ganha UI nova. Copy deliberadamente livre de jargão CPM (nunca "free float"/"total float"/"buffer"/"margem de segurança") — "Folga conhecida: N dias" explica apenas quanto o trabalho pode deslizar antes de pressionar o próximo trabalho dependente já agendado; `no_known_limit` nunca é apresentado como "ilimitado". Com D058 + D059 + D060 + D061, **o item 3 de §42 ("folga") está entregue no significado explicitamente estreito de folga conhecida do cronograma atual** — não no sentido de total float, que permanece indisponível até existir um anchor de rede que hoje não existe (nenhum candidato auditado — maior finish entre terminais, finish ASAP, `Milestone.plannedDate`, `Deliverable.targetDate`, baseline — sobrevive sem inventar semântica nova). Este microcorte **não fecha S12** — `docs/core/CURRENT_WORK.json` permanece apontando para `S12` com status `in_progress`; nenhuma seção do rework, roadmap ou `CHANGELOG.md` foi tocada. DEFER explícito, cada um até a condição semântica que o reabre: total float; earliest/latest pass da rede; schedule analítico ASAP; anchor de finish de projeto/rede; caminho crítico (item 4 — este corte não afirma que caminho crítico é matematicamente impossível, apenas que o modelo atual ainda não tem semântica canônica suficiente para identificá-lo); baseline/replanejamento com histórico (item 5); Gantt (item 6); `Milestone` como constraint de scheduling; `Deliverable.targetDate`; tipos de precedência SS/FF/SF e lag não-zero; calendário de dias úteis/feriados. Responsável: Matheus. | Aprovada | Folga livre local (não total float) foi a única definição auditada como honestamente calculável hoje sem inventar semântica: total float exige um anchor de finish de rede/projeto que nenhuma fonte atual (Milestone, Deliverable, baseline) sustenta sem violar um invariante já publicado (D040/D041: `Milestone.plannedDate` explicitamente não é deadline). Conflito de SAÍDA vence sobre um valor `known` positivo de outra aresta pelo mesmo motivo que D059 nunca esconde um conflito provado — mostrar "3 dias" para um sucessor enquanto outro já está em violação seria informação otimista e incompleta. Tie-break `createdAt`/`id` foi adotado em vez de uma prioridade nova de scheduling porque um empate exato de gap ou de severidade não tem critério de negócio para desempatar — só precisa ser estável e independente da ordem de inserção de `Dependency`, mesmo motivo que já levou D060 a adotar esse padrão para `SchedulePropagationPlan.changes`. `unknown` foi mantido distinto de `no_known_limit` porque são fatos diferentes: o primeiro é "existe uma pergunta de precedência real, falta o dado"; o segundo é "não existe pergunta nenhuma" — colapsar os dois esconderia a diferença entre "ainda não sabemos" e "não há o que saber", violando L5. Falsificado por testes automatizados focados — `domain/civil-date.spec.ts` (`civilDaysBetween`: mesmo dia, sinal positivo/negativo, virada de mês/ano, fevereiro bissexto e não-bissexto passo a passo, anos 0000-0099 sem remapeamento legado, recusa de shape inválido, resultado sempre inteiro) e `domain/transitions.spec.ts` (`findWorkItemKnownFreeSlack`: fórmula exata do gap incluindo o exemplo canônico de 3 dias, zero folga no limite exato, conflito de saída nunca negativo, múltiplos sucessores usa o menor gap, empate exato de gap e de severidade de conflito produz o mesmo resultado independente da ordem de inserção da `Dependency`, sucessor sem schedule marca `partial` sem impedir o cálculo dos agendados, todos os sucessores sem schedule produz `unknown` e não `no_known_limit`, ausência de `Dependency` sucessora produz `no_known_limit`, item sem schedule e precedência de entrada não resolvida produzem `null` sem duplicar o warning de D059, diamond permanece local às arestas de saída diretas sem traversal, status de execução irrelevante, overflow da própria aritmética do item nunca lança) — e por dogfood real (servidor e banco isolados sob diretório temporário, projeto "S12 Dogfood Folga"): A com sucessor B a 3 dias de folga; adicionar C mais próximo reduz para 1 dia; mover C para o limite exato mostra "Sem folga conhecida"; mover C um dia antes produz o aviso de conflito sem número; limpar o schedule de C volta a 3 dias com nota de conhecimento parcial; limpar também o schedule de B produz "Folga ainda não calculável"; remover as duas `Dependency` produz "Sem limite conhecido por dependências"; adicionar um predecessor P em conflito com A suprime o bloco de folga inteiro, deixando só o aviso de precedência de D059; console limpo; sem overflow em ~1280px/~390px. |
| D062 | 13/09/2026 | ETAPA 12 do rework ("Scheduling e Gantt", `docs/core/HYDRA_PRODUCT_REWORK.md` §42), quinto microcorte selado (hardening pós-dogfood, duas rodadas de correção sobre D058-D061): baseline explícita do cronograma — REFERÊNCIA aprovada por gesto humano explícito, nunca automática, escopo de projeto inteiro, via `previewScheduleBaselineCapture`/`captureScheduleBaseline` (`domain/transitions.ts`). Exige ao menos um WorkItem com schedule completo; recusa a captura inteira (zero escrita) quando qualquer WorkItem agendado tem `precedenceConflict` conhecido, incluindo `unrepresentable` (D059). O snapshot inclui TODOS os WorkItems existentes no instante da captura: `ProjectScheduleBaselineEntry` (`baselineId`, `workItemId`, `plannedStart: string\|null`, `durationDays: number\|null`) grava uma entry por WorkItem, par atomicamente null/null (existia sem schedule) ou ambos preenchidos (existia agendado) — ausência de entry é o único jeito de expressar "não existia ainda", nunca inferido de `WorkItem.createdAt`. Baseline é append-only e imutável: `ProjectScheduleBaseline` (`id`, `projectId`, `createdAt`, `version`) nunca sofre UPDATE; `version` é inteiro positivo monotônico por projeto (primeira = 1, próxima = max + 1, `UNIQUE(project_id, version)`) e é o ÚNICO critério de baseline ATIVA (maior version) — `createdAt` é fato temporal exibido, nunca ordenador (dois `createdAt` iguais não empatam version). `partial` é sempre DERIVADO varrendo as entries por qualquer null/null, nunca persistido. Preview é só leitura; a confirmação sempre recalcula o candidato contra o estado atual e só grava se corresponder a `expected` — divergência recusa por inteiro como `schedule_baseline_stale_preview`, zero escrita, nunca aplicação parcial; essa equivalência é por CONJUNTO de (`workItemId`, `plannedStart`, `durationDays`), nunca posicional (segunda rodada de hardening: a mesma coleção em ordem diferente nunca é stale, só membership ou schedule realmente diferentes contam). Mudanças fora do snapshot (título, status, Milestone) nunca entram no candidato e por isso nunca geram falso stale. Variâncias de início/término/duração (`computeScheduleBaselineComparison`) são sempre DERIVADAS, nunca persistidas; os quatro estados — `compared`, `removed`, `scheduled_after`, `added_after` — são provados exclusivamente pela presença/ausência de entry e pelo par null/null-vs-preenchido, nunca por comparação de timestamp. `Dependency`, status de execução e `Milestone` nunca são congelados pela baseline; a baseline não é e não serve como anchor de CPM (§42 item 4 continua DEFER, não reaberto). Primeiro e único consumidor: bloco "Referência do cronograma" em Acompanhamento (`tracking/+page.svelte`), com preview/confirmação explícitos. Baselines anteriores permanecem persistidas como histórico, mas seletor/diff/restauração de baseline antiga ficam DEFER, sem consumidor. Com este corte, §42 item 5 ("baseline quando aplicável") está entregue nesse escopo explicitamente estreito — não fecha S12: `docs/core/CURRENT_WORK.json` permanece apontando para S12, `in_progress`. DEFER explícito, cada um até a condição semântica que o reabre: caminho crítico (§42 item 4); Dependency/topologia histórica na baseline; comparação baseline vs baseline; seletor de baseline antiga; restore/delete/rename/note de baseline; Milestone/reachedAt como parte da baseline; Gantt (§42 item 6). Responsável: Matheus. | Aprovada | `version` (não `createdAt`/`id`) foi escolhido para ordem/ativa porque nem `createdAt` (relógio não estritamente monotônico, duas capturas no mesmo instante) nem `id` (UUID sem relação com tempo) provam ordem real de captura — só um contador monotônico por projeto o faz. Membership completo (entry para todo WorkItem, não só os agendados) substituiu a primeira versão do corte porque comparar `WorkItem.createdAt` contra `baseline.createdAt` para inferir "existia sem schedule" vs "não existia" é frágil sob qualquer relação de tempo não estritamente ordenada — presença/ausência de entry é o único fato que não depende de comparação de instantes. `partial` virou derivado (não persistido) porque é sempre recomputável das entries, e uma segunda fonte da mesma verdade violaria §13.2. A proteção de preview obsoleto segue o mesmo padrão de `applySchedulePropagation`/D060, mas a primeira versão comparava índice-a-índice, o que rejeitaria como stale uma reordenação sem significado algum — corrigido para comparar por `workItemId` (conjunto), único jeito de a equivalência refletir o significado real do candidato (um fato por WorkItem) em vez de um acidente de serialização/ordem. Falsificado por testes automatizados focados — `domain/transitions.spec.ts` (membership completo, `version` monotônica mesmo com `createdAt` empatado, preview obsoleto por schedule/membership mudados, ausência de falso stale por status/título, comparação por conjunto provada com entries em ordem invertida, divergência real continua stale mesmo comparando por conjunto), `domain/serialization.spec.ts` (round-trip com entry null/null, invariante do par, `version` única e positiva por projeto, snapshot antigo sem as chaves importa como coleções vazias) e `server/persistence/sqlite-project-repository.spec.ts` (round-trip fresh e upgrade de banco anterior a este corte, zero backfill, múltiplas versões) — e por dogfood real em duas rodadas (servidor e banco isolados sob diretório temporário): preview obsoleto recusado com zero escrita após schedule mudar em outra aba concorrente; nova captura bem-sucedida; WorkItem sem schedule na captura agendado depois mostra "Agendado após a referência"; WorkItem criado depois mostra "Adicionado após a referência"; rebaseline vira ativa e a anterior permanece preservada; captura com conflito de precedência conhecido recusada por inteiro; console limpo; sem overflow em ~1280px/~390px. |
| D063 | 14/09/2026 | ETAPA 12 do rework ("Scheduling e Gantt", `docs/core/HYDRA_PRODUCT_REWORK.md` §42), reconciliação documental de readiness do Gantt (scout READ-ONLY desta sessão identificou contradição real entre `§17`/`§18` e a lista incremental de `§42`): caminho crítico (item 4) **não é pré-requisito obrigatório** para o Gantt (item 6). Readiness do Gantt = scheduling suficiente para projetar honestamente o cronograma atual (`§17`, tabela de prontidão: "datas + duração + dependências suficientes" — sem caminho crítico nem baseline), não a posição de item 4 na lista incremental de `§42` (que descreve evolução de capacidades, não um gate sequencial rígido). Item 4 continua DEFER, pela mesma razão já registrada (falta decisão de anchor/target de rede) — esta decisão não o resolve, não o implementa e não cria nenhuma semântica nova de CPM/total float. Enquanto DEFER: o Gantt não pode calcular caminho crítico, não pode destacar barra como crítica e não pode afirmar total float — a ausência dessa camada analítica não bloqueia a existência da projeção Gantt em si. Item 6 pode avançar (Design/implementação) independentemente do estado de item 4. Esta decisão é exclusivamente documental — não fecha S12, não altera código, schema, domínio ou D058–D062, e não inicia Design nem implementação do Gantt. Responsável: Matheus. | Aprovada | Sem esta reconciliação, `PROJECT_STATUS.md` e a leitura literal de `§42` sustentavam a afirmação de que o Gantt está bloqueado por `§49` ("Gantt sem scheduling") quando o modelo de scheduling exigido por `§49` já existe (D058–D062) — a real ausência é só a camada opcional de caminho crítico, que `§17`/`§18` já tratam como posterior/analítica, não como pré-requisito de prontidão. |
| D064 | 15/09/2026 | ETAPA 12 do rework ("Scheduling e Gantt", `docs/core/HYDRA_PRODUCT_REWORK.md` §42) está **concluída**: fechamento de boundary de etapa, sem nenhuma mudança de código, schema, dado persistido ou comportamento nesta decisão — mesmo papel que D042, D048, D052 e D057 tiveram nos fechamentos da S8, S9, S10 e S11. Resultado macro entregue pelo repo publicado (branch `main`, `a8ec2f9`): `WorkItem` tem schedule manual real (`plannedStart`/`durationDays`, par atômico, data civil e duração inclusiva com fórmula de fim congelada — D058); precedência temporal finish-to-start lag-zero inteiramente DERIVADA, nunca invariante de escrita (D059); propagação de datas explícita e atômica, sempre preview → confirmação recalculada contra o estado atual, nunca auto-scheduling implícito (D060); folga conhecida local do cronograma atual, explicitamente distinta de total float/CPM tanto na semântica quanto na copy (D061); baseline histórica real — captura explícita por gesto humano, append-only, imutável, com comparação DERIVADA em quatro estados (`compared`/`removed`/`scheduled_after`/`added_after`) (D062); readiness do Gantt reconciliada como a de `§17` ("datas + duração + dependências suficientes"), não a posição de caminho crítico na lista incremental de `§42` (D063). Sobre essa fundação, o item 6 (Gantt) teve seis microcortes entregues, todos Nível 2 (só rotas/projeções/UI, sem tocar `domain/`, schema ou persistência, por isso sem entrada própria em decision-log até agora): rota `/cronograma` somente leitura — confirmado nesta auditoria que `+page.server.ts` não exporta `actions`, a rota é projeção pura — com readiness compartilhada (`$lib/schedule-readiness.ts`, `isCronogramaReady`, ao menos um WorkItem com schedule completo); agrupamento por Deliverable + "Sem entrega"; conectores de `Dependency` FS lag-zero e `precedenceConflict` perceptível sem inventar geometria (`cronograma-view.ts`, nunca sintetiza posição/duração além do que `WorkItemView` já expõe); marcos em lane própria; deep-link `?item=` até o WorkItem em Trabalho; baseline visual honesta (ghost de `compared`/linha tracejada standalone de `removed`, nunca geometria fabricada para `scheduled_after`/`added_after`/`compared_unrepresentable`); e, no oitavo microcorte (`a8ec2f9`), todo WorkItem CURRENT sem schedule completo e não `removed` passa a aparecer com identidade e rótulo factual "Sem cronograma", sem geometria/ghost/conector algum. Confronto objetivo com o critério de fechamento desta sessão — cada item da lista (schedule manual, datas civis/duração fechadas, precedência FS lag-zero derivada, propagação preview→confirmação, folga conhecida local nomeada corretamente, baseline histórica com comparação, Cronograma read-only como projeção do modelo, readiness suficiente, dependencies/milestones/conflitos sem geometria inventada, baseline visual honesta, "Sem cronograma" perceptível, dogfood anterior de desktop/mobile/console/estados defensivos) foi verificado contra o repo real nesta auditoria (leitura de `schedule-readiness.ts`, `cronograma-view.ts` e do diff de `a8ec2f9`) e está satisfeito — nenhum item foi aceito só por "estar na lista do backlog". Esta decisão **não altera** D058–D063: ela registra o boundary e **reclassifica**, sem resolver e sem cancelar, os residuais que permaneciam abertos dentro da S12, cada um DEFER até a condição semântica indicada — capacidades futuras reabertas por necessidade real, não proibições: (a) **caminho crítico/CPM/total float/earliest-latest** — DEFER até existir decisão de anchor/target de rede que hoje nenhuma fonte (Milestone, Deliverable, baseline) sustenta sem violar D040/D041 (já registrado por D061/D063). (b) **SS/FF/SF e lag não-zero** — DEFER até existir caso de produto real além de finish-to-start lag zero. (c) **calendário de trabalho/dias úteis/feriados/timezone** — DEFER; o modelo é deliberadamente dias corridos (D058). (d) **drag/resize e auto-scheduling** — DEFER; toda escrita de schedule continua gesto humano explícito com preview (D060). (e) **baseline como constraint/deadline** — DEFER; baseline é referência histórica, nunca trava scheduling futuro (D062). (f) **zoom Day/Week** — DEFER; refinamento de projeção/UX do Gantt, não capacidade ausente que impediria o resultado macro. (g) **seletor de múltiplas baselines históricas** — DEFER; D062 já persiste histórico, falta só consumidor. (h) **filtros/grouping adicionais e filtro de Deliverable na toolbar do Cronograma** — DEFER; refinamento de UX, agrupamento por Deliverable já existe como estrutura fixa. (i) **folga conhecida visível diretamente no Gantt** — DEFER; already calculável (D061), falta só exposição nessa superfície. Nenhum desses é capacidade ausente que impediria o resultado macro de Scheduling + Gantt — são elasticidade B (UX/projeção) ou semântica C ainda insuficiente (CPM sem anchor), nunca reabertura de D058–D063. `docs/core/CURRENT_WORK.json` passa a apontar para `S13` ("Execução como workspace completo", §43) com status `in_progress`: abrir o ponteiro **não** decide nenhum modelo, escopo ou corte de S13 — isso é trabalho de uma passagem futura, mesmo espírito de D048/D052/D057 ao abrir S10/S11/S12. Responsável: Matheus. | Aprovada | Auditoria de fechamento (15/09/2026) confrontou o repo real (branch `main`, `a8ec2f9`) contra o resultado macro explícito de Scheduling + Gantt e contra §17/§18/§42, e confirmou que os seis fatos de scheduling (D058–D063) e os seis microcortes do Gantt existem como comportamento vivo testado e dogfoodado — mesmo critério de boundary já aplicado em D042/D048/D052/D057 (resultado macro existe no produto; não resta capacidade incondicional madura; o restante depende de decisão semântica futura ou de sinal de código ainda inexistente, não de "todos os itens do backlog foram implementados" nem de "a tela renderiza"). Exceção consciente à regra genérica de decision-log-só-para-Nível-3: este pacote é Nível 1 (só documentação/ponteiro), mas D064 registra uma decisão durável de boundary e reclassificação de residuais — mesmo papel de D042/D048/D052/D057. |
| D065 | 15/09/2026 | ETAPA 13 do rework ("Execução como workspace completo", `docs/core/HYDRA_PRODUCT_REWORK.md` §43) — decisão de boundary que desbloqueia o Design Gate, sem implementar comportamento de app: **Execução deixa de ser operacionalmente dirigida por uma sequência obrigatória de `ActivityDefinition`s e passa a funcionar como workspace sobre os objetos vivos do projeto.** O estado final pretendido de `Agora` durante Execução não é "renderizar a próxima atividade metodológica genérica" (hoje `now/+page.server.ts` é, na prática, um renderer obrigatório de `ActivityDefinition` — 33+ ramos de `confirm*`), mas orientar o usuário a partir de fatos operacionais reais já existentes (`WorkItem`, `Impediment`, `Decision`, `PendingItem`, fatos de schedule/precedência onde já houver contrato). O motor de orientação genérico (`orientation-engine/next-activity.ts`, `phase-status.ts`, `project-status.ts`) **não é eliminado** por esta decisão e continua válido onde o catálogo ainda representa trabalho metodológico real (todas as fases anteriores a Execução); `NextActivityResult` **não é alargado** para carregar ações operacionais neste corte — isso permanece C (opção 2 do Scout desta sessão), rejeitada por enquanto em favor de uma projeção execução-específica separada (opção 1 do Scout), que reaproveita as seções já existentes de Acompanhamento (`tracking-view.ts`) em vez de estender o tipo central do motor. **`registrar_andamento` deixa de ser requisito obrigatório** da experiência madura de Execução — sem apagar Answers históricas, sem migração silenciosa, sem substituição imediata por outro formulário, sem criar entidade `CheckIn`, sem cadência/histórico/persistência de check-in; a frase de §43 sobre check-ins curtos perguntarem "o que mudou desde a última vez?" permanece **DEFER** até existir microcorte com necessidade real e contrato próprio. As demais atividades legadas de Execução (`foco_atual_execucao`, `impedimentos_execucao`, `decisoes_mudancas`, `atualizar_riscos`, `proxima_acao_acompanhamento`) e suas Answers **permanecem legíveis/compatíveis** até que um microcorte de implementação decida exatamente como retirar seu papel da orientação ativa — nenhum dual-write novo. **Atenções** — contrato mínimo congelado: WorkItem bloqueado / Impediment aberto são atenções operacionais factuais (já projetadas hoje como `TrackingBlockedWorkItem`); `PendingItem` aberto continua sendo atenção factual da trilha metodológica (já projetado como `attentionPendingItems`); Risk aberto **não** vira Atenção automaticamente; Decision pendente **não** vira Atenção automaticamente (uma Decision pendente pode sustentar fluxo guiado porque existe decisão concreta a tomar — isso não implica promovê-la a "Atenção"); `dueDate`/`reviewedAt` não criam urgência; nenhum score/health/ranking; schedule conflict continua pertencendo ao Cronograma por padrão, projetá-lo também em Atenções exige microcorte explícito posterior. "Atenções" deve ser entendida como **projeção sobre fatos reais**, não como sinônimo do atual campo `attentionPendingItems` — os dois fatos (bloqueio operacional e pendência metodológica) hoje vivem em campos separados da mesma `TrackingView` e precisam ser reconciliados sob um nome só no Design Gate, sem inventar um terceiro fato. **Tensão ProjectStatus/PhaseStatus registrada, não resolvida**: hoje `computePhaseStatus(execucao)` e `computeProjectStatus()` (`orientation-engine/phase-status.ts`, `project-status.ts`) dependem da conclusão literal do catálogo de Execução — `project-status.ts` exige toda atividade de toda fase `complete` como `concluída` (nem `pulada` conta) para o projeto ficar `concluído`. Esse contrato é incompatível com o destino de S13 se permanecer autoridade eterna, mas esta decisão **não redefine** a semântica de conclusão agora. Congela apenas: retirar o checklist de Execução da experiência operacional não pode fazer o projeto ficar falsamente `concluído`; também não pode deixá-lo eternamente `em_andamento` só por atividades de Execução aposentadas; a nova semântica de phase/project completion exige decisão própria, futura, antes do microcorte que alterar de fato esse gating; até essa decisão, qualquer elemento do Design Gate que dependa de "Execução concluída", percentual, transição automática ou fórmula final de conclusão é C, não comportamento congelado. **Design Gate autorizado** a partir desta decisão pode trabalhar o corredor `Agora → Quadro → Cronograma → Entregas → Riscos → Atenções → Decisões` com as bases: Quadro = capacidade atual de Trabalho, REUSE/ADAPT de navegação; Cronograma = surface real existente (`/cronograma`), REUSE; Entregas = surface real existente (`/deliverables`), REUSE; Riscos = objeto/lifecycle real existente, hoje operado em Acompanhamento, REUSE; Decisões = objeto/lifecycle real existente, hoje operado em Acompanhamento, REUSE; Atenções = recomposição factual conforme boundary acima, ADAPT; Acompanhamento atual (`/tracking`) nasceu por acreção e pode ser ABSORB/ADAPT no Gate — sua rota/composição atual não é assumida como destino final; Agora em Execução = home operacional, não renderer obrigatório de `ActivityDefinition`. O Gate decide intenção visual/interacional e ownership entre surfaces; **não decide** domínio novo, lifecycle novo, completion semantics, persistência de check-in ou ranking. `docs/core/CURRENT_WORK.json` permanece apontando para `S13`, `in_progress` — esta decisão não fecha S13, não altera código, schema, dado persistido ou comportamento de app. Responsável: Matheus. | Aprovada | Scout READ-ONLY desta sessão (inspeção de `now/+page.server.ts`, `orientation-engine/next-activity.ts`/`phase-status.ts`/`project-status.ts`, `tracking/tracking-view.ts`, `catalog/execution.ts`, `closure/closure-view.ts`) confirmou que a sequência de Execução hoje gera efeitos reais em `nextActivity`, status de fase/projeto, Agora, Mapa/Jornada, Registros e na transição para Validação/Encerramento — e que quase toda a "navegação operacional" de §43 já existe como seções vivas de Acompanhamento, não como capacidade a inventar. Sem esta decisão, o Design Gate corria risco real de inferir silenciosamente o modelo sequencial antigo (Agora = próxima `ActivityDefinition`) como destino de S13, ou de tentar resolver completion semantics/ranking/CheckIn como pré-requisito de design em vez de tratá-los como decisões próprias e futuras — exatamente o padrão que motivou esta passagem semântica antes do Gate. |
| D066 | 15/09/2026 | ETAPA 13 do rework ("Execução como workspace completo", `docs/core/HYDRA_PRODUCT_REWORK.md` §43) — **Design Gate aprovado e congelado pelo Product Owner** via Claude Design MCP ("Hydra S13 - Design Gate — Execução como workspace completo"), sobre as bases já autorizadas por D065: congela intenção visual/interacional e ownership entre surfaces do corredor `Agora → Quadro → Cronograma → Entregas → Riscos → Atenções → Decisões`; **não decide** domínio novo, completion semantics, lifecycle novo nem ranking, e **não autoriza implementação big-bang** — o primeiro microcorte de S13 permanece escolha futura, fora desta decisão. **Shell**: os sete destinos do corredor tornam-se navegação primária; Mapa/Registros/Resumo/Documento/Encerramento/Exportar/Configurações passam a secundários, presentes sem competir visualmente; cabeçalho usa semântica factual `Fase: Execução`. **Agora**: home operacional de orientação e deep-link sobre fatos reais (bloqueio, decisão pendente, trabalho em andamento, fato de schedule) — não formulário obrigatório de `ActivityDefinition`; Marco mostrado é factual (`Marco planejado`), sem política inventada de "próximo marco". **Quadro**: apresentação madura da capacidade atual de Trabalho, mesmo lifecycle de `WorkItem` (A fazer/Em andamento/Concluído), rota `/work` ganha rótulo "Quadro". **Cronograma**: reúso integral do S12 (surface real `/cronograma`), somente leitura quanto à geometria/schedule — sem drag, resize, edição direta de barra, restore de baseline, baseline selector ou baseline-as-constraint; capturar/rebaselinear referência é ação real de gestão sobre uma referência do cronograma, distinta de editar o `WorkItem`, e o Gate pode acomodá-la visualmente sem resolver a tensão com "Cronograma é somente leitura" (conflito sinalizado, não resolvido); mobile preserva identidade do trabalho fixa, apenas a régua temporal rola horizontalmente; `WorkItem` sem schedule permanece `Sem cronograma`, sem geometria inventada. **Entregas**: `Deliverable` continua distinto de `WorkItem` — camada de escopo/priorização, não de execução. **Riscos**: promovido a surface operacional própria (fora de Acompanhamento) — criar, editar, revisar, encerrar/reabrir são ações de primeira classe, não lista de leitura; sem score, sem probabilidade×impacto calculada, sem ranking; risco aberto não vira Atenção automaticamente. **Atenções**: reconcilia bloqueio operacional e pendência metodológica sob um nome só, sem criar terceiro fato; home operacional de `Impediment` com ou sem `WorkItem` associado (quando há `WorkItem`, Quadro mantém acesso contextual ao mesmo objeto, nunca duplicado); nunca inclui Risk aberto ou Decision pendente automaticamente — Decision pendente pode gerar ação concreta em Agora, o que não implica promovê-la a Atenção. **Decisões**: surface operacional própria (criar, editar, decidir, manter relações reais como ações de primeira classe); `Decision` e `Change` permanecem objetos distintos; Mudanças vira região secundária dentro de Decisões, fechada por padrão — se o uso mostrar que `Change` precisa de peso equivalente a `Decision`, essa composição pode não bastar (segundo conflito sinalizado, não resolvido). **Acompanhamento** (`/tracking`) é ABSORVIDA e some da navegação: o mapa de ownership do Gate dá destino explícito a cada responsabilidade real que hoje vive ali (resumo operacional → Agora; trabalho bloqueado → Atenções + Quadro; pendência metodológica → Atenções; gestão de impedimentos → Atenções; Riscos/Decisões → surfaces próprias; Mudanças → região secundária de Decisões; linha do tempo do cronograma → Cronograma). **Invariante responsivo**: mobile preserva capacidade operacional mesmo comprimindo/ocultando ações progressivamente (menus, "Ações ▾") — nunca remove o único caminho para manipular um objeto real vivo (Impediment, Risk, Decision, Change); não exige simetria visual feature-a-feature com desktop, exige paridade semântica/de ação; navegação secundária/de projeto não compete visualmente com o corredor operacional. **Limites de D065 preservados**: sem health score, sem ranking/prioridade global entre sinais heterogêneos, sem `CheckIn` novo, sem lifecycle/persistência inventados, sem redefinir agora `ProjectStatus`/`PhaseStatus`, sem CPM/caminho crítico/total float inventado. **Classificação A/B/C do próprio Gate**: A — conhecido/real (`WorkItem`/Quadro, `Deliverable`/Entregas, Cronograma e baseline honesta do S12, `Risk` e `Impediment` com lifecycle completo, `PendingItem`, `Decision` com lifecycle completo, `Change`, filtros por entrega já reais, navegação/deep-links factuais, composição de Agora sobre fatos reais) — A-known **não significa implementar agora**; B — elasticidade estrutural, nunca comportamento fake (metadados futuros de priorização de Entregas ainda não definidos; peso/prominência de Mudanças dentro de Decisões caso o uso mostre necessidade); C — semântica insuficiente, sem comportamento congelado (fórmula final de conclusão de fase/projeto após a retirada do checklist de Execução, percentual de conclusão da Execução, transição automática de fase, entidade/cadência/histórico de `CheckIn`, priorização/urgência/ranking global, saúde do projeto, política automática de Atenção para Risk/Decision, CPM/caminho crítico/total float). `docs/core/CURRENT_WORK.json` permanece apontando para `S13`, `in_progress` — esta decisão não altera nenhum arquivo em `app/`, não cria nem renomeia rota, não toca domain/schema/persistence, não decompõe Acompanhamento e não escolhe nem inicia o primeiro microcorte de implementação de S13. Responsável: Matheus. | Aprovada | O Design Gate foi produzido e revisado fora desta sessão e trazido para congelamento via Claude Design MCP; esta passagem apenas confirma que Matheus, como Product Owner, aprovou o artefato final antes de registrá-lo como fonte durável, e traduz seu conteúdo (shell, sete surfaces, mapa de ownership, invariante responsivo, classificação A/B/C, conflitos sinalizados) para o decision-log sem copiar o HTML/protótipo inteiro nem tratar conteúdo demonstrativo do mock (nomes de projeto, títulos de WorkItem, datas) como regra de produto. Os dois conflitos sinalizados pelo próprio Gate (gestão de referência dentro de um Cronograma nominalmente read-only; peso de Mudanças dentro de Decisões) são preservados como questão explícita de implementação/produto futura, não resolvidos silenciosamente por esta decisão, seguindo a mesma disciplina de D058–D065 de registrar tensão sem inventar contrato. |
| D067 | 15/09/2026 | **Decisão operacional de processo (não de produto)** — muda a cadência do dogfood humano durante o período de construção do Hydra que antecede a implementação de IA. Até essa fronteira, o Product Owner deixa de fazer dogfood humano profundo depois de cada microcorte de `/hydra-work`. Em vez disso: o mesmo agente que implementa o microcorte é responsável por executar sua própria verificação determinística (`hydra-verify fast`/`full` conforme o nível) e um QA de runtime focado no fluxo afetado (subir o preview real, rodar o caminho principal e as bordas relevantes, checar console, checar responsividade relevante, testar as principais microinterações, corrigir o que encontrar) — e, na ausência de blockers, segue direto até verificação final/documentação/selo na mesma passagem, sem esperar aprovação humana por corte. `/hydra-work` (`.claude/skills/hydra-work/SKILL.md`) foi atualizada: §3.5 deixa de exigir STOP-para-dogfood-humano a cada microcorte observável e passa a descrever essa QA de runtime do próprio agente, mantendo uma lista explícita de blockers que continuam exigindo parada imediata e devolução ao Product Owner (conflito real entre Design Gate e repo; decisão de domínio/semântica não congelada; necessidade de inventar lifecycle/estado/persistência/regra; contradição a D065/D066 ou outra decisão durável; comportamento subjetivo não objetivamente inferível; ação destrutiva/remota fora da autorização vigente; evidência de Design Gate falsificado; início de implementação material de IA sem o boundary abaixo liberado) — Nível 3 antes de editar (§2) continua parando o agente como já parava. `CLAUDE.md` (seção "Fluxo operacional") foi sincronizado com a mesma mudança de cadência e a mesma lista de blockers, sem alterar níveis de cerimônia, regras de commit/stage ou qualquer outra regra permanente. **Esta decisão não elimina a validação de produto por humano** — ela concentra a validação profunda, ponta a ponta, do produto como experiência integrada (navegação/composição, redundâncias e inconsistências, nomenclatura, UX que QA local não captura) num boundary único e obrigatório: **antes de qualquer implementação material de IA no Hydra, a implementação para e exige dogfood humano completo do produto pelo Product Owner**, explicitamente liberado por ele antes de IA começar — para que IA não mascare UX ruim, não compense navegação ruim, não explique fluxos estruturalmente confusos, nem carregue um produto que ainda não funciona bem sozinho. Motivação: evitar interrupção humana após cada microcorte enquanto o produto ainda está em construção guiada; manter a QA no agente executor, que já tem o contexto completo da implementação; preservar verificação determinística e runtime em todo corte, sem enfraquecer o gate; concentrar avaliação humana profunda no produto integrado, não fatiada por microcorte; tornar o dogfood completo pré-IA obrigatório, não opcional. **Escopo explicitamente limitado**: esta decisão vale para o fluxo `/hydra-work` do Hydra durante o período pré-IA descrito acima — não é regra universal de processo para outros projetos, e não altera D065/D066 nem qualquer decisão de produto/domínio. `docs/core/CURRENT_WORK.json` permanece apontando para `S13`, `in_progress` — esta decisão é puramente operacional/processual e não fecha, avança nem decide nada sobre a S13 ou sobre Riscos (`/risks`, em implementação sob D065/D066), que permanece intocada neste passe. Responsável: Matheus. | Aprovada | Instrução explícita do Product Owner nesta sessão, motivada pelo custo de interromper a implementação para dogfood humano a cada microcorte durante a fase de construção guiada do produto, preservando a intenção original de `/hydra-work`/`ENGINEERING_REMEDIATION.md` (R3, §3.5: nunca declarar um corte pronto sem QA real) ao mover essa QA para dentro do próprio agente em vez de removê-la, e reservando o dogfood humano ponta a ponta para o momento de maior alavancagem: imediatamente antes de introduzir IA, quando problemas estruturais de UX/navegação ainda são baratos de corrigir e caros de mascarar. |
| D068 | 16/09/2026 | ETAPA 13 do rework (`docs/core/HYDRA_PRODUCT_REWORK.md` §43) — **resolve o conflito sinalizado por D066** sobre ownership da capability de captura/rebaseline da referência do cronograma. Decisão: essa capability pertence à surface **Cronograma**, não a uma surface própria de baseline. Cronograma permanece somente leitura quanto ao plano/scheduling — o usuário não edita datas, dependências, duração nem geometria do planejamento diretamente nessa surface (sem drag, resize, edição direta de barra, baseline-as-constraint, CPM, caminho crítico, folga, calendário de trabalho ou scheduling novo). Capturar ou rebaselinear a referência é uma **ação explícita de gestão sobre a referência do cronograma**, distinta de editar o `WorkItem` ou a geometria do plano — por isso pode existir dentro de Cronograma sem invalidar seu caráter read-only quanto ao plano. Rebaseline continua sendo gesto consciente/explícito do usuário, nunca automático (preserva D062). Com esta decisão, Cronograma passa a ser o destino final de: visualização do cronograma; representação de baseline existente; divergências/variância contra baseline; captura inicial da referência; e rebaseline explícito. Isso não autoriza editar `WorkItem` dentro do Cronograma, editar datas/dependências pela geometria, drag/resize, baseline como constraint, CPM, caminho crítico, folga (além da já registrada em D061), calendário de trabalho ou qualquer scheduling novo — nenhum desses limites de D058–D067 é alterado. Esta decisão não muda contrato de domínio, não implementa UI, não altera scheduling, não cria surface própria de baseline e não escolhe nem inicia o microcorte seguinte de implementação. `docs/core/CURRENT_WORK.json` permanece apontando para `S13`, `in_progress`. Responsável: Matheus. | Aprovada | Instrução explícita do Product Owner nesta sessão para fechar, sem implementar, o primeiro dos dois conflitos que o próprio Design Gate sinalizou e D066 preservou como "conflito sinalizado, não resolvido": onde vive a capability de gestão da referência do cronograma quando Cronograma é nominalmente read-only. A resolução distingue edição de scheduling (proibida em Cronograma) de gestão explícita da referência histórica (permitida em Cronograma), preservando integralmente os limites de scheduling já congelados em D058–D067 e sem inventar uma surface nova só para baseline. |
| D069 | 17/09/2026 | ETAPA 13 do rework ("Execução como workspace completo", `docs/core/HYDRA_PRODUCT_REWORK.md` §43) está **concluída**: fechamento de boundary de etapa, sem nenhuma mudança de código, schema, dado persistido ou comportamento nesta decisão — mesmo papel que D042, D048, D052, D057 e D064 tiveram nos fechamentos da S8-S12. Resultado macro entregue pelo repo publicado (branch `main`, `4892b3f`): o corredor operacional `Agora → Quadro → Cronograma → Entregas → Riscos → Atenções → Decisões`, congelado pelo Design Gate em D066 sobre as bases de D065, existe como navegação primária real (`+layout.svelte` do projeto) com os sete destinos como rotas vivas (`/now`, `/work` rotulado "Quadro", `/cronograma`, `/deliverables`, `/risks`, `/attentions`, `/decisions`). Riscos e Decisões foram promovidos a surfaces operacionais próprias, fora de Acompanhamento, com ações de primeira classe (criar/editar/revisar/encerrar-reabrir para Risco; criar/editar/decidir/relacionar para Decisão), sem score, ranking nem promoção automática a partir de outro objeto. Atenções passou a reconciliar bloqueio operacional (`WorkItem`/`Impediment`) e pendência metodológica (`PendingItem`) sob um nome só, sem inventar terceiro fato. Agora ganhou orientação operacional sobre fatos reais (bloqueio, decisão pendente, trabalho em andamento, fato de schedule), sem voltar a ser renderer obrigatório de `ActivityDefinition`. D068 resolveu o primeiro conflito sinalizado pelo Gate (ownership da referência do cronograma): captura/rebaseline saíram de Acompanhamento e centralizaram em `/cronograma`, sem alterar semântica de scheduling. Acompanhamento (`/tracking`) foi integralmente absorvido e a rota deixou de existir — cada responsabilidade real que vivia ali recebeu destino explícito no corredor, e a Linha do tempo pré-readiness virou fallback de baixa fidelidade dentro do próprio Cronograma, sempre alcançável. O corte final (`4892b3f`) materializou a renomeação Trabalho → Quadro e fechou a navegação do corredor. O segundo conflito sinalizado por D066 — peso de Mudanças dentro de Decisões — **permanece DEFER**, sem resolução nesta decisão nem no repo: `Change` continua região secundária fechada por padrão dentro de Decisões, sem prominência própria; reabre se o uso real mostrar necessidade de peso equivalente a `Decision`. Completion semantics (`ProjectStatus`/`PhaseStatus` após a retirada do checklist de Execução), `CheckIn`, health score e ranking global permanecem DEFER exatamente como já registrado por D065/D066, sem reabertura nem resolução aqui. `docs/core/CURRENT_WORK.json` passa a apontar para `S14` ("Ações externas maduras", `docs/core/HYDRA_PRODUCT_REWORK.md` §44) com status `in_progress`: abrir o ponteiro **não** decide nenhum modelo, escopo ou corte de S14 — isso é trabalho de uma passagem futura, mesmo espírito de D048/D052/D057/D064 ao abrir S10-S13. S14, S15 e S16 continuam não-IA; a primeira etapa materialmente IA é S17, que segue exigindo o boundary de dogfood humano completo do produto integrado antes de começar (D067). Responsável: Matheus. | Aprovada | Reavaliação read-only desta sessão confrontou o repo real (branch `main`, `4892b3f`) contra o resultado macro explícito do corredor congelado por D066 e contra o mapa de ownership de Acompanhamento, e confirmou que os sete destinos existem como navegação viva, que Riscos e Decisões operam como surfaces próprias, que Atenções reconcilia os dois fatos sob um nome só, e que `/tracking` não existe mais como rota — mesmo critério de boundary já aplicado em D042/D048/D052/D057/D064 (resultado macro existe no produto; o que resta depende de decisão semântica futura ou de sinal de código ainda inexistente, não de "todos os itens do backlog foram implementados"). Exceção consciente à regra genérica de decision-log-só-para-Nível-3: este pacote é Nível 1 (só documentação/ponteiro), mas D069 registra uma decisão durável de boundary e reclassificação do residual ainda aberto — mesmo papel de D042/D048/D052/D057/D064. |
| D070 | 17/09/2026 | ETAPA 14 do rework ("Ações externas maduras", `docs/core/HYDRA_PRODUCT_REWORK.md` §44) — **congela a semântica mínima de `ExternalAction`/`Evidence` que o Design Gate não pode decidir sozinho**, antes de qualquer Gate ou implementação de S14; não implementa código, schema, UI nem escolhe o primeiro microcorte. Nove pontos, todos verificados sem conflito contra §44, os contratos atuais (`domain/state-types.ts`, `domain/transitions.ts`, `catalog/external-action.ts`) e o decision-log: (1) `ExternalAction` continua representando uma ação real que o usuário realiza fora do Hydra e depois reconcilia de volta no projeto — já é a semântica do comentário em `state-types.ts`. (2) `ExternalAction` permanece objeto vivo próprio, nunca subtype/alias de `Decision`, `WorkItem`, `Deliverable` ou outro objeto. (3) `ExternalAction` pode ter contexto/subject tipado: cada `kind` define quais subjects são semanticamente válidos, `project` pode ser um contexto válido, e os subjects se expandem só conforme kinds reais exigirem — sem mega-union antecipada obrigando todo kind a suportar todo objeto. (4) `affectedGroupId` (hoje o único campo de subject) é especialização histórica de `validate_affected_group`, não o contrato final de toda `ExternalAction`. (5) `EvidenceOutcome` (`confirmed | partially_confirmed | contradicted | new_discovery`) é semântica epistemológica própria da validação atual — não vira resultado universal automático de aprovação, aceite, kickoff, revisão, fornecedor, sponsor ou outros kinds futuros. (6) O resultado de uma `ExternalAction` pode ter contrato específico por kind; nenhum desses contratos é desenhado agora — só se congela que não existe obrigação de enum global único. (7) `Evidence` continua significando evidência/aprendizado obtido no mundo real, não um container genérico obrigatório para todo resultado de `ExternalAction`. (8) Não se cria por padrão uma nova surface top-level "Ações externas": `ExternalAction` é preparada/manipulada no contexto operacional que a origina, e Agora pode projetar ações abertas relevantes como orientação operacional, sem virar segunda fonte de verdade — compatível com (reforça, não contradiz) o corredor fechado de sete destinos congelado por D066 sobre D065 e fechado por D069 (`Agora → Quadro → Cronograma → Entregas → Riscos → Atenções → Decisões`); o padrão de interação entre contexto de origem, Agora, preparação, conclusão e retorno/evidência fica para o Design Gate decidir. (9) Não se decide ainda qual dos sete novos kinds (entrevista, aprovação, kickoff, revisão, aceite, fornecedor, sponsor) será o primeiro microcorte — essa escolha vem depois do Design Gate e de inspeção do repo real nesse momento. Pergunta que fica pronta para o Design Gate resolver: como uma `ExternalAction` madura é preparada, acompanhada e concluída entre o contexto que a originou e Agora, sem criar uma nova fonte de verdade nem uma surface top-level desnecessária — o Gate pode trabalhar com exemplos realistas dos kinds de §44, mas não pode inventar contratos de subject/resultado ainda não decididos aqui. `docs/core/CURRENT_WORK.json` permanece apontando para `S14`, `in_progress`: esta decisão não avança nem fecha S14, só remove um bloqueio semântico que o Scout read-only desta sessão identificou como não inferível do repo. Responsável: Matheus. | Aprovada | Instrução explícita do Product Owner nesta sessão, em resposta direta ao Scout read-only de S14: o Scout identificou corretamente que `ExternalAction.kind` fechado em `validate_affected_group` e `affectedGroupId` como único subject não generalizam para os sete kinds de §44, e que a pergunta de UI/ownership de experiência é matéria de Design Gate — mas apontar isso ao Gate sem primeiro congelar que `ExternalAction` não vira alias de outro objeto, que subjects crescem por kind (não por mega-union), e que `EvidenceOutcome` não é resultado universal, deixaria o Gate livre para inventar contratos de domínio que não são dele para decidir. Esta decisão fecha exatamente essa lacuna, sem tocar `app/`, sem desenhar UI, sem implementar kind novo e sem escolher o primeiro microcorte — mesmo padrão de decisão de boundary pré-Gate que D065 exerceu para a S13. |
| D071 | 18/09/2026 | ETAPA 14 do rework ("Ações externas maduras", `docs/core/HYDRA_PRODUCT_REWORK.md` §44) — **Design Gate aprovado e congelado pelo Product Owner**, sobre as bases já autorizadas por D070: congela o padrão visual/interacional de `ExternalAction` madura — `contexto de origem → preparação → ExternalAction aberta → orientação/projeção → retorno ao Hydra → conclusão quando a semântica daquele kind permitir` — e o mapa de ownership entre objeto, contexto de origem e shell; **não decide** subject por kind, outcome por kind, contrato de retorno, persistência genérica de retorno, assignee, prazo, prioridade, integrações externas ou automação de IA, e **não autoriza implementação big-bang**: o primeiro `ExternalAction.kind` a ser implementado permanece escolha futura, fora desta decisão. **Ownership congelado**: `ExternalAction` continua objeto vivo próprio, nunca subtype/alias de outro objeto (reforça D070); o contexto de origem que abriu a ação é seu owner operacional; a faixa global "ações em campo" e o drawer já existentes no shell podem fornecer retomada transversal do mesmo objeto, sem duplicá-lo; Agora pode, no futuro, projetar `ExternalAction` relevante como orientação, sem virar owner nem segunda fonte de verdade (reforça (8) de D070); não se cria nova surface top-level "Ações externas" (reforça (8) de D070). **Classificação A/B/C do próprio Gate — A, contrato já sustentado pelo repo**: `validate_affected_group`; a preparação já existente da ação; `ExternalAction` aberta; a faixa global e o drawer de retorno já existentes no shell; `EvidenceOutcome` atual, válido somente para esse kind (reforça (5) de D070); Evidence e conclusão atômica; leitura posterior da Evidence no contexto do `AffectedGroup`. **B, elasticidade/intenção, não comportamento**: o padrão visual de preparação pode se estender a novos kinds; `ExternalAction` pode futuramente aparecer como fato/orientação em Agora; o padrão pode se aplicar a contextos de origem diferentes do atual — nenhum desses pontos autoriza runtime fake nem implementação automática agora. **C, deliberadamente não definido, sem comportamento congelado**: qual subject cada novo kind aceita; outcome específico por kind; contrato de retorno de aprovação/kickoff/revisão/aceite/fornecedor/sponsor; persistência genérica de retorno; assignee/responsável; prazo/overdue; prioridade; integrações externas; automação por IA. Conteúdo de demonstração do protótipo (textos e fluxos de exemplo de aprovação/kickoff) é ilustrativo do padrão visual, não vira contrato de produto. `docs/core/CURRENT_WORK.json` permanece apontando para `S14`, `in_progress` — esta decisão não altera nenhum arquivo em `app/`, não cria nem renomeia rota, não toca domain/schema/persistence e não escolhe nem inicia o primeiro microcorte de implementação de S14. Responsável: Matheus. | Aprovada | O Design Gate foi produzido e revisado fora desta sessão e trazido para congelamento nesta sessão; um Scout read-only final classificou o Gate como `READY TO FREEZE` contra D070 e §44 antes desta decisão. Segue o mesmo padrão de D066 (freeze do Gate da S13): confirma a aprovação do Product Owner sobre o artefato final e traduz seu conteúdo (padrão visual/interacional, ownership, classificação A/B/C) para o decision-log sem copiar o protótipo inteiro nem tratar conteúdo demonstrativo do mock como regra de produto. Nenhum dos nove pontos congelados por D070 é reaberto ou contradito por este freeze — o Gate trabalhou estritamente dentro do espaço que D070 deixou livre para decisão de UI/experiência. |
| D072 | 18/09/2026 | ETAPA 14 do rework ("Ações externas maduras", `docs/core/HYDRA_PRODUCT_REWORK.md` §44) — **escolhe `approval` como primeiro novo `ExternalAction.kind` a implementar e congela seu contrato semântico mínimo**, sobre as bases de D070/D071; não implementa código, não toca `app/`, não revisa o Design Gate e não decide nenhum outro kind. **Subject**: `approval` aceita somente uma `Decision` existente (`domain/state-types.ts`) como subject, e essa `Decision` deve estar `pendente` no momento em que a `ExternalAction` é preparada — não generaliza para `Deliverable`, `Change`, `Risk` ou outro objeto; é a primeira instância real do ponto (3) de D070 ("cada `kind` define quais subjects são semanticamente válidos"), não uma abstração de subject genérica. **Significado**: `ExternalAction(kind = approval)` representa o ato externo de obter uma aprovação/decisão; continua objeto vivo próprio (reforça D070/D071), mas não é a fonte de verdade do resultado da aprovação. **Resultado canônico**: a própria `Decision` referenciada — `Decision.status = tomada`, `Decision.outcome`, `Decision.decidedAt` — permanecem os únicos campos de resultado; não se cria `approvalOutcome`, enum `approved`/`rejected` nem qualquer outcome duplicado dentro de `ExternalAction`. `EvidenceOutcome` (`confirmed | partially_confirmed | contradicted | new_discovery`) não se aplica a `approval` — reforça o ponto (5) de D070, que já previa este kind nominalmente como um dos que não herdam essa epistemologia. `Evidence` não é obrigatória para concluir `approval`; evidência/anexo opcional para esse kind permanece DEFER, sem contrato agora. **Semântica de conclusão**: quando o usuário retorna de uma `approval` e a `Decision` ainda está `pendente`, a operação conceitual é `decidir Decision + concluir ExternalAction` como uma única reconciliação coerente — arquitetura e implementação dessa reconciliação ficam para a implementação real, não para esta decisão. **Decision resolvida por outro caminho**: se a `Decision` for decidida (`tomada`) por outro caminho do produto enquanto a `approval` ainda está aberta, a `ExternalAction` nunca pode sobrescrever a `Decision` — ela continua a fonte canônica — e o usuário só pode reconciliar/concluir a `ExternalAction` existente sobre esse resultado já tomado, sem criar um segundo outcome; o Hydra nunca infere automaticamente que a ação externa ocorreu só porque a `Decision` mudou de status. **Continuam não definidos** (fora do escopo desta decisão): subjects e outcomes dos outros seis kinds de §44 (entrevista, kickoff, revisão, aceite, fornecedor, sponsor); `approval` sobre qualquer objeto além de `Decision`; assignee/responsável; prazo/overdue; prioridade; integrações externas; automação por IA — nenhum desses pontos é reaberto, e nenhuma abstração de subject genérica nasce como consequência documental deste registro. `docs/core/CURRENT_WORK.json` permanece apontando para `S14`, `in_progress` — esta decisão não escolhe nem inicia o microcorte de implementação de `approval`, só remove o bloqueio semântico que o Scout read-only desta sessão identificou como C em D071. Responsável: Matheus. | Aprovada | Instrução explícita do Product Owner nesta sessão, em resposta direta ao Scout read-only de S14 que, após o freeze do Gate em D071, verificou que nenhum dos sete novos kinds tinha subject e outcome semantics suficientes em fontes atuais para virar corte real, e identificou `aprovação` como o candidato mais barato por já existir um objeto (`Decision`) com lifecycle compatível (`pendente`/`tomada`, `outcome`, `decidedAt`). Esta decisão fecha exatamente a lacuna que D070 ponto (6)/(9) e D071 classificação C deixaram propositalmente aberta para `approval`, sem generalizar para os outros seis kinds nem inventar um segundo outcome que duplicasse `Decision` — preserva a advertência de D070 ponto (5) contra tratar qualquer epistemologia existente como resultado universal. |
